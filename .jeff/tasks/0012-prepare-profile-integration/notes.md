# Notes — 0012

This is the intentionally narrow P2 integration task. It depends on independently green case-tree safety (0010) and record decoding (0011).

Blocked task-9 evidence remains at `task-9-blocked` (`a7029d9e`). Use it to recover requirements and adversarial cases, not to merge the coupled implementation wholesale.

Task 0005 depends on 0012 and remains unavailable until this integration passes.
