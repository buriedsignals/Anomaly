# Notes — 0010

Operator approved splitting blocked task 0009. Evidence remains at jj bookmark `task-9-blocked` (`a7029d9e`). Reuse evidence and invariants, not blocked code wholesale.

This task owns only the base case-tree boundary: create, inspect, resume, fork, and the lifecycle preflight consumed by mutating operations. It must be independently shippable before any P2 implementation exists.
