{
  "schemaVersion": 1,
  "pipelineVersion": "6.1.2",
  "id": 27,
  "slug": "resolver-consolidation",
  "title": "Consolidate Anomaly on resolver plus durable attempt mechanics",
  "status": "blocked",
  "stage": "audit",
  "category": "code",
  "priority": "p1",
  "deps": [],
  "complexity": "complex",
  "createdAt": "2026-08-24T07:18:45Z",
  "updatedAt": "2026-08-24T14:36:26Z",
  "agents": {
    "implementer_agent_id": "ImplementAnomalyCouncilRecovery-2",
    "reviewer_agent_id": "ReviewAnomalyCouncilCorrect-2",
    "reviewer2_agent_id": "ReviewAnomalyCouncilStandards-2",
    "audit_agent_id": "AuditAnomalyCouncil-2"
  },
  "tests": {
    "authored_by_agent_id": "PlanAnomalyCouncilRecoverySol",
    "green": true,
    "evidence": [
      {
        "command": "uv run --extra test pytest tests/",
        "output": "cook: verify green (uv run --extra test pytest tests/)"
      },
      {
        "command": "uv run --extra test pytest tests/",
        "output": "cook: verify green (uv run --extra test pytest tests/)"
      },
      {
        "command": "uv run --extra test pytest tests/",
        "output": "cook: verify red (exit 1): uv run --extra test pytest tests/"
      },
      {
        "command": "uv run --extra test pytest tests/",
        "output": "cook: verify green (uv run --extra test pytest tests/)"
      },
      {
        "command": "uv run --extra test pytest tests/",
        "output": "cook: verify green (uv run --extra test pytest tests/)"
      },
      {
        "command": "uv run --extra test pytest tests/",
        "output": "cook: verify green (uv run --extra test pytest tests/)"
      }
    ],
    "gate": {
      "hash": "49aeca91b98d03771c904cdad1a4e1bf843e7ff5",
      "clean": true,
      "green": true,
      "command": "uv run --extra test pytest tests/",
      "at": "2026-08-24T14:17:25Z"
    }
  },
  "review": {
    "verdict": "needs-work",
    "reportedVerdict": "needs-work",
    "reviewer_agent_id": "ReviewAnomalyCouncilCorrect-2",
    "findings": [
      {
        "file": "src/anomaly/_attempt_workspace.py",
        "line": 64,
        "severity": "high",
        "class": "blocking",
        "kickTo": "plan",
        "what": "A hard exit after a phase is durably sealed but before promotion cleanup permanently wedges restart for phases that create a new artifact.",
        "why": "`promote_workspace` finishes `_apply`, writes the completed state, and only then calls `_finish`; therefore a process stop between `write_state` and `_finish` legitimately leaves a sealed state plus a `promoted` journal. For every non-original output, `_apply` has already left the live artifact present with entry status `applied`. On restart, `recover_interrupted_promotion` calls `_read_journal` before `_is_sealed`; `_read_journal` runs `_validate_recovery_artifacts`, whose non-original branch at line 289 requires the live artifact to be absent. It raises before the intended sealed-journal cleanup can run. P2-P7 fixed write tuples contain newly created outputs, so this is reachable and breaks AC3's durable, restart-idempotent atomic promotion contract even though the phase was successfully sealed. Add a deterministic hard-exit seam after the state write and before `_finish` for a phase with a non-original output, then separate safe sealed cleanup from unsealed rollback-provenance validation without weakening the forged-original deletion guard.",
        "refute": {
          "agent_id": "AnTerminalRefute1",
          "source": "review",
          "finding": "src/anomaly/_attempt_workspace.py:64 A hard exit after a phase is durably sealed but before promotion cleanup permanently wedges restart for phases that create a new artifact.",
          "verdict": "survives",
          "rationale": "The public restart path is genuinely wedged: a process stop after the completed state write leaves a promoted journal whose non-original applied artifact is live, but restart validates that journal before checking its sealed state and rejects the live artifact, so cleanup is never reached and every subsequent restart raises again.",
          "evidence": [
            {
              "command": "git rev-parse HEAD; git diff --exit-code 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 -- src/anomaly/_attempt_workspace.py src/anomaly/attempts.py src/anomaly/workflow.py tests/test_pipeline_walk.py",
              "output": "HEAD is exactly 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 and the focused diff exits 0."
            },
            {
              "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/_attempt_workspace.py",
              "output": "Lines 103-109 move each staged artifact live, persist entry status applied, then persist journal status promoted. Lines 48-54 write the completed state before _finish. On restart, line 64 calls _read_journal before lines 66-67 can recognize the seal and clean up; _read_journal lines 225-226 invokes artifact validation, where lines 288-291 require every non-original non-pending artifact to be absent and raise WorkflowError when the promoted live artifact exists."
            },
            {
              "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/workflow.py; git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/case.py",
              "output": "The condition is reachable from run_workflow: workflow.py:170-175 invokes promotion recovery on every public restart, and the P2 write set at lines 29-33 includes data/index.duckdb. case.py:49-58 and 127-132 create the initial directories and instruction stubs but not data/index.duckdb, so its first successful P2 promotion records original=false and leaves that artifact live."
            },
            {
              "command": "git grep -n -E 'hard_exit|interrupted_promotion|rollback|promotion\\.json' 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 -- tests/test_pipeline_walk.py",
              "output": "Focused recovery coverage contains the attempt-count crash at lines 625-660, an unsealed producer-ordered promotion rollback at 674-715, and rollback re-interruption at 722-781. It does not exercise a crash after the state contains the completed attempt while a promoted journal and a genuine original=false live artifact remain, so those passing recovery tests do not guard this window."
            }
          ]
        }
      }
    ],
    "evidence": [
      {
        "command": "git rev-parse HEAD; git status --short",
        "output": "HEAD is the requested gated hash 49aeca91b98d03771c904cdad1a4e1bf843e7ff5. Only Jeff task recording files and the council security report are modified/untracked; reviewed production and tests match the checkpoint."
      },
      {
        "command": "supplied clean full-suite gate evidence",
        "output": "The task gate records clean=true and green=true for `uv run --extra test pytest tests/` at 49aeca91b98d03771c904cdad1a4e1bf843e7ff5; the assignment reports 774/774 passed. Suite greenness does not exercise the post-seal/pre-cleanup crash window."
      },
      {
        "command": "read task.md, notes.md, context.md, council-final.json, plan-council-recovery.json, implement-council-recovery.json, current diff, bundled code-standards/testing/security skills, production modules, and AC tests",
        "output": "Re-derived all nine dispositions. The council survivors are addressed: whitespace gate IDs pause; fork destination, direct run_attempts, and standalone log_event scan before writes; forged P1/P7 original=false journals preserve live data; rollback progress is journalized; and hard-exit-count plus producer-ordered rollback tests are behavioral. One distinct sealed-promotion restart defect remains."
      },
      {
        "command": "read src/anomaly/_attempt_workspace.py:25-72,78-110,175-226,267-291; read src/anomaly/workflow.py:22-83",
        "output": "Promotion writes every entry `applied`, writes journal status `promoted`, then writes sealed state before cleanup. Recovery validates artifact coherence before checking `_is_sealed`; non-original non-pending entries require `not live.exists()`. Fixed P2-P7 write tuples include new artifacts, making the sealed/live/journal state reachable."
      },
      {
        "command": "grep `_finish|after.*state|sealed.*promotion|promoted.*restart|promotion.*sealed|write_state.*promotion|recover_interrupted_promotion` tests",
        "output": "The only match is the attempt-count hard-exit seam in tests/test_pipeline_walk.py:641. Tests cover missing attempt evidence and mid-rollback interruption, but none interrupts after promotion state sealing and before `_finish`."
      },
      {
        "command": "read tests/test_workflow.py, tests/test_pipeline_walk.py, tests/test_case.py, tests/test_events.py, tests/test_review.py, tests/test_skill.py; check tests/fixtures/**/*demo*.csv",
        "output": "Behavioral coverage exists for the pure resolver, fixed/dynamic owners, both human turns, identity separation and P3-P7 invalidation, README preservation/relative links, inert bare URLs, public path containment, deleted orchestration surfaces, and the single canonical orchestration_demo.csv. No change-detector test was added for the recovery-only registry/order implementation."
      }
    ],
    "acLedger": [
      {
        "ac": "AC1",
        "claimed": "revise",
        "rederived": "revise",
        "ok": true
      },
      {
        "ac": "AC2",
        "claimed": "reuse",
        "rederived": "reuse",
        "ok": true
      },
      {
        "ac": "AC3",
        "claimed": "revise",
        "rederived": "revise",
        "ok": false
      },
      {
        "ac": "AC4",
        "claimed": "revise",
        "rederived": "revise",
        "ok": true
      },
      {
        "ac": "AC5",
        "claimed": "revise",
        "rederived": "revise",
        "ok": true
      },
      {
        "ac": "AC6",
        "claimed": "reuse",
        "rederived": "reuse",
        "ok": true
      },
      {
        "ac": "AC7",
        "claimed": "revise",
        "rederived": "revise",
        "ok": true
      },
      {
        "ac": "AC8",
        "claimed": "reuse",
        "rederived": "reuse",
        "ok": true
      },
      {
        "ac": "AC9",
        "claimed": "reuse",
        "rederived": "reuse",
        "ok": true
      }
    ]
  },
  "audit": {
    "required": true,
    "verdict": "needs-work",
    "audit_agent_id": "AuditAnomalyCouncil-2",
    "findings": [
      {
        "file": "src/anomaly/_attempt_workspace.py",
        "line": 287,
        "severity": "high",
        "class": "blocking",
        "kickTo": "plan",
        "what": "Case-local promotion backups still act as forgeable provenance and can overwrite registry-bound live artifacts during public restart.",
        "why": "The recovery parser treats an original=true applied entry as coherent solely when its case-local backup exists. A portable unsealed case can therefore carry the exact P1 write set with data/sources.json as the applied prefix, original=true, and an attacker-selected regular file at .anomaly/attempts/P1/attempt-1/promotion-backup/data/sources.json; all later entries remain coherent pending originals. The exact-schema, registry-set, producer-order, and whole-tree checks all pass. _rollback then marks the entry rolling_back, discards the live source registry, and executes backup.replace(live), after which cleanup removes the forged journal and backup. The same construction with a full P7 applied prefix can replace README.md and erase journalist content added after the case was received. A regular file inside the untrusted portable case is not independent provenance, so the original=false deletion fix leaves an equally reachable original=true overwrite capability.",
        "cwe": "CWE-73",
        "refute": {
          "agent_id": "AnTerminalRefute4",
          "source": "audit",
          "finding": "src/anomaly/_attempt_workspace.py:287 Case-local promotion backups still act as forgeable provenance and can overwrite registry-bound live artifacts during public restart.",
          "verdict": "survives",
          "rationale": "The public run_workflow entry invokes interrupted-promotion recovery before normal resolution, an unsealed original=true applied entry is accepted solely because its case-local backup exists, and rollback then deletes the live registry-bound artifact and replaces it with that backup; the focused tests preserve this behavior for original=true backups and reject only the distinct original=false forgery.",
          "evidence": [
            {
              "command": "git rev-parse HEAD",
              "output": "49aeca91b98d03771c904cdad1a4e1bf843e7ff5"
            },
            {
              "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/workflow.py (lines 169-175)",
              "output": "case_root = Path(root); resume_case(case_root); case_root = case_root.resolve(); recover_interrupted_promotion(case_root, lambda phase: _OWNER_REGISTRY[phase][2])"
            },
            {
              "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/_attempt_workspace.py (lines 64-70, 124-130, 284-291)",
              "output": "Recovery reads the journal and, unless _is_sealed, calls _rollback. For original=true, _validate_recovery_artifacts sets coherent = backup.exists() or (status == \"applying\" and live.exists()); _rollback then executes discard_workspace(live) followed by backup.replace(live). No independent provenance check binds the backup to a producer run."
            },
            {
              "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:tests/test_pipeline_walk.py (lines 685-816)",
              "output": "test_public_restart_rolls_back_a_producer_ordered_interrupted_promotion creates a case-local data/sources.json backup, marks original=true/status=applied, calls run_workflow, and expects the backup restored; test_public_restart_rejects_false_non_original_provenance_without_deleting_live_data covers P1/P7 only with original=false/status=applied, so it does not reject an attacker-supplied original=true backup."
            },
            {
              "command": ".venv/bin/python -B -m pytest -p no:cacheprovider tests/test_pipeline_walk.py::test_public_restart_rolls_back_a_producer_ordered_interrupted_promotion tests/test_pipeline_walk.py::test_public_restart_resumes_after_a_second_interruption_during_rollback tests/test_pipeline_walk.py::test_public_restart_rejects_false_non_original_provenance_without_deleting_live_data tests/test_pipeline_walk.py::test_public_restart_rejects_untrusted_promotion_journal_without_case_mutation -q",
              "output": "Pytest did not initialize: FileNotFoundError: No usable temporary directory found; the verdict relies on the gated source and focused test inspection above, not an unexecuted test claim."
            }
          ]
        }
      },
      {
        "file": "src/anomaly/_attempt_workspace.py",
        "line": 289,
        "severity": "high",
        "class": "blocking",
        "kickTo": "plan",
        "what": "A producer-reachable interruption after first creating a phase artifact permanently blocks promotion recovery.",
        "why": "For a path that did not exist before promotion, _apply durably records status=applying and then moves the staged artifact live before recording status=applied. A process death after that move leaves original=false with a live artifact and either applying or applied status. On every public restart, _validate_recovery_artifacts requires not live.exists() for every non-original non-pending entry, rejects this genuine producer state, and prevents rollback, attempt rewind, and cleanup. P7 normally first-creates findings/findings.json, findings/report.md, findings/charts, and both receipts, so the crash window is reachable on the canonical flow. The recovery tests exercise original=true P1 rollback and forged false provenance, but not this producer-reachable first-creation interruption. This violates AC3 durable atomic promotion and restart-idempotent rollback.",
        "cwe": "CWE-754",
        "refute": {
          "agent_id": "AnTerminalRefute5",
          "source": "audit",
          "finding": "src/anomaly/_attempt_workspace.py:289 A producer-reachable interruption after first creating a phase artifact permanently blocks promotion recovery.",
          "verdict": "survives",
          "rationale": "At the gated/current-identical source, `_apply` durably records `applying`, moves a previously absent staged artifact live, and only afterward records `applied`, while every public restart validates before rollback and rejects any non-pending `original=false` entry whose live artifact exists; the focused tests omit this legitimate crash state and explicitly retain the journal when the same state is presented, so there is no upstream guard or fail-safe recovery path.",
          "evidence": [
            {
              "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly diff --no-ext-diff --unified=0 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 -- src/anomaly/_attempt_workspace.py tests/test_pipeline_walk.py",
              "output": "Exit 0; stdout was empty, confirming the current source and focused recovery tests are byte-identical to the gated hash."
            },
            {
              "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/_attempt_workspace.py (lines 93-107, 275-291)",
              "output": "Lines 93-107 set entry status to `applying` and atomically persist it, then for a staged artifact execute `staged.replace(live)`, and only afterward set/persist `applied`. Lines 275-291 route every non-original non-pending entry to `coherent = not live.exists()` and raise `WorkflowError(\"promotion journal recovery artifacts are inconsistent\")` when the moved artifact exists."
            },
            {
              "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/workflow.py (lines 66-78, 161-174)",
              "output": "The canonical P7 write set starts with `findings/findings.json`, `findings/report.md`, `findings/unresolved.md`, and `findings/charts`; public `run_workflow` calls `recover_interrupted_promotion` directly at entry, with no guard or fallback around it."
            },
            {
              "command": "read tests/test_pipeline_walk.py:674-818",
              "output": "The positive recovery tests construct only `original=True, status=applied` P1 entries. The false-provenance test constructs `original=False, status=applied`, expects public `run_workflow` to raise `WorkflowError`, and asserts `.anomaly/promotion.json` remains, confirming repeated public recovery cannot reach rollback or cleanup for the live non-original state."
            }
          ]
        }
      }
    ],
    "evidence": [
      {
        "command": "read /Users/tomvaillant/buried_signals/tools/anomaly/.jeff/tasks/0027-resolver-consolidation/security-council/security-audit-20260824-141744.md",
        "output": "The supplied deterministic scan covered all 25 src/anomaly files, reported six high regex hits, no criticals, two scanner gaps, no dependency vulnerabilities, and REVIEW. The hits were ast.literal_eval and five interpolated DuckDB statements; GitHub Actions and XSS were the scanner gaps."
      },
      {
        "command": "jj status",
        "output": "The audited product parent is exactly 49aeca91b98d03771c904cdad1a4e1bf843e7ff5. Working-copy changes are limited to Jeff task.json/journal.jsonl and the supplied scanner report; the gate parent contains the finished product and tests."
      },
      {
        "command": "read src/anomaly/detect.py:59-123,340-410,580-665; read src/anomaly/prepare.py:439-505; grep table_id src/anomaly/profile.py",
        "output": "The dynamic-execution hit is ast.literal_eval limited to a quoted scalar and catches SyntaxError/ValueError, not arbitrary eval. PRAGMA interpolation receives only positive non-bool integers from _validate_limits. Prepared table IDs are generated as tbl_ plus a SHA-256 hex digest, and profile rejects anything outside tbl_[0-9a-f]{64}; registered DuckDB views are internal prepared_<integer> names. The five SQL hits therefore have no injection path."
      },
      {
        "command": "read src/anomaly/_attempt_workspace.py:25-292; read src/anomaly/workflow.py:20-168",
        "output": "Promotion paths are fixed relative write tuples from the sole owner registry and are exact-schema/producer-order checked. Rollback progress is journalized per entry and original=true partial restores can restart. However line 287 trusts any case-local backup as original provenance, while line 289 rejects every non-original non-pending entry whose promoted live artifact exists, yielding the two blocking provenance/recovery paths reported above."
      },
      {
        "command": "read tests/test_pipeline_walk.py:625-850",
        "output": "The hard-exit attempt-count proof reconciles missing failure evidence; the producer-ordered and second-interruption rollback proofs use pre-existing P1 artifacts with original=true; the forged-provenance proof checks original=false/live-present rejection. No proof covers a genuine newly-created original=false artifact interrupted after staged.replace(live), and no proof challenges a forged original=true backup."
      },
      {
        "command": "read src/anomaly/case.py:102-290; read src/anomaly/attempts.py:29-110; read src/anomaly/events.py:25-48; read tests/test_case.py:429-449; read tests/test_events.py:304-327",
        "output": "Containment repairs pass inspection: create/inspect/resume scan unresolved roots, fork scans both unresolved source and destination namespaces before resolve/copy, direct run_attempts scans before load_snapshot or durable writes, and standalone log_event scans before mkdir/open and returns None on unsafe stores. The focused tests verify destination, direct-attempt, and direct-event external targets remain unchanged."
      },
      {
        "command": "read src/anomaly/workflow_inputs.py:25-176; read src/anomaly/owners.py:37-184; read src/anomaly/identities.py:8-105",
        "output": "Whitespace-only Gate A/B IDs are withheld before attempts; source and gate values are schema-checked; reviewer/Gate A/Gate B identities are stripped and compared with Unicode NFC casefold canonicalization; P7 completion identity requires all five files plus the charts directory kind and every descendant. AC1, AC2, AC4, and identity-dependent AC5 behavior are supported by the inspected code and focused tests."
      },
      {
        "command": "read src/anomaly/semantics.py:52-121; read src/anomaly/review.py:603-658; read src/anomaly/report.py:223-270; read pyproject.toml",
        "output": "Compound AWS_SECRET_ACCESS_KEY and OPENAI_API_KEY assignments match the shared recursive redactor used by durable failures/events/state. Dataset report text is HTML-escaped, Markdown punctuation-escaped, and HTTP(S) colons escaped to suppress bare autolinks; SVG labels are credential-redacted and XML-escaped, manually closing the scanner's XSS gap. Runtime dependencies remain pyarrow, duckdb, and pytz, with no risky new package."
      },
      {
        "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:45-58; read tests/test_pipeline_walk.py:455-555; read skills/anomaly/SKILL.md:20-100,207-260",
        "output": "The task gate binds the clean hash 49aeca91 to uv run --extra test pytest tests/ with green=true; supplied gate evidence reports 774/774 passed. The canonical demo still proves the dynamic skill/persona owners, both human gates, relative links, and exact P5→P6→Gate B→P7 order. Installed instructions use resolver/owner/attempt and completed-map authority, not the removed runner/event-resume model. AC1, AC2, AC4-AC9 have inspected support, but the two promotion findings leave AC3 unsatisfied."
      }
    ],
    "reportedVerdict": "needs-work",
    "scan": {
      "command": "/Users/tomvaillant/.omp/plugins/node_modules/@johanthoren/jeff/skills/security-auditor/scripts/review-security.sh src/anomaly --report-dir .jeff/tasks/0027-resolver-consolidation/security-council",
      "recommendation": "REVIEW",
      "reportPath": "/Users/tomvaillant/buried_signals/tools/anomaly/.jeff/tasks/0027-resolver-consolidation/security-council/security-audit-20260824-141744.md"
    },
    "coverage": [
      {
        "category": "secrets",
        "status": "covered_no_hits"
      },
      {
        "category": "injection_sql",
        "status": "covered_no_hits"
      },
      {
        "category": "injection_command",
        "status": "covered_no_hits"
      },
      {
        "category": "path_traversal",
        "status": "covered_with_hits"
      },
      {
        "category": "insecure_deserialization",
        "status": "covered_no_hits"
      },
      {
        "category": "weak_crypto",
        "status": "covered_no_hits"
      },
      {
        "category": "dynamic_execution",
        "status": "covered_no_hits"
      },
      {
        "category": "tls_transport",
        "status": "covered_no_hits"
      },
      {
        "category": "xss",
        "status": "covered_no_hits"
      },
      {
        "category": "sensitive_logging",
        "status": "covered_no_hits"
      },
      {
        "category": "insecure_permissions",
        "status": "covered_no_hits"
      }
    ]
  },
  "commits": [],
  "kickbacks": [
    {
      "from": "review",
      "to": "plan",
      "reason": "Invocation-wide gate decisions can bypass both required human turn boundaries.; The resolver treats presence-only, incomplete phase inputs as ready and can spend attempts before boundary validation.; The attempt utility does not provide atomic phase staging; deterministic owners write directly into the live case.; README projection deletes journalist-authored content that exactly matches the legacy Outputs block.; Public resume, inspect, and direct writer paths do not enforce the required whole-case no-symlink scan.; The multi-phase loop does not recheck whole-case containment after dynamic reasoning owners mutate the case.; Canonical-equivalent Unicode identities bypass reviewer independence at both gates.; A completed snapshot with missing authoritative identities is trusted as P7-complete.; The retained retry tests no longer verify credential-redacted durable failure evidence.; Deleting the invalidation matrix left most authoritative identity classes without earliest-suffix coverage.; No public test proves that future Gate A or Gate B decisions cannot cross a turn.; README preservation coverage omits completed restart and failed recomputation transitions.; The Markdown inertness test does not contain a standalone bare URL.; Phase attempts do not stage or atomically promote owner outputs.; The resolver's missing-input boundary is schema-blind and omits required timestamps.; The multi-phase dispatcher performs whole-case containment validation only once.; The authoritative-identity invalidation matrix was deleted without replacement coverage for P3-P6.; README preservation tests omit completed restart and failed recomputation with journalist content.; The inert-Markdown test does not exercise a standalone bare URL.",
      "at": "2026-08-24T10:06:17Z",
      "findings": [
        {
          "source": "review",
          "file": "src/anomaly/workflow.py",
          "line": 109,
          "what": "Invocation-wide gate decisions can bypass both required human turn boundaries.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "src/anomaly/workflow.py",
          "line": 35,
          "what": "The resolver treats presence-only, incomplete phase inputs as ready and can spend attempts before boundary validation.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "src/anomaly/owners.py",
          "line": 59,
          "what": "The attempt utility does not provide atomic phase staging; deterministic owners write directly into the live case.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "src/anomaly/readme.py",
          "line": 36,
          "what": "README projection deletes journalist-authored content that exactly matches the legacy Outputs block.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "src/anomaly/case.py",
          "line": 146,
          "what": "Public resume, inspect, and direct writer paths do not enforce the required whole-case no-symlink scan.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "src/anomaly/workflow.py",
          "line": 119,
          "what": "The multi-phase loop does not recheck whole-case containment after dynamic reasoning owners mutate the case.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "src/anomaly/owners.py",
          "line": 122,
          "what": "Canonical-equivalent Unicode identities bypass reviewer independence at both gates.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "src/anomaly/state.py",
          "line": 39,
          "what": "A completed snapshot with missing authoritative identities is trusted as P7-complete.",
          "kickTo": "implement"
        },
        {
          "source": "review",
          "file": "tests/test_pipeline_walk.py",
          "line": 263,
          "what": "The retained retry tests no longer verify credential-redacted durable failure evidence.",
          "kickTo": "plan"
        },
        {
          "source": "review",
          "file": "tests/test_pipeline_walk.py",
          "line": 349,
          "what": "Deleting the invalidation matrix left most authoritative identity classes without earliest-suffix coverage.",
          "kickTo": "plan"
        },
        {
          "source": "review",
          "file": "tests/test_pipeline_walk.py",
          "line": 185,
          "what": "No public test proves that future Gate A or Gate B decisions cannot cross a turn.",
          "kickTo": "plan"
        },
        {
          "source": "review",
          "file": "tests/test_pipeline_walk.py",
          "line": 438,
          "what": "README preservation coverage omits completed restart and failed recomputation transitions.",
          "kickTo": "plan"
        },
        {
          "source": "review",
          "file": "tests/test_review.py",
          "line": 425,
          "what": "The Markdown inertness test does not contain a standalone bare URL.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "src/anomaly/owners.py",
          "line": 59,
          "what": "Phase attempts do not stage or atomically promote owner outputs.",
          "kickTo": "implement"
        },
        {
          "source": "review2",
          "file": "src/anomaly/workflow.py",
          "line": 29,
          "what": "The resolver's missing-input boundary is schema-blind and omits required timestamps.",
          "kickTo": "implement"
        },
        {
          "source": "review2",
          "file": "src/anomaly/workflow.py",
          "line": 111,
          "what": "The multi-phase dispatcher performs whole-case containment validation only once.",
          "kickTo": "implement"
        },
        {
          "source": "review2",
          "file": "tests/test_pipeline_walk.py",
          "line": 348,
          "what": "The authoritative-identity invalidation matrix was deleted without replacement coverage for P3-P6.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "tests/test_pipeline_walk.py",
          "line": 438,
          "what": "README preservation tests omit completed restart and failed recomputation with journalist content.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "tests/test_review.py",
          "line": 425,
          "what": "The inert-Markdown test does not exercise a standalone bare URL.",
          "kickTo": "plan"
        }
      ]
    },
    {
      "from": "audit",
      "to": "implement",
      "reason": "Direct source registration can follow an existing in-case symlink and overwrite another case artifact.; The dispatcher scans the case only once before a multi-owner loop, so a dynamic owner can introduce a link that later phases follow.; A completed snapshot with missing artifact identities is trusted as complete.; Canonical-equivalent Unicode identities bypass Gate A, reviewer, and Gate B role separation.; Future Gate A and Gate B decisions are retained across the resolver loop and consumed without a fresh human turn.; Attempt failures persist common credentials because the central error sanitizer recognizes only three token prefixes.; A failed multi-source P1 attempt partially commits earlier sources instead of preserving atomic attempt semantics.; README projection can delete journalist-authored Markdown that happens to equal the legacy Outputs block.",
      "at": "2026-08-24T10:06:17Z",
      "findings": [
        {
          "source": "audit",
          "file": "src/anomaly/acquire.py",
          "line": 46,
          "what": "Direct source registration can follow an existing in-case symlink and overwrite another case artifact.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/workflow.py",
          "line": 111,
          "what": "The dispatcher scans the case only once before a multi-owner loop, so a dynamic owner can introduce a link that later phases follow.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/state.py",
          "line": 38,
          "what": "A completed snapshot with missing artifact identities is trusted as complete.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/owners.py",
          "line": 122,
          "what": "Canonical-equivalent Unicode identities bypass Gate A, reviewer, and Gate B role separation.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/workflow.py",
          "line": 109,
          "what": "Future Gate A and Gate B decisions are retained across the resolver loop and consumed without a fresh human turn.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/state.py",
          "line": 15,
          "what": "Attempt failures persist common credentials because the central error sanitizer recognizes only three token prefixes.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/owners.py",
          "line": 92,
          "what": "A failed multi-source P1 attempt partially commits earlier sources instead of preserving atomic attempt semantics.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/readme.py",
          "line": 36,
          "what": "README projection can delete journalist-authored Markdown that happens to equal the legacy Outputs block.",
          "kickTo": "implement"
        }
      ]
    },
    {
      "from": "review",
      "to": "plan",
      "reason": "The recovered full-suite gate is 735/736 green; the sole failure is the original pure-resolver fixture still expecting P3 ready without supplying the newly required now input. Production correctly pauses before attempts. Plan must migrate that stale existing assertion to the accepted schema-aware boundary without changing production.",
      "at": "2026-08-24T10:50:39Z"
    },
    {
      "from": "review",
      "to": "plan",
      "reason": "The repaired schema-aware input boundary still treats unconsumable source and gate values as complete capabilities.; A process interruption can consume an attempt without failure evidence and eventually strand run_workflow in an infinite ready/no-op loop.; The deleted runner/verb/event-resume model remains as a stale test expectation and installed skill contract.; The recovered whole-case containment boundary still omits the public create path.; P7 completion does not validate the identities or existence of the outputs that README advertises.; The input-capability boundary remains only shape-aware, not schema-aware, for source and human-decision values.; No behavioral test proves journalled promotion rollback or interrupted-promotion recovery.; Installed documentation still resurrects the removed runner and states the wrong resume authority and constant owner.",
      "at": "2026-08-24T11:11:53Z",
      "findings": [
        {
          "source": "review",
          "file": "src/anomaly/workflow_inputs.py",
          "line": 61,
          "what": "The repaired schema-aware input boundary still treats unconsumable source and gate values as complete capabilities.",
          "kickTo": "plan"
        },
        {
          "source": "review",
          "file": "src/anomaly/attempts.py",
          "line": 48,
          "what": "A process interruption can consume an attempt without failure evidence and eventually strand run_workflow in an infinite ready/no-op loop.",
          "kickTo": "plan"
        },
        {
          "source": "review",
          "file": "tests/test_skill.py",
          "line": 76,
          "what": "The deleted runner/verb/event-resume model remains as a stale test expectation and installed skill contract.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "src/anomaly/case.py",
          "line": 101,
          "what": "The recovered whole-case containment boundary still omits the public create path.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "src/anomaly/identities.py",
          "line": 17,
          "what": "P7 completion does not validate the identities or existence of the outputs that README advertises.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "src/anomaly/workflow_inputs.py",
          "line": 39,
          "what": "The input-capability boundary remains only shape-aware, not schema-aware, for source and human-decision values.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "tests/test_pipeline_walk.py",
          "line": 518,
          "what": "No behavioral test proves journalled promotion rollback or interrupted-promotion recovery.",
          "kickTo": "plan"
        },
        {
          "source": "review2",
          "file": "skills/anomaly/SKILL.md",
          "line": 46,
          "what": "Installed documentation still resurrects the removed runner and states the wrong resume authority and constant owner.",
          "kickTo": "implement"
        }
      ]
    },
    {
      "from": "audit",
      "to": "implement",
      "reason": "A case-controlled promotion journal can delete arbitrary regular files or directories inside the case during public resume recovery.; The shared durable-error sanitizer misses standard compound credential assignments such as AWS_SECRET_ACCESS_KEY and OPENAI_API_KEY.",
      "at": "2026-08-24T11:11:53Z",
      "findings": [
        {
          "source": "audit",
          "file": "src/anomaly/_attempt_workspace.py",
          "line": 109,
          "what": "A case-controlled promotion journal can delete arbitrary regular files or directories inside the case during public resume recovery.",
          "kickTo": "implement"
        },
        {
          "source": "audit",
          "file": "src/anomaly/semantics.py",
          "line": 52,
          "what": "The shared durable-error sanitizer misses standard compound credential assignments such as AWS_SECRET_ACCESS_KEY and OPENAI_API_KEY.",
          "kickTo": "implement"
        }
      ]
    },
    {
      "from": "review",
      "to": "plan",
      "reason": "Council block: Registry binding still lets an untrusted promotion journal delete a legitimate phase-owned artifact by lying that it did not originally exist.; The public fork writer still follows a symlinked destination ancestor and can create the child case outside the caller-selected path.; Whitespace-only Gate A and Gate B IDs are still classified as complete human decisions.; The interrupted-attempt test never exercises restart reconciliation for a counted attempt with no failure evidence.; The claimed valid interrupted-promotion test constructs a journal state the sequential promoter cannot produce.; The public fork writer follows a symlinked destination namespace and can copy a case outside the caller's requested tree.; A second interruption during promotion rollback leaves a valid recovery state that the next restart rejects permanently.; A schema-valid, registry-bound forged P7 promotion journal can still delete the case README and its journalist-authored content during restart.; The documented public run_attempts utility writes before enforcing whole-case no-symlink containment.; The standalone public log_event writer follows case-controlled symlinks outside the case.",
      "at": "2026-08-24T13:26:58Z"
    }
  ],
  "blockedReason": "Registry binding still lets an untrusted promotion journal delete a legitimate phase-owned artifact by lying that it did not originally exist.; The public fork writer still follows a symlinked destination ancestor and can create the child case outside the caller-selected path.; Whitespace-only Gate A and Gate B IDs are still classified as complete human decisions.; The interrupted-attempt test never exercises restart reconciliation for a counted attempt with no failure evidence.; The claimed valid interrupted-promotion test constructs a journal state the sequential promoter cannot produce.; The public fork writer follows a symlinked destination namespace and can copy a case outside the caller's requested tree.; A second interruption during promotion rollback leaves a valid recovery state that the next restart rejects permanently.; A schema-valid, registry-bound forged P7 promotion journal can still delete the case README and its journalist-authored content during restart.; The documented public run_attempts utility writes before enforcing whole-case no-symlink containment.; The standalone public log_event writer follows case-controlled symlinks outside the case.",
  "abandonReason": null,
  "plan": {
    "result": "red",
    "slices": [
      "Validate whitespace gate IDs and enforce pre-write containment for fork destinations, direct attempts, and direct event logging.",
      "Fail closed on forged non-original promotion provenance and require producer-reachable journal ordering.",
      "Persist rollback progress so a second interruption can restart, restore in reverse producer order, rewind, and clean up.",
      "Implement against the focused public-behavior proofs, then rerun the requested three-module suite."
    ],
    "testFiles": [
      "tests/test_pipeline_walk.py",
      "tests/test_case.py",
      "tests/test_events.py"
    ],
    "redRun": {
      "command": "uv run --extra test pytest tests/test_pipeline_walk.py tests/test_case.py tests/test_events.py -q",
      "output": "8 failed, 206 passed in 8.69s (exit 1)"
    },
    "escalation": null,
    "refactorOpportunity": null
  },
  "implement": {
    "agent_id": "ImplementAnomalyCouncilRecovery-2",
    "result": "green",
    "files": [
      "src/anomaly/_attempt_workspace.py",
      "src/anomaly/attempts.py",
      "src/anomaly/case.py",
      "src/anomaly/events.py",
      "src/anomaly/workflow_inputs.py"
    ],
    "greenRun": {
      "command": "uv run --extra test pytest tests/test_pipeline_walk.py tests/test_case.py tests/test_events.py -q",
      "output": "214 passed in 8.33s"
    }
  },
  "refactor": {
    "agent_id": "RefactorAnomalySecondRepair",
    "result": "clean",
    "files": [],
    "outsideDiff": [],
    "greenRun": {
      "command": "uv run --extra test pytest tests/test_pipeline_walk.py::test_value_invalid_source_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_value_invalid_gate_a_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_value_invalid_gate_b_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_public_dispatcher_persists_three_failed_attempts_and_blocks tests/test_pipeline_walk.py::test_interrupted_attempts_recover_with_evidence_and_stop_at_the_limit tests/test_pipeline_walk.py::test_public_restart_rolls_back_an_interrupted_promotion_before_retry tests/test_pipeline_walk.py::test_public_restart_rejects_untrusted_promotion_journal_without_case_mutation tests/test_pipeline_walk.py::test_failed_reasoning_attempts_redact_credentials_from_all_durable_evidence tests/test_pipeline_walk.py::test_public_dispatcher_invalidates_changed_gate_b_from_p7 tests/test_pipeline_walk.py::test_changed_or_missing_p7_output_demotes_completion_and_readme tests/test_case.py::test_create_case_rejects_a_symlink_before_writing tests/test_skill.py::test_skill_contracts_durable_state_bounded_retries_and_portable_paths",
      "output": "34 passed in 2.70s"
    },
    "summary": [
      "No production refactor applied: the second repair already deletes the superseded shape-only input helpers and stale runner/Verbs/event-resume prose. The registry write-set callback is the smallest direct derivation from _OWNER_REGISTRY; replacing it would add the prohibited second phase map or couple promotion recovery to the registry tuple shape.",
      "Applied the authoritative code-standards guidance resolved through skill://code-standards and preserved all second-repair behavior."
    ]
  },
  "review2": {
    "verdict": "needs-work",
    "reportedVerdict": "needs-work",
    "reviewer_agent_id": "ReviewAnomalyCouncilStandards-2",
    "findings": [
      {
        "file": "src/anomaly/_attempt_workspace.py",
        "line": 119,
        "severity": "high",
        "class": "blocking",
        "kickTo": "plan",
        "what": "Promotion recovery still lets forged original=true metadata and an attacker-controlled backup delete and replace a live registry-bound artifact, while its original=false rule rejects a genuine interrupted promotion of a newly created artifact.",
        "why": "For an original=true/applied entry, _validate_recovery_artifacts accepts backup.exists() as sufficient provenance at lines 281-284. Public restart then enters _rollback, calls discard_workspace(live), and backup.replace(live) at lines 119-124. A portable case can therefore forge the exact ordered P1 or P7 write set, place arbitrary regular bytes under attempt-1/promotion-backup/data/sources.json or README.md, and have run_workflow replace the legitimate live artifact before state identity validation. Conversely, the original=false branch requires live absence, so a genuine crash after _apply has installed a brand-new P7 output and journaled it applied is rejected forever. The new tests cover false original=false deletion claims and existing-original rollback only; they never cover forged original=true backup replacement or genuine newly-created-artifact recovery. This violates AC3 atomic recovery and AC5 journalist-content preservation. Redesign the recovery authority so no case-supplied original bit or backup alone authorizes destructive replacement, while retaining restart-idempotent recovery for producer-created states; add public-restart proofs for both forged original=true P1/P7 backups and a genuine interrupted new-artifact promotion.",
        "refute": {
          "agent_id": "AnTerminalRefute2",
          "source": "review2",
          "finding": "src/anomaly/_attempt_workspace.py:119 Promotion recovery still lets forged original=true metadata and an attacker-controlled backup delete and replace a live registry-bound artifact, while its original=false rule rejects a genuine interrupted promotion of a newly created artifact.",
          "verdict": "survives",
          "rationale": "The public restart path reaches rollback before any identity binding, where an unsealed, structurally valid journal can use original=true plus any regular case-local backup to replace a live registry-bound artifact, while the validator's unconditional live-absence requirement for original=false rejects the producer-reachable state created immediately after a new staged artifact is installed.",
          "evidence": [
            {
              "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:50-80",
              "output": "The recorded clean, green gate hash is exactly 49aeca91b98d03771c904cdad1a4e1bf843e7ff5, and recorded checkpoint evidence says reviewed production and tests match that hash."
            },
            {
              "command": "read src/anomaly/workflow.py:158-205; src/anomaly/case.py:139-152,257-304",
              "output": "run_workflow calls resume_case and then recover_interrupted_promotion before load_snapshot or identity validation; resume_case rejects links, special files, and executables but accepts ordinary attacker-controlled promotion.json and backup files inside a portable case."
            },
            {
              "command": "read src/anomaly/_attempt_workspace.py:57-70,109-130,166-299",
              "output": "For an unsealed journal, recovery validates artifacts and calls _rollback; original=true/applied is accepted whenever backup.exists(), after which rollback discards live and backup.replace(live), with no digest or trusted provenance binding. For every original=false non-pending state, validation instead requires not live.exists()."
            },
            {
              "command": "read src/anomaly/_attempt_workspace.py:82-107; src/anomaly/case.py:49-58,127-137; src/anomaly/workflow.py:68-79",
              "output": "_apply durably marks an entry applying, then staged.replace(live), then marks it applied, so a crash after replacement leaves a genuine original=false entry with live present. P7 writes newly created artifacts such as findings/report.md and findings/findings.json, which create_case does not initialize, making this rejected state producer-reachable."
            },
            {
              "command": "read tests/test_pipeline_walk.py:191-260,674-849",
              "output": "Focused recovery tests cover trusted existing-original rollback, rollback re-interruption, and forged original=false metadata against already-live P1/P7 artifacts; they do not cover forged original=true backup replacement or recovery after installation of a genuinely new original=false artifact."
            }
          ]
        }
      },
      {
        "file": "src/anomaly/owners.py",
        "line": 28,
        "severity": "medium",
        "class": "blocking",
        "kickTo": "implement",
        "what": "The consolidated dynamic-review boundary still exposes two live names for the same attestation field instead of a clean alias-free contract.",
        "why": "_REVIEW_FIELDS accepts both attestation and independent_attestation, and _review_owner_result translates the former into the latter at lines 146-149. The split is reachable: agents/anomaly-data-reviewer.md documents attestation, while the canonical demo fake in tests/test_pipeline_walk.py returns independent_attestation. This is a compatibility alias and a second schema authority, contrary to AC8 and the explicit no-alias clean cutover. Select one canonical field, migrate the reviewer instructions and every caller/test to it, and delete the translation and duplicate-field branch.",
        "refute": {
          "agent_id": "AnTerminalRefute3",
          "source": "review2",
          "finding": "src/anomaly/owners.py:28 The consolidated dynamic-review boundary still exposes two live names for the same attestation field instead of a clean alias-free contract.",
          "verdict": "survives",
          "rationale": "The public run_workflow → invoke_resolved_owner → consume_owner path applies no upstream shape guard, and _review_owner_result demonstrably accepts both the installed persona’s attestation field and the focused recovery fixture’s independent_attestation field as the same canonical value, so the duplicate live contract is reachable and violates the task’s explicit unused-alias removal criterion.",
          "evidence": [
            {
              "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly rev-parse HEAD",
              "output": "49aeca91b98d03771c904cdad1a4e1bf843e7ff5"
            },
            {
              "command": "read src/anomaly/workflow.py:135-213",
              "output": "run_workflow wraps the supplied invoke callback with invoke_resolved_owner; invoke_resolved_owner returns the callback result unchanged; the P0-P7 loop passes that result through consume_owner. No upstream response-field guard selects one attestation name."
            },
            {
              "command": "read src/anomaly/owners.py:22-30,141-151",
              "output": "_REVIEW_FIELDS contains both \"independent_attestation\" and \"attestation\". _review_owner_result rejects only fields outside that set, then converts attestation to independent_attestation; it rejects only the case where both are simultaneously present."
            },
            {
              "command": "grep 'attestation|independent_attestation' agents/anomaly-data-reviewer.md tests/test_pipeline_walk.py",
              "output": "agents/anomaly-data-reviewer.md:19 and :52-53 instruct the live persona to return attestation; tests/test_pipeline_walk.py:42 uses independent_attestation in the focused canonical dynamic-review fixture."
            },
            {
              "command": ".venv/bin/python -c \"from anomaly.owners import _review_owner_result; ... compare canonical and alias inputs ...\"",
              "output": "{\"alias_keys_after_acceptance\": [\"independent_attestation\", \"reviewer_id\", \"verdicts\"], \"canonical_keys\": [\"independent_attestation\", \"reviewer_id\", \"verdicts\"], \"equal\": true}"
            },
            {
              "command": "read .jeff/tasks/0027-resolver-consolidation/task.md",
              "output": "Acceptance criterion 8 requires: \"Unused aliases ... are removed.\""
            }
          ]
        }
      },
      {
        "file": "tests/test_pipeline_walk.py",
        "line": 196,
        "severity": "low",
        "class": "follow-up",
        "kickTo": "plan",
        "what": "The P7 completion matrix still does not prove that changing a chart descendant invalidates completion.",
        "why": "P7_OUTPUT_MUTATIONS removes the whole charts directory but never changes, adds, removes, or changes the kind of a descendant. An existence-only implementation would satisfy the current row, although identities._completion_identity correctly hashes descendant path, kind, and bytes. Add one deterministic completed-case row that mutates a chart file and asserts P7 invalidation plus README demotion; this is consumer-observable regression coverage, not a production defect."
      },
      {
        "file": "src/anomaly/workflow.py",
        "line": 184,
        "severity": "low",
        "class": "follow-up",
        "kickTo": "refactor",
        "what": "run_workflow still resolves the same snapshot twice on every loop iteration.",
        "why": "The first resolve_workflow call is needed only to establish entry_phase, but after entry_phase is set its result is discarded and the same snapshot is resolved again at line 198. Restrict the preliminary resolution to the first iteration, preserving fresh gate filtering while deleting avoidable computation and supporting the lean-consolidation objective."
      },
      {
        "file": "tests/test_case.py",
        "line": 277,
        "severity": "low",
        "class": "follow-up",
        "kickTo": "plan",
        "what": "A retained test name still says events participate in resume authority.",
        "why": "test_resume_reads_progress_from_state_and_events_not_reports neither reads nor asserts events; the production and installed contracts correctly make state.completed authoritative and events observational. Rename it to state-not-report wording so the suite does not preserve stale authority terminology."
      }
    ],
    "evidence": [
      {
        "command": "read task.md, notes.md, context.md, council-final.json, plan-council-recovery.json, implement-council-recovery.json, and task.json gate",
        "output": "Reviewed all nine locked ACs, the council survivor union, the selected causal-subgraph recovery, current dispositions, and the exact gate. HEAD is 49aeca91b98d03771c904cdad1a4e1bf843e7ff5; the gate records clean=true and green=true for uv run --extra test pytest tests/. Supplied authoritative evidence reports 774/774 passed."
      },
      {
        "command": "git rev-parse HEAD; git status --short; git diff --stat/--name-status/--numstat 98542737b170..49aeca91b98d",
        "output": "HEAD matches the requested gate. Current post-gate dirt is limited to Jeff journal/task recording and the supplied security-council report; reviewed product and tests match the gated commit. The full task deletes product_workflow.py and 706 lines from workflow.py; product src changes total +1649/-1013 and tests +1118/-212."
      },
      {
        "command": "read src/anomaly/workflow.py:1-247; owners.py:1-165; attempts.py:1-287; state.py:1-205; workflow_inputs.py:1-186",
        "output": "resolve_workflow is pure; one _OWNER_REGISTRY owns P0-P7 phase selection and fixed write tuples; dynamic P5/P6 loading is bounded to installed instruction paths; run_attempts is a separate three-attempt utility; completed state plus identities—not events or report presence—is resume authority. No WorkflowRunner, PhaseResult, RetryLimitExceeded, product_workflow.py, or duplicate phase-owner registry remains."
      },
      {
        "command": "read src/anomaly/_attempt_workspace.py:25-311 and tests/test_pipeline_walk.py:625-888",
        "output": "Rollback progress is journaled before backup consumption and the hard-exit/producer-prefix/double-interruption tests are now behaviorally ordered. However, original=true recovery still treats backup existence as deletion/replacement authority, whereas original=false applied recovery requires live absence. Tests exercise false non-original claims and trusted existing-original fixtures only, leaving the asymmetric forgery and genuine-new-output cases uncovered."
      },
      {
        "command": "read case.py:102-383; attempts.py:29-128; events.py:25-78; workflow_inputs.py:116-186; relevant case/events/pipeline tests",
        "output": "Whitespace-only Gate A/B IDs pause without attempts. create, inspect, resume, fork destination, direct run_attempts, standalone log_event, dispatcher entry, and post-reasoning continuation all scan unresolved case trees before protected writes; focused tests assert no external mutation for destination and in-case symlink attacks."
      },
      {
        "command": "grep runner/alias/owner registry contracts across src, skills, agents, tests, and PRD; glob workflow files and demo CSVs",
        "output": "Only workflow.py and workflow_inputs.py remain; product_workflow.py and runner/result/retry surfaces are absent; exactly one orchestration_demo.csv exists. One owner registry remains. The search also exposed the still-live attestation→independent_attestation alias in owners.py, with opposing canonical names in the reviewer instructions and demo fake."
      },
      {
        "command": "read tests/test_workflow.py, pipeline gate/order/invalidation/recovery ranges, test_review.py inert-markdown case, test_case.py containment cases, and test_skill.py conformance cases",
        "output": "AC1/AC2/AC4/AC6/AC7/AC9 tests are consumer-observable and deterministic: resolver immutability/resume detail, dynamic owner loading, fresh human turns, exact P5→P6→Gate B→P7 ordering, event-failure resume, earliest invalidation/source replacement/identity separation, inert links/HTML/bare URLs, relative links, full containment, and the one checked-in demo are genuinely exercised. No new runner symbol-absence change-detector test was added."
      },
      {
        "command": "read .jeff/tasks/0027-resolver-consolidation/security-council/security-audit-20260824-141744.md and inspect each hit's validation site",
        "output": "The supplied scan covers 25 files, reports six high regex hits, two scope gaps, and REVIEW. ast.literal_eval is restricted to quoted scalar parsing; interpolated PRAGMA values are validated positive integers; interpolated table identifiers are generated or regex-bound tbl_<64 hex>. Inert Markdown coverage manually closes the relevant XSS serialization boundary. The promotion-journal overwrite finding above is a separate manual trust-boundary defect."
      }
    ],
    "acLedger": [
      {
        "ac": "AC1",
        "claimed": "revise",
        "rederived": "revise",
        "ok": true
      },
      {
        "ac": "AC2",
        "claimed": "reuse",
        "rederived": "reuse",
        "ok": true
      },
      {
        "ac": "AC3",
        "claimed": "revise",
        "rederived": "write",
        "ok": false
      },
      {
        "ac": "AC4",
        "claimed": "revise",
        "rederived": "revise",
        "ok": true
      },
      {
        "ac": "AC5",
        "claimed": "revise",
        "rederived": "write",
        "ok": false
      },
      {
        "ac": "AC6",
        "claimed": "reuse",
        "rederived": "reuse",
        "ok": true
      },
      {
        "ac": "AC7",
        "claimed": "revise",
        "rederived": "revise",
        "ok": true
      },
      {
        "ac": "AC8",
        "claimed": "reuse",
        "rederived": "delete",
        "ok": false
      },
      {
        "ac": "AC9",
        "claimed": "reuse",
        "rederived": "reuse",
        "ok": true
      }
    ]
  },
  "refutes": [
    {
      "agent_id": "AnRefute01",
      "source": "review",
      "finding": "src/anomaly/workflow.py:109 Invocation-wide gate decisions can bypass both required human turn boundaries.",
      "verdict": "survives",
      "rationale": "No upstream stage-aware guard exists: the public entry accepts Gate A and Gate B together, retains both decisions for the whole invocation, checks only key presence at P4/P7, and continues after successful P3/P6 seals, so both required paused returns are reachable to bypass.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:102-138",
          "output": "run_workflow calls normalize_inputs once into supplied before entering while True; every iteration builds available from that same supplied mapping, pauses only when resolution.status == 'paused', otherwise executes the owner, and continues unless the phase becomes blocked or unavailable."
        },
        {
          "command": "read src/anomaly/workflow.py:28-68 and src/anomaly/workflow_inputs.py:11-29",
          "output": "_REQUIRED_INPUTS maps P4 to gate_a and P7 to gate_b; _missing_input returns no pause whenever the name is present. normalize_inputs accepts both names in _PUBLIC_INPUTS, copies the whole caller mapping, and has no phase-aware rejection or deferral guard."
        },
        {
          "command": "read src/anomaly/owners.py:52-70,124-134 and src/anomaly/attempts.py:52-73",
          "output": "The P4 handler reads inputs['gate_a'] and immediately approves/detects; P7 reads inputs['gate_b'] and immediately accepts/reports. A successful non-P7 phase is sealed with status 'active', so after P3 and P6 run_workflow loops directly to the next ready owner instead of returning a human-turn pause."
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.md and task.json gate evidence",
          "output": "The locked acceptance criterion requires Gate A/Gate B turn boundaries, and task state records the inspected candidate as clean and green at gated hash 4a1746f9050120df52e7845166f0ab935c4e584a."
        }
      ]
    },
    {
      "agent_id": "AnRefute02",
      "source": "review",
      "finding": "src/anomaly/workflow.py:35 The resolver treats presence-only, incomplete phase inputs as ready and can spend attempts before boundary validation.",
      "verdict": "survives",
      "rationale": "The public dispatcher passes only top-level input names to the resolver, so missing `now` and incomplete Gate A/B mappings resolve ready; the selected owners validate or index those values only inside `run_attempts`, whose broad exception path durably increments and retries through all three attempts rather than pausing without attempt consumption.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/workflow.py:32-138",
          "output": "`_REQUIRED_INPUTS = {\"P1\": \"sources\", \"P4\": \"gate_a\", \"P7\": \"gate_b\"}`; `_missing_input` checks only membership in `supplied`; `run_workflow` derives `available` from top-level names, resolves, and sends every ready result directly to `run_attempts`."
        },
        {
          "command": "read anomaly/src/anomaly/workflow_inputs.py:13-71",
          "output": "`normalize_inputs` validates only mapping/key shape and unknown top-level names; `input_time` later raises unless `now` is a timezone-aware datetime; `mapping_input` accepts any subset of allowed Gate A/B fields and does not require the fields consumed by owners."
        },
        {
          "command": "read anomaly/src/anomaly/owners.py:49-92,135-143",
          "output": "P2 and P3 call `input_time(inputs)`; P4 reads `gate[\"approved_ids\"]` and `gate[\"approved_by\"]` after subset-only mapping validation, then calls `input_time`; P7 passes a subset-only Gate B mapping to `_accept_and_report`, which reads required fields. These checks occur in owner execution, after resolution."
        },
        {
          "command": "read anomaly/src/anomaly/attempts.py:21-67",
          "output": "`run_attempts` increments and persists the phase attempt before calling `execute`; `except Exception` records the failure and retries until `MAX_ATTEMPTS`, proving these ordinary missing/incomplete inputs consume attempts and can end unavailable."
        },
        {
          "command": "read anomaly/.jeff/tasks/0027-resolver-consolidation/task.md; read anomaly/.jeff/tasks/0027-resolver-consolidation/notes.md",
          "output": "The accepted contract requires the pure resolver to return missing input/human decision, and the plan states Gate A/Gate B pauses end the turn without consuming an attempt; the observed execution path contradicts that contract."
        }
      ]
    },
    {
      "agent_id": "AnRefute03",
      "source": "review",
      "finding": "src/anomaly/owners.py:59 The attempt utility does not provide atomic phase staging; deterministic owners write directly into the live case.",
      "verdict": "survives",
      "rationale": "The failure is reachable through the public P1 source-batch path: only request shape and duplicate IDs are prevalidated, each valid request is then persisted directly into the live case before the next request is validated, and run_attempts records failures without rolling those mutations back; the gated-hash probe confirms a later invalid source leaves the earlier source live after P1 becomes unavailable.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:101-139 and src/anomaly/attempts.py:21-105",
          "output": "run_workflow passes the live case_root to consume_owner inside run_attempts; run_attempts creates attempt_dir but success/failure handling only writes attempt result/failure evidence and state, with no snapshot, rollback, or cleanup of owner mutations."
        },
        {
          "command": "read src/anomaly/owners.py:53-103",
          "output": "_run_handler names the staging argument _attempt_dir and does not use it. For P1, _register_sources loops over requests and calls register_local_source(root, ...) against the live root for each item."
        },
        {
          "command": "read src/anomaly/workflow_inputs.py:42-57 and src/anomaly/acquire.py:25-126",
          "output": "source_requests validates only list/mapping shape and required/unknown fields. Per-item path existence, extension, portability, and metadata validation happen later inside register_local_source, after earlier loop items may already have written raw bytes, receipts, and data/sources.json."
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/review-correctness.json:115-145",
          "output": "The supplied gated-hash public probe with a valid CSV followed by an unsupported .txt ended {\"attempts\":{\"P0\":1,\"P1\":3},\"phase\":\"P1\",\"status\":\"unavailable\",\"sources\":[{\"source_id\":\"good\",...}],\"raw\":[\"data/raw/good/good.csv\"]}, directly demonstrating reachability and durable partial promotion."
        }
      ]
    },
    {
      "agent_id": "AnRefute04",
      "source": "review",
      "finding": "src/anomaly/readme.py:36 README projection deletes journalist-authored content that exactly matches the legacy Outputs block.",
      "verdict": "survives",
      "rationale": "A journalist-edited regular README can contain this exact unmarked section, and public completion, completed-case resume, or invalidation reaches the unconditional global replacement, silently deleting user-authored content in direct conflict with the preservation acceptance criterion.",
      "evidence": [
        {
          "command": "read src/anomaly/readme.py:24-48",
          "output": "project_readme accepts any existing non-symlink README, removes the marked Anomaly block, then unconditionally executes .replace(_OUTPUTS, \"\"); Python str.replace removes every exact occurrence, with no ownership or position guard."
        },
        {
          "command": "read src/anomaly/workflow.py:101-139; src/anomaly/attempts.py:21-88; src/anomaly/state.py:34-52,133-171",
          "output": "The public run_workflow entry repeatedly calls load_snapshot; completed P7 snapshots call project_readme, successful P7 sealing calls it, and invalidation from a previously completed P7 calls it again. The destructive branch is therefore reachable during ordinary completion, restart, and invalidation flows."
        },
        {
          "command": "read src/anomaly/case.py:196-224,237-251",
          "output": "resume_case/_load_case validates case.json and progress but imposes no README-content restriction; the initial README is ordinary journalist-facing Markdown. No upstream guard makes an exact unmarked Outputs section impossible."
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.md; grep gated hash and finding evidence in task state/review return",
          "output": "The locked acceptance criterion explicitly requires README projection to preserve journalist-authored content; gate state binds the inspected candidate to 4a1746f9050120df52e7845166f0ab935c4e584a, and the supplied pure replacement probe reports journalist_outputs_preserved=False with only the later Notes section remaining."
        }
      ]
    },
    {
      "agent_id": "AnRefute07",
      "source": "review",
      "finding": "src/anomaly/owners.py:122 Canonical-equivalent Unicode identities bypass reviewer independence at both gates.",
      "verdict": "survives",
      "rationale": "The public workflow accepts nonempty identity strings without Unicode normalization, both role-separation checks compare only casefolded text, and the project’s canonical identity function additionally normalizes to NFC, so canonically equivalent composed and decomposed identities can reach and pass both gates.",
      "evidence": [
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:34-38,101-109,266-287",
          "output": "The task binds the reviewed candidate to gated hash 4a1746f9050120df52e7845166f0ab935c4e584a and records that reviewed product source matches that gate; the supplied review finding is at src/anomaly/owners.py:122."
        },
        {
          "command": "read src/anomaly/owners.py:116-150",
          "output": "Gate A/reviewer separation is `reviewer_id.casefold() == gate_a_approver.casefold()` and reviewer/Gate B separation is `journalist_id.casefold() == reviewer_id.casefold()`; `_required_identity` only requires a nonempty string and returns `value.strip()`."
        },
        {
          "command": "read src/anomaly/semantics.py:63-79",
          "output": "`canonical_key` returns `unicodedata.normalize(\"NFC\", unicodedata.normalize(\"NFC\", value).casefold())`, establishing NFC normalization as part of the project’s canonical identity policy."
        },
        {
          "command": "read src/anomaly/workflow.py:101-139; src/anomaly/workflow_inputs.py:10-65; src/anomaly/recommend.py:335-379; src/anomaly/review.py:249-335",
          "output": "`run_workflow` forwards caller gate inputs and dynamic reviewer output into owners; gate mappings restrict field names but not Unicode form, Gate A accepts and persists any nonempty `approved_by`, and review recording only strips `reviewer_id` while its attestation check compares against that same unnormalized value. No upstream NFC guard makes the bypass unreachable."
        }
      ]
    },
    {
      "agent_id": "AnRefute08",
      "source": "review",
      "finding": "src/anomaly/state.py:39 A completed snapshot with missing authoritative identities is trusted as P7-complete.",
      "verdict": "survives",
      "rationale": "Normal sealing captures identities, but the public portable-case entry accepts any regular state.json; load_snapshot converts a missing or non-mapping identities value to {}, changed_identities cannot notice omitted required keys, and resolve_workflow then treats a sequential completed map through P7 as terminal and even projects README completion without validating the authoritative artifacts.",
      "evidence": [
        {
          "command": "read src/anomaly/attempts.py:70-90; src/anomaly/state.py:34-49; src/anomaly/identities.py:22-31",
          "output": "_seal_success normally calls capture_identities before write_state, but load_snapshot passes {} when identities is absent or malformed, and changed_identities compares only recorded.items(); it has no required-key check, so omitted identities produce no invalidation."
        },
        {
          "command": "read src/anomaly/workflow.py:37-49,106-128; src/anomaly/case.py:146-150,255-301",
          "output": "run_workflow is a real public entry: its case-tree guard rejects links/special files but accepts regular files, resume_case validates the case record rather than state provenance, then load_snapshot feeds resolve_workflow; resolve_workflow returns complete solely when completed_phase is P7."
        },
        {
          "command": "read src/anomaly/state.py:22-49; src/anomaly/readme.py:24-58",
          "output": "completed_phase accepts any sequential P0-P7 key mapping; load_snapshot calls project_readme when it sees P7, and project_readme marks Status complete and adds accepted-findings/report links without checking that those artifacts or their identities exist."
        }
      ]
    },
    {
      "agent_id": "AnRefute10",
      "source": "review",
      "finding": "tests/test_pipeline_walk.py:349 Deleting the invalidation matrix left most authoritative identity classes without earliest-suffix coverage.",
      "verdict": "survives",
      "rationale": "Production directly trusts the unguarded identity-to-phase table to choose and destructively clear the invalidation suffix, while the candidate deleted the only P3–P6 behavioral matrix and retained public tests exercise only source/P1, prepared/P2, and Gate B/P7, so no upstream guard or equivalent coverage makes the blocker unreachable or less severe.",
      "evidence": [
        {
          "command": "git rev-parse HEAD",
          "output": "4a1746f9050120df52e7845166f0ab935c4e584a"
        },
        {
          "command": "read src/anomaly/identities.py:9-30",
          "output": "IDENTITY_PHASES maps recommendation→P3, gate_a/detector→P4, draft→P5, replay/review→P6; changed_identities compares every recorded digest and returns the minimum mapped phase without any independent validation or fallback."
        },
        {
          "command": "read src/anomaly/state.py:34-45,124-162",
          "output": "load_snapshot passes changed_identities' start directly to _invalidate_state; _invalidate_state removes every completed phase from that start onward, clears attempts/failures and identities for the same suffix, and persists the result."
        },
        {
          "command": "git diff bd3d49e 61a2b7c -- tests/test_pipeline_walk.py",
          "output": "The task-27 cutover deletes MUTATIONS entries for source/P1, prepared/P2, detector/P4, parameters/P3, draft/P5, replay/P6, review/P6, Gate A/P4, and Gate B/P7, and deletes test_fresh_session_invalidates_changed_authoritative_inputs_from_earliest_phase, which asserted invalidated_from, retained prefix, cleared suffix, and suffix rerun."
        },
        {
          "command": "grep 'invalidat|rebuilds_stale|replaces_changed' tests",
          "output": "Current workflow-dispatch coverage is tests/test_pipeline_walk.py:349 prepared mutation, :371 source replacement, and :438 Gate-B mutation; tests/test_m5_handoff.py detector-change cases exercise replay/acceptance rejection rather than load_snapshot's earliest workflow suffix, and no tests reference changed_identities, capture_identities, artifact_identity, or IDENTITY_PHASES."
        }
      ]
    },
    {
      "agent_id": "AnRefute11",
      "source": "review",
      "finding": "tests/test_pipeline_walk.py:185 No public test proves that future Gate A or Gate B decisions cannot cross a turn.",
      "verdict": "survives",
      "rationale": "At the gated hash, run_workflow normalizes inputs once and reuses the same supplied mapping on every resolver iteration, with no guard that rejects or consumes a future gate decision, while the cited demo supplies Gate A and Gate B only in separate later invocations; the consumer-observable AC4 turn boundary is therefore reachable and unproved.",
      "evidence": [
        {
          "command": "git rev-parse HEAD",
          "output": "4a1746f9050120df52e7845166f0ab935c4e584a"
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py",
          "output": "109: supplied = normalize_inputs(inputs)\n119: while True:\n120:     snapshot = load_snapshot(case_root)\n122:     available = frozenset({*supplied, *({\"sources\"} if has_sources else set())})\n123:     resolution = resolve_workflow(snapshot, supplied=available)\n126:     if resolution[\"status\"] == \"paused\":\n127:         return pause_at(case_root, snapshot, resolution[\"phase\"], resolution[\"missing\"])\n131-137: consume_owner receives the unchanged resolution and supplied mapping; the loop then resolves again without clearing gate keys."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py",
          "output": "101: the first invocation supplies only source inputs and asserts pause at gate_a; 118-130: the next invocation supplies gate_a and asserts pause at gate_b; 142-151: a third invocation supplies gate_b and asserts completion. No invocation pre-supplies a future gate decision."
        },
        {
          "command": "grep pattern 'gate_a|gate_b' path tests",
          "output": "Public-dispatcher gate inputs occur in tests/test_pipeline_walk.py at gate_a lines 122, 334, 402 and gate_b lines 145, 425, 470, 496; the canonical demo path at lines 92-151 supplies each gate only after the prior invocation has paused."
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.md",
          "output": "Acceptance criterion 4: \"Gate A/Gate B turn boundaries, identity separation, source replacement, earliest invalidation, event-failure resume, case portability and exact P5→P6→Gate B→P7 order remain green.\""
        }
      ]
    },
    {
      "agent_id": "AnRefute13",
      "source": "review",
      "finding": "tests/test_review.py:425 The Markdown inertness test does not contain a standalone bare URL.",
      "verdict": "survives",
      "rationale": "AC6 explicitly requires bare HTTP(S) autolink coverage, but the only tested URLs are structurally embedded and arbitrary dataset statements can reach report serialization, so no upstream guard makes a standalone URL unreachable; the current generic escaping regex is fail-safe today but does not replace the missing behavioral regression proof.",
      "evidence": [
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.md; grep 4a1746f9050120df52e7845166f0ab935c4e584a .jeff/tasks/0027-resolver-consolidation/task.json",
          "output": "The locked acceptance criterion says \"Dataset-derived Markdown is inert, including bare URL autolinks.\" The task gate records clean=true and green=true at 4a1746f9050120df52e7845166f0ab935c4e584a."
        },
        {
          "command": "grep -n 'https?://' tests/test_review.py",
          "output": "The only input occurrences are line 432, \"Acme [click](https://example.invalid/pixel)\", and line 433, \"<img src=https://example.invalid/pixel>\"; lines 450-453 only assert those constructs and the shared raw substring are absent. There is no standalone prose URL anywhere in the file."
        },
        {
          "command": "read src/anomaly/review.py:170-235,360-465,609-703",
          "output": "draft_findings copies signal['statement'] without a URL-shape restriction, accept_findings promotes that statement, and write_report passes it to _markdown_text. Thus a standalone dataset URL is reachable from the same public path exercised by the test."
        },
        {
          "command": "read src/anomaly/review.py:647-656",
          "output": "_markdown_text currently neutralizes every word-boundary HTTP(S) scheme with re.sub(r'(?i)\\bhttps?://', ...) after generic Markdown escaping. This makes the present implementation fail safe, but it is not an upstream impossibility guard and the existing embedded-only fixture would also pass an implementation that removed link/image constructs while leaving standalone autolinks active."
        }
      ]
    },
    {
      "agent_id": "AnRefute14",
      "source": "review2",
      "finding": "src/anomaly/owners.py:59 Phase attempts do not stage or atomically promote owner outputs.",
      "verdict": "survives",
      "rationale": "The public dispatcher reaches this path with normal phase inputs, but deterministic and dynamic owners write against the live case root while the attempt utility records only a post-hoc result and has no validation, promotion, or rollback of owner artifacts, so a later owner failure can leave partially committed case state and the high-severity AC3 violation is reachable.",
      "evidence": [
        {
          "command": "read skills/anomaly/SKILL.md:49-98",
          "output": "The installed operating contract requires every phase's outputs to be written under .anomaly/attempts/<phase>/attempt-<n>/ first, validated for hashes, schemas, safety, and relative paths, and only then moved into case folders; superseded attempts must remain auditable."
        },
        {
          "command": "read src/anomaly/workflow.py:101-145 and src/anomaly/owners.py:30-75",
          "output": "run_workflow passes both case_root and attempt_dir into consume_owner. Handler dispatch then calls _run_handler, whose attempt parameter is named _attempt_dir and unused while every deterministic operation receives root. Dynamic dispatch invokes the reasoning owner with only case_root, and P5 rejects the result unless live root/findings/draft.json already exists."
        },
        {
          "command": "read src/anomaly/attempts.py:18-114",
          "output": "run_attempts calls execute(attempt_dir) before writing only result.json into the attempt directory. On exception it records failure.json and state evidence but performs no owner-output rollback; on success _seal_success only updates identities/state and performs no artifact validation or atomic promotion."
        },
        {
          "command": "read src/anomaly/owners.py:67-76,135-144 and src/anomaly/detect.py:583-744",
          "output": "Reachable deterministic phases perform compound live mutations: P4 seals Gate A and then executes detectors, while detector execution creates live run directories, snapshots, provenance, and appends signals inside a per-detector loop before later operations can raise; P7 likewise accepts findings, writes the report, and only then generates charts. No phase-level transaction protects these sequences."
        }
      ]
    },
    {
      "agent_id": "AnRefute15",
      "source": "review2",
      "finding": "src/anomaly/workflow.py:29 The resolver's missing-input boundary is schema-blind and omits required timestamps.",
      "verdict": "survives",
      "rationale": "No upstream guard makes these inputs unreachable: the public dispatcher reduces inputs to their top-level names, the resolver therefore marks missing `now` or structurally incomplete gate mappings ready, and owner-side validation failures are caught only after attempt accounting and retried until the phase becomes unavailable.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:29-62,101-138 and src/anomaly/workflow_inputs.py:15-64",
          "output": "`_REQUIRED_INPUTS` contains only P1 `sources`, P4 `gate_a`, and P7 `gate_b`; `run_workflow` passes the keys of normalized inputs to `resolve_workflow`, while `normalize_inputs` validates only top-level names and does not validate values or require `now`, so these malformed or incomplete public inputs reach a ready resolution."
        },
        {
          "command": "read src/anomaly/owners.py:37-101,136-144",
          "output": "P1 with supplied sources and P2-P4 call `input_time(inputs)`, which raises when `now` is absent; P4 indexes `gate[\"approved_ids\"]` and `gate[\"approved_by\"]`, and P7 indexes `gate[\"accepted_claim_ids\"]`, so present-but-incomplete gate mappings fail only inside owner execution."
        },
        {
          "command": "read src/anomaly/attempts.py:21-71 and src/anomaly/state.py:1-14",
          "output": "`run_attempts` increments and persists the attempt before executing the owner, catches every owner exception, retries while the attempt is below `MAX_ATTEMPTS`, and marks the phase unavailable at the limit; `MAX_ATTEMPTS` is 3."
        }
      ]
    },
    {
      "agent_id": "AnRefute17",
      "source": "review2",
      "finding": "tests/test_pipeline_walk.py:348 The authoritative-identity invalidation matrix was deleted without replacement coverage for P3-P6.",
      "verdict": "survives",
      "rationale": "The public dispatcher reaches identity comparison for ordinary modified case artifacts, production maps six retained P3–P6 identities to earliest-suffix invalidation, and no current test exercises that state/resolution contract for those phases, so neither reachability nor the medium blocking severity is disproved.",
      "evidence": [
        {
          "command": "read src/anomaly/identities.py:6-18; read src/anomaly/state.py:34-52,116-171; read src/anomaly/workflow.py:101-129",
          "output": "IDENTITY_PHASES maps recommendation→P3, gate_a/detector→P4, draft→P5, and replay/review→P6; load_snapshot calls changed_identities and _invalidate_state removes the suffix from the earliest mapped phase; run_workflow calls load_snapshot on every public resolver iteration."
        },
        {
          "command": "grep 'changed_identities|capture_identities|artifact_identity|IDENTITY_PHASES|invalidated_from' anomaly/tests; grep 'invalidat|_mark\\(' anomaly/tests/test_pipeline_walk.py",
          "output": "No test imports or invokes the identity comparison/capture API; test_workflow.py only places invalidated_from=P3 in a resolver input fixture, while test_pipeline_walk.py mutates prepared/transforms.json (P2) and gate-b.json (P7), with no recommendation, Gate A, detector, draft, replay, or review mutation matrix."
        },
        {
          "command": "read src/anomaly/case.py:255-303; read tests/test_m5_handoff.py:283-354",
          "output": "The upstream case scan rejects symlinks, special files, and executable artifacts but permits changed regular case files, so the identity-change path is reachable; the detector tests in test_m5_handoff exercise replay_signals/accept_findings domain checks rather than run_workflow/load_snapshot earliest-suffix invalidation and do not replace P3–P6 matrix coverage."
        }
      ]
    },
    {
      "agent_id": "AnRefute18",
      "source": "review2",
      "finding": "tests/test_pipeline_walk.py:438 README preservation tests omit completed restart and failed recomputation with journalist content.",
      "verdict": "survives",
      "rationale": "Both omitted transitions are reachable through run_workflow and invoke README projection, while the completed-restart test adds no journalist content and the P7 chart-failure test contains no preservation assertion, so no upstream guard makes the AC5 coverage gap unreachable or less severe.",
      "evidence": [
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py",
          "output": "Lines 218-219 restart the completed case immediately (`resumed = run_workflow(root)`) before any journalist notes are added; lines 445-452 add `journalist_notes` only inside the later invalidation test; lines 481-508 exercise failed P7 chart recomputation but neither add journalist content nor assert its preservation."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py && git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py && git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py",
          "output": "workflow.py:119-125 calls `load_snapshot(case_root)` before returning a complete case; state.py:49-52 calls `project_readme` whenever that snapshot is complete through P7; attempts.py:48-61 records P7 failures and calls `project_readme(root, state, completed_phase(state))` after the final failed attempt. Therefore both omitted lifecycle transitions reach README rewriting."
        }
      ]
    },
    {
      "agent_id": "AnRefute19",
      "source": "review2",
      "finding": "tests/test_review.py:425 The inert-Markdown test does not exercise a standalone bare URL.",
      "verdict": "survives",
      "rationale": "The acceptance criterion explicitly requires inert bare HTTP(S) autolinks, but the fixture supplies URLs only as Markdown-link and HTML-image operands, so it can pass if those enclosing constructs are stripped while a prose-only URL still autolinks.",
      "evidence": [
        {
          "command": "read tests/test_review.py:425-454",
          "output": "The statement fixture is `Acme [click](https://example.invalid/pixel)`, `<img src=https://example.invalid/pixel>`, and a forged heading; there is no standalone prose URL, and the assertions only reject the two enclosing constructs, the heading, and the raw URL substring."
        },
        {
          "command": "read task.md:13-15; read src/anomaly/review.py:655-665",
          "output": "AC6 says dataset-derived Markdown must be inert `including bare URL autolinks`; `_markdown_text` has a dedicated context-independent HTTP(S) colon-neutralization branch, but the test never drives that branch with a prose-only bare-URL input or asserts that its visible text survives."
        }
      ]
    },
    {
      "agent_id": "AnRefute20",
      "source": "audit",
      "finding": "src/anomaly/acquire.py:46 Direct source registration can follow an existing in-case symlink and overwrite another case artifact.",
      "verdict": "survives",
      "rationale": "The finding survives because this mainline registration entry calls only unscanned case/document validation, resolves an in-case destination symlink to its artifact target, and writes payload bytes to that resolved target; neither its decorator nor an upstream guard makes the overwrite unreachable or fail-safe.",
      "evidence": [
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/acquire.py (lines 31-47, 71-75, 123-131, 158-163)",
          "output": "register_local_source calls resume_case(root) and validate_case_documents(root), builds relative = data/raw/<source_id>/<source-name>, and sets dest = _under_root(root, relative). _under_root computes candidate = (base / relative).resolve(), rejects only candidates outside base, and returns an in-case resolved candidate. For a new included source, lines 128-129 execute dest.parent.mkdir(...) and dest.write_bytes(payload), so a destination symlink resolving to case.json causes the CSV payload to be written to case.json."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/case.py src/anomaly/semantics.py src/anomaly/events.py src/anomaly/workflow.py (targeted guard inspection)",
          "output": "case.py:146-150 resume_case only checks _case_exists and calls _load_case; it does not call _scan_case_tree. semantics.py:180-225 validate_case_documents reads data/sources.json and scans only .anomaly/receipts. events.py:51-67 phase_event merely calls the wrapped function and logs success/failure. workflow.py:102-113 does scan in run_workflow, but that guard belongs only to that separate wrapper and does not precede a direct register_local_source call."
        }
      ]
    },
    {
      "agent_id": "AnRefute22",
      "source": "audit",
      "finding": "src/anomaly/state.py:38 A completed snapshot with missing artifact identities is trusted as complete.",
      "verdict": "survives",
      "rationale": "The public workflow accepts the supplied state because missing identities become an empty mapping, the identity checker examines only recorded keys, and sequential P0-P7 completion causes an immediate terminal return without validating required artifact identities.",
      "evidence": [
        {
          "command": "read src/anomaly/state.py:18-48",
          "output": "completed_phase advances by key presence through PHASES; load_snapshot passes {} to changed_identities when identities is missing or not a Mapping, then returns the state unchanged when no change is reported."
        },
        {
          "command": "read src/anomaly/identities.py:22-31",
          "output": "changed_identities builds changed only from `for name, digest in recorded.items()` and returns None when that list is empty; it does not require any IDENTITY_PHASES key."
        },
        {
          "command": "read src/anomaly/workflow.py:37-47,101-126",
          "output": "resolve_workflow returns status `complete` as soon as completed_phase(snapshot) is P7; run_workflow calls load_snapshot, then returns that snapshot immediately for a complete resolution before run_attempts or any owner executes."
        },
        {
          "command": "read src/anomaly/readme.py:24-62",
          "output": "The P7 load-time README projection checks only completed/status fields and writes static output links; it does not verify that the linked artifacts exist or have identities."
        }
      ]
    },
    {
      "agent_id": "AnRefute23",
      "source": "audit",
      "finding": "src/anomaly/owners.py:122 Canonical-equivalent Unicode identities bypass Gate A, reviewer, and Gate B role separation.",
      "verdict": "survives",
      "rationale": "The public dispatcher accepts unnormalized role identities, both separation checks compare only whitespace-stripped casefold values, and no downstream attestation or persistence guard canonicalizes across roles, so composed and decomposed spellings let one logical identity pass both independent-role checks.",
      "evidence": [
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/owners.py (lines 111-165)",
          "output": "_replay_and_review obtains reviewer_id through _required_identity, reads Gate A approved_by through the same helper, and rejects equality only with reviewer_id.casefold() == gate_a_approver.casefold() at line 122; _accept_and_report performs the analogous casefold-only reviewer/Gate B comparison at line 134; _required_identity at lines 162-165 only validates non-empty string and returns value.strip()."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 102-137) and src/anomaly/workflow_inputs.py (lines 25-36, 69-76)",
          "output": "run_workflow is the reachable public entry, copies inputs with normalize_inputs, and passes the resulting supplied mapping to consume_owner; normalize_inputs validates only top-level input names, while mapping_input copies gate mappings and rejects only unknown fields. Neither performs Unicode normalization or identity canonicalization."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/recommend.py (lines 364-377) and src/anomaly/review.py (lines 295-330, 399-407)",
          "output": "Gate A validates approved_by only as a non-empty string and persists it verbatim. record_review only strips reviewer_id; strict attestation checks attested_by == reviewer but does not compare a canonical identity against Gate A. Gate B validates non-empty identity and optional exact configured equality, with no canonical cross-role guard."
        },
        {
          "command": "python3 -c 'compare Café with Cafe\\u0301 using str.casefold and the gated canonical_key algorithm'",
          "output": "{'casefold_equal': False, 'canonical_equal': True, 'a_nfc': True, 'b_nfc': False}"
        }
      ]
    },
    {
      "agent_id": "AnRefute24",
      "source": "audit",
      "finding": "src/anomaly/workflow.py:109 Future Gate A and Gate B decisions are retained across the resolver loop and consumed without a fresh human turn.",
      "verdict": "survives",
      "rationale": "The public dispatcher captures every input once before its execution loop, carries that same mapping through every newly sealed phase, and treats mere key presence as sufficient to cross Gate A or Gate B; downstream artifact and identity validation does not prove that either decision was supplied in a fresh, informed human turn.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:101-139 and src/anomaly/workflow_inputs.py:16-31",
          "output": "run_workflow calls normalize_inputs(inputs) once before while True, then on every iteration builds available from the unchanged supplied mapping and passes supplied into consume_owner; normalize_inputs copies all public inputs, including gate_a and gate_b, without phase or turn binding."
        },
        {
          "command": "read src/anomaly/workflow.py:37-69,160-167 and src/anomaly/owners.py:49-80,125-137",
          "output": "resolve_workflow pauses only when the phase's required input name is absent. If gate_a or gate_b was supplied earlier, P4/P7 resolves ready immediately and consume_owner reads the retained mapping; neither boundary checks when the decision entered the call."
        },
        {
          "command": "read src/anomaly/recommend.py:335-390 and src/anomaly/review.py:371-477",
          "output": "Gate A validates approved IDs against the current deterministic plan and Gate B validates review/replay/identity artifacts, but neither receipt requires a post-P3/post-P6 turn token or decision timestamp. Gate B also permits an empty accepted_claim_ids list, so a future Gate B decision can remain structurally valid without knowing claim IDs in advance."
        }
      ]
    },
    {
      "agent_id": "AnRefute25",
      "source": "audit",
      "finding": "src/anomaly/state.py:15 Attempt failures persist common credentials because the central error sanitizer recognizes only three token prefixes.",
      "verdict": "survives",
      "rationale": "The public workflow can pass an arbitrary dynamic-owner exception directly into durable attempt recording, and the only sanitizer on that path recognizes three provider prefixes, so common credentials remain reachable and are persisted repeatedly without an upstream guard or fail-safe.",
      "evidence": [
        {
          "command": "read src/anomaly/state.py:1-121; read src/anomaly/semantics.py:44-101",
          "output": "safe_error calls _safe_text, which applies redact_credentials and _SECRET; both credential regexes match only sk_live_, ghp_, and github_pat_, with no assignment, Bearer, AWS, Slack, or credential-bearing URL handling."
        },
        {
          "command": "read src/anomaly/workflow.py:75-136; read src/anomaly/attempts.py:21-121",
          "output": "run_workflow is the public entry point, invokes the caller-supplied reasoning callback without constraining exception text, and run_attempts catches any Exception; _record_failure then writes the sanitized message to failure.json, events.jsonl, state.failures, and blocked_reason after the third attempt."
        },
        {
          "command": "read src/anomaly/review.py:20-35,1040-1072; read skills/anomaly/SKILL.md:35-58",
          "output": "review.py already redacts sensitive assignments, Bearer values, Slack xox tokens, and AWS AKIA keys, while the installed workflow contract requires credential-redacted failure persistence and forbids credentials in case JSON or events; no equivalent protection exists on the attempt-error path."
        }
      ]
    },
    {
      "agent_id": "AnRefute26",
      "source": "audit",
      "finding": "src/anomaly/owners.py:92 A failed multi-source P1 attempt partially commits earlier sources instead of preserving atomic attempt semantics.",
      "verdict": "survives",
      "rationale": "The defect is reachable through the public run_workflow sources input because only request shape and duplicate IDs are prevalidated, each valid earlier request commits directly to the live case before a later request can fail source-specific validation, and run_attempts records and retries the failure without restoring those mutations, so the high-severity atomicity violation is honest.",
      "evidence": [
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:1-180",
          "output": "The clean green gate is bound to hash 4a1746f9050120df52e7845166f0ab935c4e584a, and task AC3 requires durable attempt staging, failure evidence, and atomic promotion."
        },
        {
          "command": "read src/anomaly/workflow.py:101-139, src/anomaly/owners.py:49-108, and src/anomaly/workflow_inputs.py:48-63",
          "output": "Public run_workflow routes P1 into run_attempts and consume_owner; _run_handler ignores attempt_dir and _register_sources iterates register_local_source against the live case. source_requests prevalidates only nonempty list/mapping shape and required/unknown fields, leaving later per-source path, format, availability, and inclusion validation reachable after an earlier request has succeeded."
        },
        {
          "command": "read src/anomaly/acquire.py:31-132 and src/anomaly/attempts.py:21-67,87-121",
          "output": "For a new included source, register_local_source writes raw bytes, a receipt, and data/sources.json immediately. A later exception is caught by run_attempts, which writes failure evidence and retries or marks P1 unavailable; no rollback or attempt-directory promotion restores the earlier live writes."
        }
      ]
    },
    {
      "agent_id": "AnRefute27",
      "source": "audit",
      "finding": "src/anomaly/readme.py:36 README projection can delete journalist-authored Markdown that happens to equal the legacy Outputs block.",
      "verdict": "survives",
      "rationale": "The public workflow reaches this projection on P7 completion, completed-case resume, and P7 invalidation, and no upstream content guard prevents a journalist-authored block from exactly matching _OUTPUTS, so the global replacement can delete valid authored Markdown and the medium data-loss severity is honest.",
      "evidence": [
        {
          "command": "read src/anomaly/readme.py:24-38",
          "output": "project_readme reads the existing README, removes the bounded anomaly marker block, then calls .replace(_OUTPUTS, \"\") without a count or marker requirement; the only local guards are README existence and non-symlink status."
        },
        {
          "command": "read src/anomaly/workflow.py:101-126; src/anomaly/state.py:34-52,151-174; src/anomaly/attempts.py:50-88",
          "output": "run_workflow calls load_snapshot on a real case; load_snapshot projects completed P7 READMEs, _invalidate_state projects after invalidating a previously completed P7 case, and _seal_success projects after successful P7 completion."
        },
        {
          "command": "grep 'README.md|Outputs|accepted findings' src/anomaly",
          "output": "The only README content mutation/guard in production is readme.py; no validator rejects or distinguishes an unmarked journalist-authored exact _OUTPUTS block."
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.md",
          "output": "Acceptance explicitly requires README projection to preserve journalist-authored content, making deletion of a valid unmarked matching section a contract violation."
        }
      ]
    },
    {
      "agent_id": "AnRefute05",
      "source": "review",
      "finding": "src/anomaly/case.py:146 Public resume, inspect, and direct writer paths do not enforce the required whole-case no-symlink scan.",
      "verdict": "survives",
      "rationale": "The finding is reachable and honestly high severity: direct resume/inspect calls never scan, while register_local_source calls the unguarded resume path, resolves a destination symlink to an allowed in-case target, and then writes through that resolved path, permitting overwrite of another case artifact; run_workflow's upstream scan does not protect direct public calls.",
      "evidence": [
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/case.py (lines 139-165)",
          "output": "139: def inspect_case(root: Path) -> ExistingCaseOffer | None:\n140:     root = Path(root)\n141:     if not _case_exists(root):\n142:         return None\n143:     return _offer(root)\n146: def resume_case(root: Path) -> Case:\n147:     root = Path(root)\n148:     if not _case_exists(root):\n149:         raise CaseNotFoundError(str(root))\n150:     return _load_case(root)\n161:     source = Path(source)\n162:     dest = Path(dest).resolve()\n163:     _scan_case_tree(source)\n165:     parent = resume_case(source)"
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/acquire.py (lines 30-46, 71-75, 125-130, 158-163)",
          "output": "30: @phase_event(\"P1\", \"register_local_source\")\n31: def register_local_source(...):\n45:     root = Path(root).resolve()\n46:     resume_case(root)\n71:     relative = _posix(\"data/raw\", persisted_source_id, source_path.name)\n72:     dest = _under_root(root, relative)\n75:     payload = source_path.read_bytes() if source_path.is_file() else None\n125:     if existing_index is None:\n126:         if included:\n128:             dest.parent.mkdir(parents=True, exist_ok=True)\n129:             dest.write_bytes(payload)\n158: def _under_root(root: Path, relative: str) -> Path:\n159:     base = Path(root).resolve()\n160:     candidate = (base / relative).resolve()\n161:     if candidate != base and base not in candidate.parents:\n162:         raise UnsafeCasePathError(relative)\n163:     return candidate"
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 102-113)",
          "output": "102: def run_workflow(...):\n110:     case_root = Path(root)\n111:     _scan_case_tree(case_root)\n112:     case_root = case_root.resolve()\n113:     resume_case(case_root)"
        },
        {
          "command": "read src/anomaly/events.py:50-68",
          "output": "phase_event wraps the function with try/except, calls the wrapped function directly, then performs best-effort log_event; it contains no case-tree scan or path guard."
        }
      ]
    },
    {
      "agent_id": "AnRefute06",
      "source": "review",
      "finding": "src/anomaly/workflow.py:119 The multi-phase loop does not recheck whole-case containment after dynamic reasoning owners mutate the case.",
      "verdict": "survives",
      "rationale": "The finding is reachable: run_workflow performs its only whole-tree scan before the loop, grants the external reasoning invoker the case root, and after that invoker returns run_attempts writes through the previously created attempt directory with no rescan or symlink-parent guard, so an owner that replaces that directory with a symlink can make the same public call write result.json outside the case.",
      "evidence": [
        {
          "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 101-140)",
          "output": "111: _scan_case_tree(case_root); 119: while True; 128-138: run_attempts(... lambda attempt_dir: consume_owner(...)); there is no _scan_case_tree call in the loop before or after owner execution."
        },
        {
          "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 89-99); git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/owners.py (lines 37-56)",
          "output": "workflow.py:99 passes case_root=Path(case_root) to the external invoke callable; owners.py:49-55 calls that reasoning owner for P5 and accepts its return after only checking that findings/draft.json is_file()."
        },
        {
          "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py (lines 41-48,122-124); git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py (lines 102-105)",
          "output": "attempts.py creates attempt_dir before invocation, then executes the owner and calls _write_output(attempt_dir, output); _write_output calls write_json_atomic(attempt_dir / \"result.json\", payload). state.py implements that helper as temporary.write_text(...) followed by temporary.replace(path), with no containment rescan or symlink-parent rejection."
        }
      ]
    },
    {
      "agent_id": "AnRefute09",
      "source": "review",
      "finding": "tests/test_pipeline_walk.py:263 The retained retry tests no longer verify credential-redacted durable failure evidence.",
      "verdict": "survives",
      "rationale": "The public retry path can persist one sanitized exception message into failure.json, event error fields, state failures, and blocked_reason, but the gated retry test supplies no credential and asserts none of those redaction outcomes, while no focused retry redaction coverage exists in the retained orchestration tests.",
      "evidence": [
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py [lines 263-288]",
          "output": "The test sets source_id to \"../unsafe\", runs run_workflow, and asserts only blocked/unavailable status, three P1 attempts, relative attempt paths, and attempt-directory existence; it does not inject a credential or inspect persisted error text."
        },
        {
          "command": "git grep -n -E 'sk_live_|ghp_|github_pat_|failure\\.json|phase_failed' 4a1746f9050120df52e7845166f0ab935c4e584a -- tests/test_pipeline_walk.py tests/test_workflow.py",
          "output": "Exit 1 with no matches."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py [lines 91-108]",
          "output": "_record_failure computes message = safe_error(error), writes it to attempt failure.json, appends it as the phase_failed event error, stores it in state failures, and on the third attempt assigns the same message to blocked_reason."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py [lines 120-125]",
          "output": "safe_error returns _safe_text(value)[:2000], and _safe_text applies credential redaction; this shows the intended fail-safe exists in production but does not supply the missing public durable-evidence regression assertion."
        }
      ]
    },
    {
      "agent_id": "AnRefute12",
      "source": "review",
      "finding": "tests/test_pipeline_walk.py:438 README preservation coverage omits completed restart and failed recomputation transitions.",
      "verdict": "survives",
      "rationale": "Both transitions are reachable production branches that invoke README projection, but the gated tests do not exercise either branch with journalist-authored content despite the plan explicitly requiring preservation across completed restart and failure.",
      "evidence": [
        {
          "command": "grep -n \"survive completed restart\" .jeff/tasks/0027-resolver-consolidation/notes.md",
          "output": "47|5. **README lifecycle and links — `revise`.** Consumer behavior: public P7 completion projects complete/P7 and three resolving relative links; later journalist-authored README sections survive completed restart, Gate B invalidation, failure, and recompletion. Seams: revised public demo and Gate B invalidation tests."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py (lines 185-219, 438-508)",
          "output": "The completed-restart test calls resumed = run_workflow(root) and asserts only resumed == first_state; it never adds or checks journalist content. Journalist notes are first added at lines 446-452 inside the Gate B invalidation test and are checked through invalidation/recompletion. The chart-failure test at lines 481-508 adds no journalist content and asserts only unavailable/P7/three-attempt state plus removal of complete/P7 README claims."
        },
        {
          "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py; git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py (targeted lines)",
          "output": "state.py:49-51 calls project_readme whenever completed_phase(state) == \"P7\", so completed restart reaches projection. attempts.py:48-60 records each failure and calls project_readme after the third P7 failure, so failed recomputation also reaches projection. No upstream guard makes either lifecycle transition impossible."
        }
      ]
    },
    {
      "agent_id": "AnRefute16",
      "source": "review2",
      "finding": "src/anomaly/workflow.py:111 The multi-phase dispatcher performs whole-case containment validation only once.",
      "verdict": "survives",
      "rationale": "The public dispatcher scans only before entering its multi-phase loop, while the P5/P6 adapter receives the live case root and later state/event operations use unrescanned paths; no upstream guard prevents an adapter-created symlink from being followed, including by the post-phase event append.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:101-145",
          "output": "run_workflow calls _scan_case_tree(case_root) once at line 111 before while True; each iteration then calls load_snapshot, resolves an owner, and invokes run_attempts without another whole-case scan. The reasoning wrapper passes the live case_root to invoke_resolved_owner."
        },
        {
          "command": "read src/anomaly/owners.py:37-51 and src/anomaly/workflow.py:75-99",
          "output": "For P5, consume_owner calls the external reason adapter, which is invoked with case_root, and accepts the result after only checking that findings/draft.json is_file; there is no post-adapter containment scan."
        },
        {
          "command": "read src/anomaly/attempts.py:21-59 and src/anomaly/state.py:31-48,65-93",
          "output": "After owner execution, run_attempts seals success and calls append_event. load_snapshot performs durable-state and identity work but no whole-case scan, while append_event directly opens root/.anomaly/events.jsonl in append mode. Therefore a valid P5 adapter can create a symlink at that path and the successful post-phase append follows it; the code does not fail safe."
        },
        {
          "command": "read tests/test_pipeline_walk.py:512-553",
          "output": "Both surviving dispatcher containment tests create their symlinks before run_workflow is called and assert rejection before attempts begin; neither exercises a link introduced by P5 or P6 during the loop."
        }
      ]
    },
    {
      "agent_id": "AnRefute21",
      "source": "audit",
      "finding": "src/anomaly/workflow.py:111 The dispatcher scans the case only once before a multi-owner loop, so a dynamic owner can introduce a link that later phases follow.",
      "verdict": "survives",
      "rationale": "The public dispatcher gives the P5/P6 invoker the live case root after its sole tree scan, and post-callback state/event operations perform ordinary path reads or appends without no-follow validation, so a callback can replace state.json or events.jsonl with a symlink and cause the same invocation to access an external target.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:101-145 and src/anomaly/owners.py:37-62",
          "output": "run_workflow calls _scan_case_tree only once before entering while True; P5 then calls reason(resolution), which reaches invoke(owner=..., instructions=..., case_root=Path(case_root)), and the loop contains no later _scan_case_tree call."
        },
        {
          "command": "read src/anomaly/attempts.py:21-71 and src/anomaly/state.py:31-110,188-203",
          "output": "After execute returns, run_attempts writes the result, calls _seal_success/load_snapshot, and appends phase_completed. _read_state follows .anomaly/state.json via read_text, while append_event follows .anomaly/events.jsonl via open('a'); neither path is lstat-checked or opened with no-follow semantics."
        },
        {
          "command": "grep 4a1746f9050120df52e7845166f0ab935c4e584a and supplied finding in .jeff/tasks/0027-resolver-consolidation/task.json",
          "output": "The task gate records clean=true and green=true at 4a1746f9050120df52e7845166f0ab935c4e584a, and the active audit finding is the workflow.py:111 single-scan finding evaluated here."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute8",
      "source": "review2",
      "finding": "skills/anomaly/SKILL.md:46 Installed documentation still resurrects the removed runner and states the wrong resume authority and constant owner.",
      "verdict": "survives",
      "rationale": "The installed P5 skill is loaded verbatim with no correcting guard, yet it names a durable runner, treats the last completed event as resume authority, and assigns MAX_ATTEMPTS to workflow.py even though resolution derives progress from state.completed and attempt enforcement imports the constant from state.py, directly violating the task's no-runner and stale-expectation criteria.",
      "evidence": [
        {
          "command": "grep -n 'durable runner\\|last completed event\\|MAX_ATTEMPTS' skills/anomaly/SKILL.md",
          "output": "46: `.anomaly/receipts/`, and `.anomaly/attempts/`. The durable runner records each; 232: resumes from file presence instead of the last completed event; 247: `MAX_ATTEMPTS` ... `src/anomaly/workflow.py` (the runner refuses any other value)."
        },
        {
          "command": "read src/anomaly/workflow.py:1-93,94-210 and src/anomaly/owners.py:30-65",
          "output": "P5 maps to owner (`skill`, `anomaly`); `_OWNER_INSTRUCTIONS` maps that owner to `skills/anomaly/SKILL.md`; `invoke_resolved_owner` reads the entire file as instructions; `consume_owner` invokes it for P5. `resolve_workflow` calls `completed_phase(snapshot)` to choose the next phase, and `run_workflow` delegates execution to `run_attempts`."
        },
        {
          "command": "read src/anomaly/state.py:1-35 and src/anomaly/attempts.py:1-125",
          "output": "`MAX_ATTEMPTS = 3` is defined in state.py; `completed_phase` walks snapshot[`completed`]. attempts.py imports MAX_ATTEMPTS and enforces `while attempt < MAX_ATTEMPTS`, then records success in state[`completed`]; events are appended after state/promotion logic and are not consulted for resume."
        },
        {
          "command": "grep -n 'No shared runner\\|stale expectations\\|Durable attempt staging' .jeff/tasks/0027-resolver-consolidation/task.md",
          "output": "11: durable attempt staging and retries remain separate execution utilities rather than a second orchestration API; 16: stale expectations are removed; 21: No shared runner."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute1",
      "source": "review",
      "finding": "src/anomaly/workflow_inputs.py:61 The repaired schema-aware input boundary still treats unconsumable source and gate values as complete capabilities.",
      "verdict": "survives",
      "rationale": "The public run_workflow path is reachable at P1, P4, and P7 with these malformed values, because capability detection accepts them before run_attempts invokes stricter downstream validators whose exceptions consume all three attempts, and no upstream guard makes that path unreachable or fail-safe.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow_inputs.py:20-119",
          "output": "normalize_inputs validates only top-level names; source_requests requires source-field presence but does not validate an empty source_id or the included=False reason condition; _is_complete_mapping requires only an outer list/tuple and nonblank identity, without validating sequence elements."
        },
        {
          "command": "read src/anomaly/workflow.py:85-204; src/anomaly/attempts.py:29-92; src/anomaly/owners.py:37-140",
          "output": "run_workflow derives capabilities from input_capabilities and resolves P1/P4/P7 ready, then run_attempts calls consume_owner with the original supplied values; downstream owner exceptions are caught and retried until MAX_ATTEMPTS, so malformed values consume attempts instead of pausing."
        },
        {
          "command": "read src/anomaly/acquire.py:31-65; src/anomaly/recommend.py:335-374; src/anomaly/review.py:366-447",
          "output": "The missing guards exist only after attempt entry: register_local_source rejects an empty source_id and requires reason when included is false; approve_detector_plan rejects non-string detector IDs; accept_findings rejects non-string claim IDs."
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/review-recovered-correctness.json:95-102",
          "output": "The gated reviewer probe reports blank_source_id ['sources'], non_string_gate_id ['gate_a'], non_string_claim_id ['gate_b'], and included=False without reason ['sources'], confirming each malformed payload is advertised as a capability."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute5",
      "source": "review2",
      "finding": "src/anomaly/identities.py:17 P7 completion does not validate the identities or existence of the outputs that README advertises.",
      "verdict": "survives",
      "rationale": "The blocker is reachable because load_snapshot derives completion invalidation solely from changed_identities, P7 requires only the gate_b identity, and that identity hashes only .anomaly/receipts/gate-b.json; no upstream guard revalidates findings/findings.json, findings/report.md, findings/unresolved.md, findings/charts, or .anomaly/receipts/charts.json before project_readme preserves Status: complete and its output links.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/identities.py:8-18,22-39,74-101",
          "output": "IDENTITY_PHASES assigns only \"gate_b\": \"P7\". changed_identities iterates that table and invalidates only when artifact_identity differs or is absent. _identity_files maps gate_b solely to (\".anomaly/receipts/gate-b.json\",)."
        },
        {
          "command": "read anomaly/src/anomaly/state.py:32-50",
          "output": "load_snapshot calls changed_identities(... through_phase=completed_phase(state)); if no mapped identity changed, completed remains P7 and project_readme(root, state, completed) runs."
        },
        {
          "command": "read anomaly/src/anomaly/readme.py:7-17,24-58",
          "output": "The complete projection advertises findings/findings.json, findings/report.md, and findings/unresolved.md, and sets Status to complete whenever completed is P7 and snapshot status is complete; it performs no existence or digest validation of those targets."
        },
        {
          "command": "read anomaly/src/anomaly/workflow.py:65-79",
          "output": "The P7 owner write set explicitly includes findings/findings.json, findings/report.md, findings/unresolved.md, findings/charts, .anomaly/receipts/gate-b.json, .anomaly/receipts/charts.json, events, and README, confirming the omitted artifacts are authoritative P7 outputs."
        },
        {
          "command": "read anomaly/tests/test_pipeline_walk.py:319-381,748-789",
          "output": "The completion test checks output existence only immediately after generation and resumes without mutating outputs; the P7 invalidation test mutates only gate-b.json. There is no consumer test deleting or changing each advertised/authoritative P7 output after completion."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute3",
      "source": "review",
      "finding": "tests/test_skill.py:76 The deleted runner/verb/event-resume model remains as a stale test expectation and installed skill contract.",
      "verdict": "survives",
      "rationale": "The blocker is reachable and high-severity because the public P5 path loads the entire installed skill without filtering, while that skill and its test still encode deleted runner, verb-binding, event-resume, and MAX_ATTEMPTS-location claims that directly contradict the completed-phase-map authority required by the task and PRD.",
      "evidence": [
        {
          "command": "read anomaly/tests/test_skill.py:69-79; read anomaly/skills/anomaly/SKILL.md:207-247",
          "output": "test_skill.py requires `resume.{0,120}(event|receipt|state)` and `last completed event|resume from`; SKILL.md satisfies the latter with `resumes from file presence instead of the last completed event`, claims abstract read-file/write-file/search/execute-shell/invoke-skill plus spawn-agent/wait-agent bindings, and says MAX_ATTEMPTS is enforced by workflow.py's runner."
        },
        {
          "command": "read anomaly/PRD.md:202-230; read anomaly/src/anomaly/state.py:12-25",
          "output": "PRD.md says state.json is authoritative, events.jsonl is best-effort and never resume authority, and restart follows the canonical completed-phase map; state.completed_phase walks the snapshot `completed` mapping to determine the last completed phase."
        },
        {
          "command": "read anomaly/src/anomaly/workflow.py:51-84,94-154; read anomaly/src/anomaly/owners.py:37-53",
          "output": "P5 resolves to owner `{kind: skill, id: anomaly}`; invoke_resolved_owner reads all of `skills/anomaly/SKILL.md` and passes it unchanged to the caller-supplied invoker; consume_owner invokes that reasoning owner for P5, so no upstream guard makes the stale installed instructions unreachable."
        },
        {
          "command": "read anomaly/.jeff/tasks/0027-resolver-consolidation/task.md",
          "output": "Acceptance explicitly requires `stale expectations ... removed`, and the non-goals explicitly prohibit a shared runner."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute4",
      "source": "review2",
      "finding": "src/anomaly/case.py:101 The recovered whole-case containment boundary still omits the public create path.",
      "verdict": "survives",
      "rationale": "The public create_case entry is directly reachable and resolves a symlinked root before mkdir and every write without ever calling _scan_case_tree, while inspect_case and resume_case scan the unresolved caller path first; therefore creation can follow a root symlink or preserve an unrelated nested symlink and produce/write through a case boundary that the other public paths reject.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/case.py:102-151",
          "output": "create_case starts with `root = Path(root).resolve()`, then creates directories and writes AGENTS.md, README.md, case.json, instruction, data, findings, state, and event files with no `_scan_case_tree` call. By contrast, inspect_case and resume_case retain `Path(root)` and call `_scan_case_tree(root)` before existence checks or loads."
        },
        {
          "command": "read anomaly/src/anomaly/case.py:257-288",
          "output": "`_scan_case_tree` converts the caller path with abspath rather than resolve, rejects symlinks in the root/ancestor chain via lstat, and walks existing descendants rejecting nested symlinks and special files; create_case bypasses this guard entirely."
        },
        {
          "command": "read anomaly/tests/test_case.py:227-238",
          "output": "The whole-case nested-symlink behavioral test is parametrized only over `[inspect_case, resume_case]`; create_case is not covered."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute6",
      "source": "review2",
      "finding": "src/anomaly/workflow_inputs.py:39 The input-capability boundary remains only shape-aware, not schema-aware, for source and human-decision values.",
      "verdict": "survives",
      "rationale": "The public path is concretely reachable: malformed but shape-complete source and gate values become capabilities, make P1/P4/P7 ready, and reach run_attempts before the owner performs the missing value validation; run_attempts increments and persists the attempt first, retries the same invalid input through all three attempts, and marks the phase unavailable, so no upstream guard makes the failure unreachable or less severe.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow_inputs.py:39-130",
          "output": "input_capabilities adds sources whenever source_requests accepts the container, but source_requests checks only list/nonempty, mapping, and field names; it does not validate field values. _is_complete_mapping adds gate_a/gate_b when the decision field is merely a list/tuple and the identity is a nonblank string; it neither validates collection elements nor requires Gate A approved_ids to be nonempty."
        },
        {
          "command": "read src/anomaly/workflow.py:158-205",
          "output": "run_workflow is the public dispatcher. It derives capabilities from the supplied values, resolves the phase from those capabilities, and, for a ready resolution, calls run_attempts with consume_owner; there is no intervening schema validation."
        },
        {
          "command": "read src/anomaly/owners.py:52-91,129-136; src/anomaly/recommend.py:343-362; src/anomaly/review.py:428-432",
          "output": "The P4 owner passes approved_ids to approve_detector_plan, which rejects an empty collection or non-string entries; the P7 owner passes accepted_claim_ids to accept_findings, which rejects non-string entries. These owner checks occur inside the callback already handed to run_attempts. Source requests likewise reach _register_sources after being classified complete."
        },
        {
          "command": "read src/anomaly/attempts.py:29-83,153-203",
          "output": "run_attempts increments attempts[phase], writes active state, and records phase_started before execute(workspace). An owner exception is recorded as a failed attempt and retried while attempt < MAX_ATTEMPTS; at attempt 3 the phase is persisted as unavailable and blocked."
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/notes.md:9-18; tests/test_pipeline_walk.py:209-318",
          "output": "The locked approach says schema validation must produce complete input capabilities and keep human pauses attempt-free. Existing public tests cover missing now and missing gate keys, but the shown gate tests do not cover malformed decision elements, Gate A's empty selection at a Gate A entry, or malformed source-field values."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute7",
      "source": "review2",
      "finding": "tests/test_pipeline_walk.py:518 No behavioral test proves journalled promotion rollback or interrupted-promotion recovery.",
      "verdict": "survives",
      "rationale": "The blocker survives because the line-518 test fails during owner execution before promotion starts, while the public workflow can be interrupted after an entry is applied and invokes durable recovery on the next run; no test reaches or observes that restart contract.",
      "evidence": [
        {
          "command": "grep -RniE 'promotion|recover_interrupted|rollback|_apply|attempt_workspace|promote' tests",
          "output": "The only relevant test hits are tests/test_pipeline_walk.py:518 and :791, both pre-promotion failure-isolation cases, plus tests/test_case.py:426, which only checks fork cleanup; no test references recover_interrupted_promotion, _apply, rollback, or a promotion journal."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:25-69,75-105; src/anomaly/workflow.py:163-170",
          "output": "promote_workspace writes .anomaly/promotion.json, _apply journals each entry as applying/applied around live replacements, and recover_interrupted_promotion rolls back an unsealed journal, rewinds the attempt, and removes recovery artifacts. run_workflow calls recover_interrupted_promotion at every public entry, so interruption after replacement and restart is reachable."
        },
        {
          "command": "read tests/test_pipeline_walk.py:518-551,791-828",
          "output": "The line-518 invalid-source case raises while consuming the source batch in the workspace, and the P7 case makes findings/charts a file so phase execution fails in the workspace; neither forces a failure after _apply begins, restarts with a journal, checks restoration to the sealed live case, checks attempt rewind, or proves a fresh resolution succeeds."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute9",
      "source": "audit",
      "finding": "src/anomaly/_attempt_workspace.py:109 A case-controlled promotion journal can delete arbitrary regular files or directories inside the case during public resume recovery.",
      "verdict": "survives",
      "rationale": "The blocker is reachable: run_workflow accepts a portable case whose promotion journal and target are ordinary files, then recovery trusts the journal's unbound original/status fields, and an unsealed applied entry with original=false and no backup causes _rollback to call discard_workspace on the attacker-selected in-case path.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/workflow.py:158-166",
          "output": "run_workflow calls resume_case(case_root), resolves the root, and then calls recover_interrupted_promotion(case_root) before loading the workflow snapshot."
        },
        {
          "command": "read anomaly/src/anomaly/case.py:257-290",
          "output": "_scan_case_tree rejects symlinks, non-regular artifacts, and executable files, but accepts ordinary .anomaly/promotion.json and README.md files; it does not validate promotion-journal semantics."
        },
        {
          "command": "read anomaly/src/anomaly/_attempt_workspace.py:57-69,109-119,159-193",
          "output": "Recovery reads the journal, treats a non-sealed/non-rolled-back journal as rollback work, and _rollback skips only status=pending; when backup.exists() is false and entry['original'] is false, it calls discard_workspace(live). _read_journal validates schema_version, entry object/list shape, relative path syntax, and phase/attempt path spelling, but does not bind paths to _OWNER_REGISTRY write sets or validate original/status transitions."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute10",
      "source": "audit",
      "finding": "src/anomaly/semantics.py:52 The shared durable-error sanitizer misses standard compound credential assignments such as AWS_SECRET_ACCESS_KEY and OPENAI_API_KEY.",
      "verdict": "survives",
      "rationale": "The blocker survives because the gated sanitizer's leading word boundary cannot start at SECRET or API after an underscore, no other sanitizer pattern covers these compound assignment names, and real P5/P6 invoker exceptions are caught and copied through safe_error into every named durable failure surface without an upstream guard.",
      "evidence": [
        {
          "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/semantics.py",
          "output": "58:_SECRET_ASSIGNMENT = re.compile(\n59:    r\"(?i)\\b(api[_-]?key|access[_-]?token|auth(?:entication)?|authorization|\"\n60:    r\"credential|password|passwd|secret|token|private[_-]?key)\"\n61:    r\"\\s*[:=]\\s*(?:Bearer\\s+)?[^,;\\s]+\"\n63:_BEARER = re.compile(r\"(?i)\\bBearer\\s+[A-Za-z0-9._~+/=-]{8,}\")\n64:_TOKEN_PREFIX = re.compile(\n65:    r\"\\b(?:sk_live_|ghp_|github_pat_|xox[baprs]-|AKIA)[A-Za-z0-9_./+=-]{8,}\"\n67:_URL_USERINFO = re.compile(r\"(?i)\\b(https?://)[^/\\s:@]+:[^@\\s/]+@\")"
        },
        {
          "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/workflow.py && git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/attempts.py",
          "output": "workflow.py:155:return invoke(owner=selected, instructions=instructions, case_root=Path(case_root)); workflow.py:198-206 passes consume_owner into run_attempts. attempts.py:63-68 catches Exception from execute(workspace) and sends it to _finish_failure."
        },
        {
          "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/state.py && git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/attempts.py",
          "output": "state.py:122-127 safe_error returns str(redact_credentials(str(value)))[:2000]. attempts.py:172-196 assigns that message to failure.json, phase_failed events.jsonl, state.failures, and final-attempt blocked_reason; lines 197-205 also emit it in phase_unavailable."
        },
        {
          "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:tests/test_pipeline_walk.py",
          "output": "test_failed_reasoning_attempts_redact_credentials_from_all_durable_evidence covers Authorization: Bearer, password=, AKIA under aws=, xoxb under slack=, and URL userinfo, but contains no AWS_SECRET_ACCESS_KEY or OPENAI_API_KEY assignment."
        }
      ]
    },
    {
      "agent_id": "AnPostRefute2",
      "source": "review",
      "finding": "src/anomaly/attempts.py:48 A process interruption can consume an attempt without failure evidence and eventually strand run_workflow in an infinite ready/no-op loop.",
      "verdict": "survives",
      "rationale": "The public dispatcher reaches an interruption window after durably incrementing the phase attempt but before any failure evidence or promotion journal exists; because BaseException interruptions bypass both Exception handlers, three such interruptions leave active attempts at the limit, where the resolver still returns ready while run_attempts executes nothing, so run_workflow has no terminal path.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/attempts.py:1-180",
          "output": "run_attempts increments attempts[phase], writes active state, creates the attempt directory, and appends phase_started before entering owner execution; both failure paths catch only Exception, while the bounded loop is `while attempt < MAX_ATTEMPTS` and falls through by returning the unchanged state."
        },
        {
          "command": "read anomaly/src/anomaly/_attempt_workspace.py:1-190",
          "output": "promote_workspace creates `.anomaly/promotion.json` only after owner execution and output validation reach promotion; recover_interrupted_promotion immediately returns when that journal does not exist, so it cannot rewind an interruption during workspace creation or owner execution."
        },
        {
          "command": "read anomaly/src/anomaly/workflow.py:70-225",
          "output": "resolve_workflow treats only status blocked/unavailable as terminal and otherwise reports ready with `attempts + 1` without enforcing MAX_ATTEMPTS; run_workflow repeatedly calls run_attempts and returns only for complete, unavailable, paused, blocked, or unavailable state, so active attempts==3 repeats ready/no-op indefinitely."
        },
        {
          "command": "read anomaly/src/anomaly/owners.py:1-155",
          "output": "The public run_workflow path passes its resolved owner through consume_owner, which directly invokes deterministic handlers or the supplied reasoning callback; there is no upstream BaseException guard that makes an interruption unreachable."
        },
        {
          "command": "grep 'KeyboardInterrupt|BaseException|interrupted attempt|promotion recovery|recover_interrupted_promotion|attempt 4 of 3' anomaly/tests anomaly/src/anomaly",
          "output": "KeyboardInterrupt/BaseException coverage exists for prepare/profile commit restoration, but no attempt-runner interruption or exhausted-active recovery test appears; the only workflow-level recovery hit is recover_interrupted_promotion."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute1",
      "source": "review",
      "finding": "src/anomaly/_attempt_workspace.py:262 Registry binding still lets an untrusted promotion journal delete a legitimate phase-owned artifact by lying that it did not originally exist.",
      "verdict": "survives",
      "rationale": "The public dispatcher accepts a regular-file portable case containing a forged but schema-valid P1 journal, and no upstream guard independently establishes the target's pre-promotion existence before the non-original applied branch authorizes deletion.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:22-83,158-176; src/anomaly/case.py:139-151,257-295",
          "output": "P1's fixed registry write set includes data/sources.json; run_workflow calls resume_case and then recover_interrupted_promotion, while resume_case's tree scan rejects links/special/executable files but does not authenticate promotion-journal contents or artifact provenance."
        },
        {
          "command": "read src/anomaly/case.py:37-62,102-124",
          "output": "create_case creates data/raw and .anomaly/receipts and writes legitimate data/sources.json and .anomaly/events.jsonl, so all four P1 live targets exist before P1."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:166-264",
          "output": "_read_journal accepts an exact P1 path set with boolean original values and allowed entry statuses; for a prepared entry with original=false and status=applied, _validate_recovery_artifacts declares it coherent solely when the backup is absent, without checking the existing live target."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:57-70,109-136",
          "output": "An unsealed public recovery calls _rollback; for the forged applied data/sources.json entry, the absent backup and original=false branch calls discard_workspace(live), deleting the legitimate file, before _rewind_attempt and cleanup."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute2",
      "source": "review",
      "finding": "src/anomaly/case.py:166 The public fork writer still follows a symlinked destination ancestor and can create the child case outside the caller-selected path.",
      "verdict": "survives",
      "rationale": "The public fork_case entry point resolves the caller-supplied destination before validation, scans only the source, and passes the resolved path directly to shutil.copytree, so an existing symlink ancestor is followed and a nonexistent child is created under the ancestor's external target with no later guard to restore caller-selected containment.",
      "evidence": [
        {
          "command": "read anomaly/.git/HEAD:raw",
          "output": "429c34deae1b9f5a92252147ec16acebfcb4812b"
        },
        {
          "command": "read anomaly/src/anomaly/case.py:157-181",
          "output": "fork_case performs `dest = Path(dest).resolve()`, then `_scan_case_tree(source)`, and later `shutil.copytree(source, dest)`; there is no scan or containment check on the unresolved destination."
        },
        {
          "command": "read anomaly/src/anomaly/case.py:257-286",
          "output": "_scan_case_tree would reject a symlink in the path argument's ancestor chain via lstat, but fork_case invokes this guard only for source after dest has already been resolved."
        },
        {
          "command": "read anomaly/tests/test_case.py:320-450",
          "output": "The fork symlink tests exercise links within the source tree and a symlinked source alias; they do not exercise a symlinked destination root or destination ancestor."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute3",
      "source": "review",
      "finding": "src/anomaly/workflow_inputs.py:168 Whitespace-only Gate A and Gate B IDs are still classified as complete human decisions.",
      "verdict": "survives",
      "rationale": "The public capability predicate accepts whitespace-only sequence IDs, so Gate A enters bounded execution and consumes attempts on an unknown detector while Gate B can ignore the unknown claim, seal an empty accepted set, and complete instead of pausing at the human boundary.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow_inputs.py:39-57,116-174",
          "output": "input_capabilities adds gate_a/gate_b whenever gate_a_input/gate_b_input returns; _gate_input rejects sequence members only when they are non-strings, empty strings, or (for Gate A) contain separators, and never checks item.strip(), although the actor is explicitly checked with actor.strip(). Thus ['   '] is phase-consumable for both gates."
        },
        {
          "command": "read src/anomaly/workflow.py:85-128,158-205; src/anomaly/owners.py:49-72,135-145; src/anomaly/attempts.py:29-75",
          "output": "P4/P7 readiness depends on the derived gate capability; run_workflow sends a ready phase into run_attempts, whose exception path records and retries attempts. P4 passes the whitespace ID to approve_detector_plan, while P7 passes it to accept_findings, so no upstream guard restores the required no-attempt pause."
        },
        {
          "command": "read src/anomaly/recommend.py:335-379; src/anomaly/review.py:366-474",
          "output": "approve_detector_plan rejects an ID absent from the recommended detector set, making whitespace-only Gate A fail inside an attempt. accept_findings builds by_id from real claims and continues when claim is missing; it then writes findings and the Gate B receipt from the promoted list, so a whitespace-only requested claim can produce an empty accepted set and complete P7."
        },
        {
          "command": "read tests/test_pipeline_walk.py:378-431; .jeff/tasks/0027-resolver-consolidation/task.md:7-15; .jeff/tasks/0027-resolver-consolidation/context.md:58-90",
          "output": "The public invalid-value tables cover empty/non-string/duplicate/unsafe/over-limit Gate A values and non-string/duplicate Gate B values, but omit whitespace-only IDs. The locked contract preserves Gate A/Gate B turn boundaries, and the context states human-gate pauses consume no phase attempt."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute4",
      "source": "review",
      "finding": "tests/test_pipeline_walk.py:605 The interrupted-attempt test never exercises restart reconciliation for a counted attempt with no failure evidence.",
      "verdict": "survives",
      "rationale": "The test's KeyboardInterrupt is always caught by run_attempts' BaseException handler and persisted by _finish_failure before control returns, so the next invocation finds recorded evidence and cannot enter the missing-evidence reconciliation branch; no upstream guard or alternate assertion makes the claimed restart failure unreachable.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/attempts.py:29-125",
          "output": "run_attempts durably increments and writes the attempt count, but both execution and promotion wrap BaseException with _finish_failure(...) before re-raising; _reconcile_interrupted_attempt only synthesizes interrupted failure evidence when no matching recorded failure exists."
        },
        {
          "command": "read anomaly/src/anomaly/attempts.py:220-275",
          "output": "_finish_failure calls _record_failure, which writes failure.json, appends phase_failed, adds the failure to state, and writes state before returning."
        },
        {
          "command": "read anomaly/tests/test_pipeline_walk.py:605-641",
          "output": "The test raises KeyboardInterrupt three times through run_attempts, then asserts failures 1, 2, and 3 and each failure.json exist; it never creates a durable counted attempt lacking failure evidence or restarts from such a state."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute5",
      "source": "review",
      "finding": "tests/test_pipeline_walk.py:644 The claimed valid interrupted-promotion test constructs a journal state the sequential promoter cannot produce.",
      "verdict": "survives",
      "rationale": "The producer preserves the P1 write order and `_apply` advances sequentially, persisting each entry as `applied` before touching the next, so a journal with only the final events entry applied is unreachable; the fixture also supplies only that entry's backup rather than the applied-prefix artifacts a genuine interruption would leave, and journal validation contains no prefix-order guard that could make this fabricated state representative.",
      "evidence": [
        {
          "command": "read anomaly/tests/test_pipeline_walk.py:644-672",
          "output": "The fixture creates canonical-order entries, creates only `promotion-backup/.anomaly/events.jsonl`, mutates the live events file, then executes `entries[-1][\"status\"] = \"applied\"`; all three preceding entries remain `pending`."
        },
        {
          "command": "read anomaly/src/anomaly/workflow.py:8-28 and anomaly/src/anomaly/_attempt_workspace.py:25-47,82-104",
          "output": "P1 writes are ordered `data/sources.json`, `data/raw`, `.anomaly/receipts`, `.anomaly/events.jsonl`; `promote_workspace` creates entries in validated write order, and `_apply` loops in that order, persisting `applying`, moving artifacts, then persisting `applied` before advancing."
        },
        {
          "command": "read anomaly/src/anomaly/_attempt_workspace.py:166-223,244-270",
          "output": "Journal parsing validates allowed per-entry statuses and write-path set equality but does not require statuses to form an applied/applying prefix; artifact validation consequently accepts the test's pending-prefix/applied-last construction when only the events backup exists."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute6",
      "source": "review",
      "finding": "tests/test_pipeline_walk.py:196 The P7 identity matrix does not prove that a changed chart descendant invalidates completion.",
      "verdict": "survives",
      "rationale": "The completed-case matrix only deletes the whole charts directory; because that mutation also fails an existence-only implementation, it does not exercise the distinct descendant path that production hashes by relative path, kind, and file content, and no other test requires a descendant mutation to cause P7 invalidation plus README demotion.",
      "evidence": [
        {
          "command": "read anomaly/tests/test_pipeline_walk.py:196-215",
          "output": "P7_OUTPUT_MUTATIONS mutates findings.json, report.md, unresolved.md, removes findings/charts with shutil.rmtree, and mutates charts.json; it never changes, adds, removes, or changes the kind of a charts descendant."
        },
        {
          "command": "read anomaly/tests/test_pipeline_walk.py:1081-1101",
          "output": "test_changed_or_missing_p7_output_demotes_completion_and_readme applies only P7_OUTPUT_MUTATIONS, then asserts paused at gate_b, invalidated_from == P7, Status: active, Last completed phase: P6, and removal of the outputs block."
        },
        {
          "command": "read anomaly/src/anomaly/identities.py:78-105",
          "output": "_completion_identity separately requires findings/charts to be a directory, recursively enumerates descendants, rejects symlinks and unsupported kinds, hashes directory markers, and hashes each descendant file's relative path, kind, and bytes."
        },
        {
          "command": "grep 'findings[\"\\' /]+charts|charts_dir|charts_path|_chart_files' anomaly/tests",
          "output": "The only pipeline-walk mutations are whole-directory removal and replacing charts with a non-directory before initial P7; test_report.py exercises chart generation/idempotence and stale-file cleanup, but no test mutates a completed chart descendant and asserts workflow invalidation or README demotion."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute7",
      "source": "review2",
      "finding": "src/anomaly/case.py:166 The public fork writer follows a symlinked destination namespace and can copy a case outside the caller's requested tree.",
      "verdict": "survives",
      "rationale": "The public entry point resolves the caller-supplied destination before scanning only the source, so an existing symlink ancestor is converted into an external destination and no later guard restores containment to the requested namespace.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/case.py:150-181",
          "output": "fork_case assigns `dest = Path(dest).resolve()`, calls `_scan_case_tree(source)` only, and then calls `shutil.copytree(source, dest)`; there is no destination scan between resolution and the copy."
        },
        {
          "command": "read anomaly/src/anomaly/case.py:334-381",
          "output": "Downstream `_under_root` checks paths relative to the already-resolved `root`; once `dest` has become the external path, these checks accept writes inside that external path rather than enforcing the caller's original namespace."
        },
        {
          "command": "read anomaly/tests/test_case.py:414-441",
          "output": "The existing fork symlink test passes a symlink as `source` and a normal `dest`; it proves source rejection but provides no guard or coverage for a symlinked destination ancestor."
        },
        {
          "command": "grep 429c34deae1b9f5a92252147ec16acebfcb4812b anomaly/.jeff/tasks/0027-resolver-consolidation/task.json",
          "output": "The task gate records hash `429c34deae1b9f5a92252147ec16acebfcb4812b` with `clean: true` and `green: true`, confirming the inspected final-cycle checkpoint."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute8",
      "source": "review2",
      "finding": "src/anomaly/_attempt_workspace.py:112 A second interruption during promotion rollback leaves a valid recovery state that the next restart rejects permanently.",
      "verdict": "survives",
      "rationale": "The failure is reachable because applied original entries are persisted before rollback, rollback can restore one by consuming its backup without journalizing that entry, and every later public restart deterministically rejects the resulting original/applied/live-without-backup state before rollback can resume.",
      "evidence": [
        {
          "command": "read src/anomaly/_attempt_workspace.py:82-128",
          "output": "_apply persists each entry as status \"applied\" and can persist the whole journal as \"promoted\"; _rollback then calls backup.replace(live) for each non-pending original entry but neither changes the entry status nor writes the journal inside the loop, writing status \"rolled_back\" only after every entry is restored."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:245-270",
          "output": "For a non-rolled-back original entry, _validate_recovery_artifacts accepts only backup.exists() or (status == \"applying\" and live.exists()). After partial rollback of an \"applied\" entry, backup is absent and live exists, so coherent is false and WorkflowError is raised; no upstream recovery guard accepts this legitimate rollback-progress shape."
        },
        {
          "command": "read src/anomaly/workflow.py:158-178",
          "output": "The public run_workflow entry calls recover_interrupted_promotion before normal resolution/execution, so the inconsistent validation is reached on a real public restart and aborts that restart."
        },
        {
          "command": "read tests/test_pipeline_walk.py:190-228,644-694",
          "output": "P1's real promotion write set has four entries, and the existing restart test models only one applied entry with its backup still present. Multiple applied original entries make interruption after restoring an earlier applied entry reachable, while current coverage does not exercise a second interruption during rollback."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute9",
      "source": "audit",
      "finding": "src/anomaly/_attempt_workspace.py:261 A schema-valid, registry-bound forged P7 promotion journal can still delete the case README and its journalist-authored content during restart.",
      "verdict": "survives",
      "rationale": "The blocker is reachable: public run_workflow resumes an ordinary unsealed case and immediately recovers any journal; an exact P7 journal may mark the live README.md as applied/original=false with no backup, which passes artifact validation and causes rollback to delete the README, while the only README projector returns without recreating a missing file.",
      "evidence": [
        {
          "command": "read src/anomaly/workflow.py:66-79,166-175",
          "output": "P7's fixed registry write tuple includes \"README.md\"; run_workflow calls resume_case(case_root), resolves the root, then calls recover_interrupted_promotion(case_root, lambda phase: _OWNER_REGISTRY[phase][2]) before loading and resolving the workflow snapshot."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:195-220,237-264",
          "output": "_read_journal requires entry paths to equal the phase write set, but for every non-pending entry with original=false, _validate_recovery_artifacts sets coherent = not backup.exists() and does not require the live path to be absent; therefore an applied/original=false README.md with no backup is accepted even when README.md exists."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:57-72,112-132; src/anomaly/readme.py:24-30",
          "output": "For an unsealed journal, recover_interrupted_promotion calls _rollback. _rollback skips only pending entries; when an entry has no backup and original=false it calls discard_workspace(live), deleting the existing README.md. project_readme explicitly returns when README.md is not a file, so later P7 execution does not restore the deleted journalist-authored content."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute10",
      "source": "audit",
      "finding": "src/anomaly/attempts.py:29 The documented public run_attempts utility writes before enforcing whole-case no-symlink containment.",
      "verdict": "survives",
      "rationale": "A valid-phase direct call reaches load_snapshot before any containment check, and load_snapshot creates durable paths through a symlinked root or .anomaly directory; the only later scan examines a copied workspace after owner execution, so no upstream guard prevents the documented public utility from writing outside the case and the high CWE-59 severity is honest.",
      "evidence": [
        {
          "command": "git rev-parse HEAD",
          "output": "429c34deae1b9f5a92252147ec16acebfcb4812b"
        },
        {
          "command": "read src/anomaly/attempts.py:29-103; read src/anomaly/state.py:25-48,184-205",
          "output": "run_attempts validates only that phase is known and immediately calls load_snapshot(root). load_snapshot immediately calls _ensure_durable_tree(root), which mkdirs root/.anomaly/receipts and attempts, writes state.json through a temporary file plus replace, and touches events.jsonl; none of these operations performs lstat or calls _scan_case_tree."
        },
        {
          "command": "read src/anomaly/attempts.py:55-84; read src/anomaly/_attempt_workspace.py:10-23; read src/anomaly/case.py:257-299",
          "output": "After durable state and attempt-path writes, run_attempts calls create_workspace and execute, then calls _scan_case_tree(workspace), not _scan_case_tree(root). create_workspace uses shutil.copytree(root, workspace) without symlinks=True, so the later scan sees the copied target content rather than protecting the caller-supplied symlink path; _scan_case_tree itself would reject root/ancestor and descendant links if it had been called before the writes."
        },
        {
          "command": "read src/anomaly/workflow.py:158-210; grep -n 'run_attempts' tests/test_pipeline_walk.py skills/anomaly/SKILL.md",
          "output": "run_workflow is protected by resume_case before it calls run_attempts, but run_attempts has no equivalent guard. tests/test_pipeline_walk.py imports anomaly.attempts.run_attempts directly and calls it at lines 623 and 625, while the installed skill explicitly documents run_attempts as the durable attempt executor, confirming a separately reachable utility path rather than a dispatcher-only private helper."
        }
      ]
    },
    {
      "agent_id": "AnFinalRefute11",
      "source": "audit",
      "finding": "src/anomaly/events.py:25 The standalone public log_event writer follows case-controlled symlinks outside the case.",
      "verdict": "survives",
      "rationale": "The finding is reachable and high severity: direct callers bypass the only `_scan_case_tree` invocation, while `mkdir` and append-open follow a symlinked case root or `.anomaly` directory and the broad exception handler does not fail safe when that redirected write succeeds.",
      "evidence": [
        {
          "command": "read anomaly/src/anomaly/events.py:25-68",
          "output": "`log_event` builds `Path(case_root) / \".anomaly\" / \"events.jsonl\"`, calls `path.parent.mkdir(parents=True, exist_ok=True)`, and opens it with mode `a` at lines 39-43; it never calls `_scan_case_tree`. The only `_scan_case_tree(Path(root))` call is in the separate `phase_event` wrapper at line 62."
        },
        {
          "command": "read anomaly/tests/test_events.py:278-305",
          "output": "Tests import and invoke `log_event` directly, and `test_log_event_does_not_invent_state_for_a_missing_case` asserts `log_event(tmp_path / \"not-a-case\", ...) is not None`, demonstrating that the standalone writer is a reachable entry point and creates its destination without prior case validation."
        },
        {
          "command": "read anomaly/.jeff/tasks/0027-resolver-consolidation/task.md",
          "output": "Acceptance explicitly requires: `Whole-case no-symlink validation protects every public resume/read/write path.`"
        }
      ]
    },
    {
      "agent_id": "AnTerminalRefute2",
      "source": "review2",
      "finding": "src/anomaly/_attempt_workspace.py:119 Promotion recovery still lets forged original=true metadata and an attacker-controlled backup delete and replace a live registry-bound artifact, while its original=false rule rejects a genuine interrupted promotion of a newly created artifact.",
      "verdict": "survives",
      "rationale": "The public restart path reaches rollback before any identity binding, where an unsealed, structurally valid journal can use original=true plus any regular case-local backup to replace a live registry-bound artifact, while the validator's unconditional live-absence requirement for original=false rejects the producer-reachable state created immediately after a new staged artifact is installed.",
      "evidence": [
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:50-80",
          "output": "The recorded clean, green gate hash is exactly 49aeca91b98d03771c904cdad1a4e1bf843e7ff5, and recorded checkpoint evidence says reviewed production and tests match that hash."
        },
        {
          "command": "read src/anomaly/workflow.py:158-205; src/anomaly/case.py:139-152,257-304",
          "output": "run_workflow calls resume_case and then recover_interrupted_promotion before load_snapshot or identity validation; resume_case rejects links, special files, and executables but accepts ordinary attacker-controlled promotion.json and backup files inside a portable case."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:57-70,109-130,166-299",
          "output": "For an unsealed journal, recovery validates artifacts and calls _rollback; original=true/applied is accepted whenever backup.exists(), after which rollback discards live and backup.replace(live), with no digest or trusted provenance binding. For every original=false non-pending state, validation instead requires not live.exists()."
        },
        {
          "command": "read src/anomaly/_attempt_workspace.py:82-107; src/anomaly/case.py:49-58,127-137; src/anomaly/workflow.py:68-79",
          "output": "_apply durably marks an entry applying, then staged.replace(live), then marks it applied, so a crash after replacement leaves a genuine original=false entry with live present. P7 writes newly created artifacts such as findings/report.md and findings/findings.json, which create_case does not initialize, making this rejected state producer-reachable."
        },
        {
          "command": "read tests/test_pipeline_walk.py:191-260,674-849",
          "output": "Focused recovery tests cover trusted existing-original rollback, rollback re-interruption, and forged original=false metadata against already-live P1/P7 artifacts; they do not cover forged original=true backup replacement or recovery after installation of a genuinely new original=false artifact."
        }
      ]
    },
    {
      "agent_id": "AnTerminalRefute3",
      "source": "review2",
      "finding": "src/anomaly/owners.py:28 The consolidated dynamic-review boundary still exposes two live names for the same attestation field instead of a clean alias-free contract.",
      "verdict": "survives",
      "rationale": "The public run_workflow → invoke_resolved_owner → consume_owner path applies no upstream shape guard, and _review_owner_result demonstrably accepts both the installed persona’s attestation field and the focused recovery fixture’s independent_attestation field as the same canonical value, so the duplicate live contract is reachable and violates the task’s explicit unused-alias removal criterion.",
      "evidence": [
        {
          "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly rev-parse HEAD",
          "output": "49aeca91b98d03771c904cdad1a4e1bf843e7ff5"
        },
        {
          "command": "read src/anomaly/workflow.py:135-213",
          "output": "run_workflow wraps the supplied invoke callback with invoke_resolved_owner; invoke_resolved_owner returns the callback result unchanged; the P0-P7 loop passes that result through consume_owner. No upstream response-field guard selects one attestation name."
        },
        {
          "command": "read src/anomaly/owners.py:22-30,141-151",
          "output": "_REVIEW_FIELDS contains both \"independent_attestation\" and \"attestation\". _review_owner_result rejects only fields outside that set, then converts attestation to independent_attestation; it rejects only the case where both are simultaneously present."
        },
        {
          "command": "grep 'attestation|independent_attestation' agents/anomaly-data-reviewer.md tests/test_pipeline_walk.py",
          "output": "agents/anomaly-data-reviewer.md:19 and :52-53 instruct the live persona to return attestation; tests/test_pipeline_walk.py:42 uses independent_attestation in the focused canonical dynamic-review fixture."
        },
        {
          "command": ".venv/bin/python -c \"from anomaly.owners import _review_owner_result; ... compare canonical and alias inputs ...\"",
          "output": "{\"alias_keys_after_acceptance\": [\"independent_attestation\", \"reviewer_id\", \"verdicts\"], \"canonical_keys\": [\"independent_attestation\", \"reviewer_id\", \"verdicts\"], \"equal\": true}"
        },
        {
          "command": "read .jeff/tasks/0027-resolver-consolidation/task.md",
          "output": "Acceptance criterion 8 requires: \"Unused aliases ... are removed.\""
        }
      ]
    },
    {
      "agent_id": "AnTerminalRefute1",
      "source": "review",
      "finding": "src/anomaly/_attempt_workspace.py:64 A hard exit after a phase is durably sealed but before promotion cleanup permanently wedges restart for phases that create a new artifact.",
      "verdict": "survives",
      "rationale": "The public restart path is genuinely wedged: a process stop after the completed state write leaves a promoted journal whose non-original applied artifact is live, but restart validates that journal before checking its sealed state and rejects the live artifact, so cleanup is never reached and every subsequent restart raises again.",
      "evidence": [
        {
          "command": "git rev-parse HEAD; git diff --exit-code 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 -- src/anomaly/_attempt_workspace.py src/anomaly/attempts.py src/anomaly/workflow.py tests/test_pipeline_walk.py",
          "output": "HEAD is exactly 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 and the focused diff exits 0."
        },
        {
          "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/_attempt_workspace.py",
          "output": "Lines 103-109 move each staged artifact live, persist entry status applied, then persist journal status promoted. Lines 48-54 write the completed state before _finish. On restart, line 64 calls _read_journal before lines 66-67 can recognize the seal and clean up; _read_journal lines 225-226 invokes artifact validation, where lines 288-291 require every non-original non-pending artifact to be absent and raise WorkflowError when the promoted live artifact exists."
        },
        {
          "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/workflow.py; git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/case.py",
          "output": "The condition is reachable from run_workflow: workflow.py:170-175 invokes promotion recovery on every public restart, and the P2 write set at lines 29-33 includes data/index.duckdb. case.py:49-58 and 127-132 create the initial directories and instruction stubs but not data/index.duckdb, so its first successful P2 promotion records original=false and leaves that artifact live."
        },
        {
          "command": "git grep -n -E 'hard_exit|interrupted_promotion|rollback|promotion\\.json' 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 -- tests/test_pipeline_walk.py",
          "output": "Focused recovery coverage contains the attempt-count crash at lines 625-660, an unsealed producer-ordered promotion rollback at 674-715, and rollback re-interruption at 722-781. It does not exercise a crash after the state contains the completed attempt while a promoted journal and a genuine original=false live artifact remain, so those passing recovery tests do not guard this window."
        }
      ]
    },
    {
      "agent_id": "AnTerminalRefute4",
      "source": "audit",
      "finding": "src/anomaly/_attempt_workspace.py:287 Case-local promotion backups still act as forgeable provenance and can overwrite registry-bound live artifacts during public restart.",
      "verdict": "survives",
      "rationale": "The public run_workflow entry invokes interrupted-promotion recovery before normal resolution, an unsealed original=true applied entry is accepted solely because its case-local backup exists, and rollback then deletes the live registry-bound artifact and replaces it with that backup; the focused tests preserve this behavior for original=true backups and reject only the distinct original=false forgery.",
      "evidence": [
        {
          "command": "git rev-parse HEAD",
          "output": "49aeca91b98d03771c904cdad1a4e1bf843e7ff5"
        },
        {
          "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/workflow.py (lines 169-175)",
          "output": "case_root = Path(root); resume_case(case_root); case_root = case_root.resolve(); recover_interrupted_promotion(case_root, lambda phase: _OWNER_REGISTRY[phase][2])"
        },
        {
          "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/_attempt_workspace.py (lines 64-70, 124-130, 284-291)",
          "output": "Recovery reads the journal and, unless _is_sealed, calls _rollback. For original=true, _validate_recovery_artifacts sets coherent = backup.exists() or (status == \"applying\" and live.exists()); _rollback then executes discard_workspace(live) followed by backup.replace(live). No independent provenance check binds the backup to a producer run."
        },
        {
          "command": "git show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:tests/test_pipeline_walk.py (lines 685-816)",
          "output": "test_public_restart_rolls_back_a_producer_ordered_interrupted_promotion creates a case-local data/sources.json backup, marks original=true/status=applied, calls run_workflow, and expects the backup restored; test_public_restart_rejects_false_non_original_provenance_without_deleting_live_data covers P1/P7 only with original=false/status=applied, so it does not reject an attacker-supplied original=true backup."
        },
        {
          "command": ".venv/bin/python -B -m pytest -p no:cacheprovider tests/test_pipeline_walk.py::test_public_restart_rolls_back_a_producer_ordered_interrupted_promotion tests/test_pipeline_walk.py::test_public_restart_resumes_after_a_second_interruption_during_rollback tests/test_pipeline_walk.py::test_public_restart_rejects_false_non_original_provenance_without_deleting_live_data tests/test_pipeline_walk.py::test_public_restart_rejects_untrusted_promotion_journal_without_case_mutation -q",
          "output": "Pytest did not initialize: FileNotFoundError: No usable temporary directory found; the verdict relies on the gated source and focused test inspection above, not an unexecuted test claim."
        }
      ]
    },
    {
      "agent_id": "AnTerminalRefute5",
      "source": "audit",
      "finding": "src/anomaly/_attempt_workspace.py:289 A producer-reachable interruption after first creating a phase artifact permanently blocks promotion recovery.",
      "verdict": "survives",
      "rationale": "At the gated/current-identical source, `_apply` durably records `applying`, moves a previously absent staged artifact live, and only afterward records `applied`, while every public restart validates before rollback and rejects any non-pending `original=false` entry whose live artifact exists; the focused tests omit this legitimate crash state and explicitly retain the journal when the same state is presented, so there is no upstream guard or fail-safe recovery path.",
      "evidence": [
        {
          "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly diff --no-ext-diff --unified=0 49aeca91b98d03771c904cdad1a4e1bf843e7ff5 -- src/anomaly/_attempt_workspace.py tests/test_pipeline_walk.py",
          "output": "Exit 0; stdout was empty, confirming the current source and focused recovery tests are byte-identical to the gated hash."
        },
        {
          "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/_attempt_workspace.py (lines 93-107, 275-291)",
          "output": "Lines 93-107 set entry status to `applying` and atomically persist it, then for a staged artifact execute `staged.replace(live)`, and only afterward set/persist `applied`. Lines 275-291 route every non-original non-pending entry to `coherent = not live.exists()` and raise `WorkflowError(\"promotion journal recovery artifacts are inconsistent\")` when the moved artifact exists."
        },
        {
          "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 49aeca91b98d03771c904cdad1a4e1bf843e7ff5:src/anomaly/workflow.py (lines 66-78, 161-174)",
          "output": "The canonical P7 write set starts with `findings/findings.json`, `findings/report.md`, `findings/unresolved.md`, and `findings/charts`; public `run_workflow` calls `recover_interrupted_promotion` directly at entry, with no guard or fallback around it."
        },
        {
          "command": "read tests/test_pipeline_walk.py:674-818",
          "output": "The positive recovery tests construct only `original=True, status=applied` P1 entries. The false-provenance test constructs `original=False, status=applied`, expects public `run_workflow` to raise `WorkflowError`, and asserts `.anomaly/promotion.json` remains, confirming repeated public recovery cannot reach rollback or cleanup for the live non-original state."
        }
      ]
    }
  ],
  "convergence": {
    "cap": 2,
    "stages": {
      "review": {
        "blockingKickbacks": 2
      },
      "audit": {
        "blockingKickbacks": 2
      }
    },
    "council": {
      "convened": true,
      "stage": "review",
      "members": [
        {
          "agent_id": "CouncilAnomalyIntegrity-2",
          "lens": "integrity",
          "temperature": null,
          "inquiry": {
            "causalHypotheses": [
              "The promotion journal is asked to be simultaneously untrusted portable input and the sole record of pre-promotion reality; since an attacker controlling a portable case controls every file, no file-local predicate can distinguish a forged applied/original=false/live-present entry from the legitimate promotion of a brand-new artifact, so deletion decisions keyed on the self-asserted original bit are unsound by construction (F1, F9), and the mirror flaw — rollback mutating the world without journalizing per-entry progress — yields genuine states the validator rejects forever (F8).",
              "AC7's whole-case no-symlink guarantee was implemented as a library primitive plus per-entry-point discipline instead of a mandatory chokepoint on the public write API; create_case/resume_case/phase_event remember the order, fork_case/run_attempts/log_event forgot it, so the four containment blockers are instances of one missing architectural guarantee rather than four bugs.",
              "The cycle-2 seams were authored to demonstrate planned behavior rather than discriminate contract from accident: each fabricates a state the production machinery cannot reach or omits the branch-distinct input, so the suite stays green while the genuinely reachable second-interruption state deterministically bricks recovery — the seams did not merely miss F8, they concealed it.",
              "All three families follow from one unstated default: durability artifacts (promotion.json, events.jsonl, state.json) and caller-supplied paths are treated as trusted internal records at some sites and untrusted input at others, wherever convenient; making that trust boundary explicit per artifact would have surfaced every blocker in this union."
            ],
            "findingVotes": [
              {
                "id": "F1",
                "blocking": true,
                "rationale": "Verified: forged applied/original=false journal passes _validate_recovery_artifacts' else-branch and rollback's discard_workspace(live) deletes phase-owned data/sources.json. Decisive impossibility argument: the forged state is extensionally identical to a legitimately promoted new artifact, so no confined predicate separates them; only a fail-closed destructive rule (destroy only what a held/corroborated backup proves the promoter moved) fixes it. Causal-subgraph-reconstruction."
              },
              {
                "id": "F2",
                "blocking": true,
                "rationale": "fork_case resolves dest before scanning only the source; copytree lands the child under the symlink target. The containment primitive and canonical scan-unresolved-first order already exist in create_case; scanning the unresolved destination before resolve is a one-point confined repair that also closes F7. Known invariant, lapsed enforcement — not evidence for reconstruction on its own."
              },
              {
                "id": "F3",
                "blocking": true,
                "rationale": "_gate_input strip-checks the actor but only emptiness-checks sequence members, so ['   '] derives gate capability; Gate A burns attempts failing inside approve_detector_plan while Gate B seals an empty accepted set and completes P7 — completion without a human decision, violating the locked pauses-consume-no-attempts contract. Mirroring actor.strip() onto items plus value-table rows is confined-repair."
              },
              {
                "id": "F4",
                "blocking": true,
                "rationale": "Every exit path, including the BaseException handler, persists failure evidence before unwinding, so the test's caught KeyboardInterrupt can never reach _reconcile_interrupted_attempt's missing-evidence branch — the exact branch AC2 added for hard termination. The seam must synthesize a counted attempt with no failure.json and prove deterministic evidence synthesis plus the bounded stop. test-contract-repair."
              },
              {
                "id": "F5",
                "blocking": true,
                "rationale": "Ordered _apply persists each entry applied before advancing, so the fixture's pending-prefix/applied-last journal with only the last backup is unreachable; the test certifies rollback of an impossible state while reachable shapes go untested — it directly masked F8. Rebuild as a real applied-prefix (drive promote_workspace under interruption or construct prefix-consistent artifacts/backups). test-contract-repair."
              },
              {
                "id": "F6",
                "blocking": false,
                "rationale": "_completion_identity demonstrably hashes chart descendants by relative path, kind, and bytes, so no live misbehavior is shown; the gap is regression-proofing. Add descendant add/change/remove/kind-mutation rows to P7_OUTPUT_MUTATIONS asserting invalidated_from == P7 and README demotion. Coverage debt, not a defect: blocking false, test-contract-repair."
              },
              {
                "id": "F7",
                "blocking": true,
                "rationale": "Standards-lens duplicate of F2 with the identical resolution point (scan the unresolved destination before resolve); one confined repair closes both. Council should count it once against the containment family rather than as an independent defect — direct evidence the union is not eleven independent faults."
              },
              {
                "id": "F8",
                "blocking": true,
                "rationale": "Rollback restores applied-original entries by consuming backups without journalizing progress and persists rolled_back only after the loop; a second interruption leaves applied/original/backup-absent/live-restored, which validation rejects on every subsequent restart — deterministic permanent bricking through ordinary double-crash use. Ad hoc acceptance of that shape is unsound because it collides with forged F1-shaped states; rollback must become resumable with journaled restore-progress inside the same reconstructed recovery machine as F1/F9."
              },
              {
                "id": "F9",
                "blocking": true,
                "rationale": "Identical mechanism to F1 aimed at README.md; project_readme returns early on a missing file, so rollback-deleted journalist-authored content is unrecoverable. Highest-severity member of the same trust-model defect, sharing both the reconstruction remedy and the impossibility argument (legitimate new-artifact promotion is locally indistinguishable from the forgery)."
              },
              {
                "id": "F10",
                "blocking": true,
                "rationale": "Documented public run_attempts calls load_snapshot, whose durable tree mkdir/state/event writes precede any _scan_case_tree, and its only later scan inspects the copied workspace, not the caller path — AC7 requires every public write path protected. Hoisting the existing scan to entry, as phase_event already does, is confined-repair; nothing beyond the chokepoint question needs redesign."
              },
              {
                "id": "F11",
                "blocking": true,
                "rationale": "Standalone log_event mkdir+append follows symlinked ancestors with no scan; the decorator's scan misses direct callers, and the broad handler fails safe only when the redirected write errors — a successful out-of-case append returns normally. Scanning inside log_event and mapping UnsafeCasePathError to the documented None return preserves never-raise. confined-repair."
              }
            ],
            "problemRestatement": "Are these independent defects, or evidence that this part of the design should be reconstructed? Concretely: do the promotion-journal trust model, the unchoked public write surface, and the non-binding test seams reduce to one design decision — treating durability artifacts and caller paths as trusted records wherever convenient — such that F1/F8/F9 require reconstructing the promotion-recovery state machine around fail-closed destructive rules, F2/F7/F10/F11 and F3 admit confined repairs through the existing containment and validation primitives, and F4/F5/F6 require rebuilding the seams to bind to genuinely reachable production states? — Eleven survived cycle-2 blockers form three families over one substrate. Trust model (F1, F8, F9): recovery validates journal schema and registry binding but trusts the journal's claims about the world; a forged applied/original=false/no-backup entry passes the 'coherent = not backup.exists()' branch and rollback deletes the live registry-bound artifact (data/sources.json; README.md with unrecoverable journalist content), while rollback's own unjournaled progress creates legitimate states the same validator permanently rejects on every restart. Containment (F2/F7, F10, F11): the _scan_case_tree primitive and its canonical scan-unresolved-path-before-resolve-or-write order exist (create_case, resume_case, phase_event), yet fork_case resolves dest before any destination scan, run_attempts writes durable state before any scan and afterwards scans only the copied workspace, and standalone log_event never scans. Seams (F4, F5, F6): the interrupted-attempt test only produces evidence-backed interruptions so the missing-evidence reconcile branch is unreachable, the promotion fixture fabricates an applied-last/pending-prefix journal the sequential applier cannot produce, and the P7 matrix never mutates a chart descendant, leaving descendant-hash invalidation unpinned.",
            "question": "Are these independent defects, or evidence that this part of the design should be reconstructed?",
            "solutionStrategies": [
              "confined-repair",
              "test-contract-repair",
              "causal-subgraph-reconstruction"
            ],
            "decisiveEvidence": [
              "See top-level decisiveEvidence."
            ]
          }
        },
        {
          "agent_id": "CouncilAnomalySecurity-2",
          "lens": "security",
          "temperature": null,
          "inquiry": {
            "question": "Deletion authority over registry-bound artifacts currently derives from an untrusted journal's own original=false assertion, the same validator permanently rejects genuinely mid-rollback states, and the whole-case no-symlink boundary is enforced at create/resume but skipped by fork destination resolution, run_attempts pre-write durable state, and standalone log_event — should each site get its local guard while the promotion-recovery authorization alone is rebuilt on filesystem-verified provenance plus journalized rollback progress? Are these independent defects, or evidence that this part of the design should be reconstructed?",
            "problemRestatement": "Eleven surviving cycle-2 blockers at gated hash 429c34de collapse, under independent code verification, into four causal clusters. (a) Journal-asserted deletion authority: _validate_recovery_artifacts accepts a non-rolled-back entry with original=false solely because the backup is absent, never requiring the live target to be absent, and _rollback then calls discard_workspace(live) — so a schema-valid, registry-bound forged journal deletes a legitimate P1 artifact (F1) or the journalist-authored README.md during public restart (F9, irreversible because project_readme returns when README.md is missing); reachability runs through the ordinary public dispatcher, which resumes any regular-file portable case before recovering the journal. (b) Recovery liveness: _rollback restores applied originals by consuming their backups without journalizing per-entry progress, so a second interruption mid-rollback leaves an applied/original/no-backup/live shape the validator rejects on every subsequent restart (F8). (c) Symlink boundary incompleteness: fork_case resolves dest before any scan and copies into the resolved external path (F2, re-raised as F7); run_attempts loads and durably writes snapshot state through a symlinked root before its only scan, which examines the copied workspace afterward (F10); standalone log_event mkdirs and appends through case-controlled symlinks with no scan at all (F11). (d) Validation and proof gaps: _gate_input applies .strip() to the actor but not to sequence IDs, so whitespace-only Gate A/B selections count as complete human decisions and can consume attempts or seal an empty accepted P7 set (F3); the interrupted-attempt test always records evidence via the BaseException handler so the missing-evidence reconciliation branch is never exercised (F4); the valid-rollback fixture's pending-prefix/applied-last journal is unreachable from the sequential promoter (F5); and the P7 matrix removes only the whole charts directory, leaving descendant-content invalidation unproven (F6).",
            "causalHypotheses": [
              "The promotion validator substitutes schema and registry-shape checking for provenance verification: it trusts the journal's own original flag instead of testing the asserted pre-promotion existence against the live tree, making deletion authority forgeable by anyone who can place a file in a portable case (F1, F9).",
              "Rollback mutates live files and consumes backups without persisting per-entry progress, so crash windows produce intermediate states that the validator's producer-shaped invariants classify as incoherent — the same validator is simultaneously too permissive toward forgeries and too strict toward genuine progress, indicating the recovery state machine's invariants were derived per-case rather than from one verified model (F8, and the laxness F1/F9 exploit).",
              "Containment is implemented per-writer rather than as a single entry-point invariant: create_case and resume_case scan before touching disk, but fork_case resolved its destination first, run_attempts performs durable writes before any scan, and log_event ships with no scan — AC7's 'every public resume/read/write path' was satisfied entry-by-entry instead of structurally (F2, F7, F10, F11).",
              "Gate predicates validate presence, type, uniqueness, and separators but normalize only the actor field; sequence IDs miss the same .strip() discipline, so whitespace-only selections masquerade as decisions at the human turn boundary (F3).",
              "Test fixtures synthesize journal states by editing individual fields instead of replaying producer sequences, yielding unreachable shapes (pending prefix with applied tail) and assertions that cannot distinguish content-hash identity from existence-only checks (F4, F5, F6)."
            ],
            "solutionStrategies": [
              "confined-repair",
              "test-contract-repair",
              "causal-subgraph-reconstruction"
            ],
            "findingVotes": [
              {
                "id": "F1",
                "blocking": true,
                "rationale": "Verified at _attempt_workspace.py:244-264: for a non-rolled-back entry, original=False yields coherent = not backup.exists() with no live-absence requirement, and _rollback (:109-131) then discards the live artifact. run_workflow (:163-168) reaches recover_interrupted_promotion on any ordinary regular-file case, so untrusted case input grants deletion authority over a registry-bound artifact. Blocks: forgeable destructive capability. Fix is confined: require (not original) implies not live.exists() for non-pending entries, verifying the journal's existence claims against the tree before authorizing deletion."
              },
              {
                "id": "F2",
                "blocking": true,
                "rationale": "Verified at case.py: fork_case assigns dest = Path(dest).resolve() before its only scan (which covers source), then shutil.copytree(source, dest) — a symlinked destination ancestor relocates the child case outside the caller-selected namespace with no later guard, violating locked AC7 for a public writer. create_case (:102-104) proves the correct order exists in-repo. Fix is confined: lstat-scan the unresolved destination ancestor chain before resolve/copy, mirroring create_case."
              },
              {
                "id": "F3",
                "blocking": true,
                "rationale": "Verified at workflow_inputs.py:_gate_input: sequence members are rejected only for non-string/empty/(Gate A) separators, while the actor is checked with .strip(); '   ' therefore passes both gates. Downstream, approve_detector_plan fails inside a consumed attempt and accept_findings seals an empty accepted set and completes P7 — a malformed selection silently converts a pending human decision into completion, defeating the gate boundary AC4 locks. Fix is confined: apply the same nonblank rule to sequence members and extend the gate tables."
              },
              {
                "id": "F4",
                "blocking": true,
                "rationale": "Verified at attempts.py:88-93 and :106-110 — both BaseException handlers call _finish_failure before re-raising, and _record_failure persists failure.json/state/events — while _reconcile_interrupted_attempt (:119-160) synthesizes evidence only when none is recorded. The test at test_pipeline_walk.py:605-641 raises KeyboardInterrupt through run_attempts, so it can never enter the missing-evidence branch, yet that branch is the sole handler for a real hard kill between write_state and failure persistence. The AC3 seam is vacuous for its headline scenario; a regression there would go undetected. Blocks as an unverifiable durability guarantee."
              },
              {
                "id": "F5",
                "blocking": true,
                "rationale": "Verified at test_pipeline_walk.py:644-672 against _apply (:82-104): the promoter persists each entry applying→applied strictly in write order, so a journal with three pending entries and only the events tail applied — backed by a single backup — is unreachable; validation imposes no prefix-order guard, which is exactly the laxness F1/F9 weaponize. The test thereby certifies rollback behavior only from an impossible state and a validator that accepts impossible journals. Blocks: rebuild the fixture from a producer-reachable interruption (applied prefix with backups, one applying entry) and consider a status-prefix rule in _read_journal so validation admits only genuine shapes."
              },
              {
                "id": "F6",
                "blocking": false,
                "rationale": "Verified that identities._completion_identity recursively enumerates findings/charts and hashes each descendant's relative path, kind, and bytes, so a mutated chart descendant does invalidate P7 today; the matrix at test_pipeline_walk.py:196-215 merely fails to prove it, and its rmtree-only mutation cannot distinguish content hashing from existence checks. Non-blocking: no reachable falsification exists in current code. Still require one matrix row mutating a chart descendant's bytes/kind as cheap conformance insurance for the publication-truth invariant."
              },
              {
                "id": "F7",
                "blocking": true,
                "rationale": "Same code site and identical mechanism as F2, re-derived under the standards lens; independent verification confirms dest resolution precedes any containment check and copytree follows the resolved external path. The lens duplication does not dilute the underlying hole — one confined destination scan fixes both findings, but until it lands, a public writer remains outside the locked no-symlink contract. Blocks alongside F2; tracked separately only so neither review record can be closed while the escape survives."
              },
              {
                "id": "F8",
                "blocking": true,
                "rationale": "Verified at _rollback (:109-131): each non-pending original entry is restored via backup.replace(live) with no journal write inside the loop, and the rolled_back status lands only after all entries; _validate_recovery_artifacts (:244-256) then demands backup.exists() (or applying+live) for applied originals, so a second interruption mid-rollback leaves applied/original/no-backup/live — rejected with WorkflowError on every future restart, permanently locking the case out of recovery. Fail-closed-forever is an availability defect reachable by ordinary crash timing, not just adversaries. Confined fix: journalize per-entry rollback progress (or accept restored-original-no-backup-live-exists as coherent and complete the rollback idempotently)."
              },
              {
                "id": "F9",
                "blocking": true,
                "rationale": "Worst instance of the F1 root cause, verified end-to-end: P7's registry tuple includes README.md; a forged applied/original=false/no-backup README entry passes _validate_recovery_artifacts even while the live README exists; _rollback deletes it; and project_readme (readme.py:25-26) returns when README.md is not a file, so journalist-authored content is destroyed irreversibly through the public restart path. Blocks absolutely: permanent loss of human-authored evidence via untrusted input. Shares one confined fix with F1 (verify original=false claims against live absence) plus a regression test forging a P7 journal and asserting the README survives."
              },
              {
                "id": "F10",
                "blocking": true,
                "rationale": "Verified at attempts.py:29-40 — run_attempts validates only the phase name and immediately calls load_snapshot, whose durable-tree setup and atomic state write follow a symlinked root/.anomaly; the only scan (:84-86) examines the copied workspace after owner execution, so it protects neither the caller-supplied path nor the pre-execution writes. The utility is publicly documented in the installed skill and invoked directly by tests, so this is a separately reachable AC7 violation, not a dispatcher-internal shortcut. Confined fix: hoist _scan_case_tree(root) ahead of load_snapshot, mirroring resume_case."
              },
              {
                "id": "F11",
                "blocking": true,
                "rationale": "Verified at events.py: log_event builds .anomaly/events.jsonl, mkdirs parents, and opens in append mode with no containment check; the only _scan_case_tree lives in the phase_event wrapper, and test_events.py exercises bare log_event on a nonexistent case, establishing the standalone writer as supported surface. A symlinked root or .anomaly directory redirects durable event writes outside the case, and the broad never-raise handler swallows nothing here because the redirected write succeeds. Confined fix: contain the scan inside log_event's existing try, mapping UnsafeCasePathError to the documented None (event store unavailable) so best-effort logging still fails safe."
              }
            ],
            "decisiveEvidence": [
              "src/anomaly/_attempt_workspace.py:237-264 — for non-rolled-back entries, original=false coherence is 'not backup.exists()' with no live-absence requirement; original=true applied coherence demands backup.exists() or applying+live — the asymmetric pair behind F1/F9 acceptance and F8 permanent rejection.",
              "src/anomaly/_attempt_workspace.py:109-131 — _rollback restores applied originals by consuming backups without journalizing per-entry progress and writes rolled_back only after the loop, so interruption mid-loop produces the rejected applied/no-backup/live shape (F8).",
              "src/anomaly/workflow.py:163-168 — public run_workflow resumes any regular-file portable case and then calls recover_interrupted_promotion before loading state, so forged schema-valid journals are recovered through the mainline API (F1, F9 reachability).",
              "src/anomaly/readme.py:24-30 — project_readme returns when README.md is not a file, confirming deleted journalist content is never recreated (F9 irreversibility).",
              "src/anomaly/case.py:102-104 vs :162-171 — create_case scans before resolve/mkdir while fork_case resolves dest first, scans only source, and copies into the resolved path (F2/F7); _scan_case_tree(:257+) lstat-rejects link ancestors when actually invoked.",
              "src/anomaly/attempts.py:29-40 and :84-86 — run_attempts writes durable state through load_snapshot before any scan; its sole scan targets the post-copy workspace, not the caller-supplied root (F10).",
              "src/anomaly/events.py:36-46 — log_event mkdirs and append-opens .anomaly/events.jsonl with no scan; containment exists only in the phase_event wrapper (:59-62) (F11).",
              "src/anomaly/workflow_inputs.py:150-176 — _gate_input checks actor with .strip() but sequence members only for type/emptiness/separators, admitting whitespace-only IDs (F3).",
              "src/anomaly/attempts.py:88-93,106-110,119-160 — both BaseException handlers persist failure evidence pre-reraise and reconciliation synthesizes only when no recorded failure matches, so test_pipeline_walk.py:605-641 structurally cannot reach the missing-evidence branch (F4).",
              "tests/test_pipeline_walk.py:644-694 vs src/anomaly/_attempt_workspace.py:82-104 — fixture's pending-prefix/applied-tail journal with a single backup is unreachable from the sequentially persisting promoter (F5).",
              "tests/test_pipeline_walk.py:196-215 — P7_OUTPUT_MUTATIONS contains only shutil.rmtree of findings/charts among chart mutations; identities.py descendant byte/path/kind hashing is never exercised by an invalidation assertion (F6)."
            ]
          }
        },
        {
          "agent_id": "CouncilAnomalyPragmatist-2",
          "lens": "pragmatist",
          "temperature": null,
          "inquiry": {
            "causalHypotheses": [
              "Promotion-trust cluster (F1/F8/F9): _validate_recovery_artifacts encodes coherence without provenance — original=false is accepted regardless of live-file presence — while _rollback restores backups without persisting per-entry progress, so the single journalized status field must serve forgery detection, apply resumption, and rollback resumption at once and fails in both directions.",
              "Containment cluster (F2/F7/F10/F11): _scan_case_tree exists and works, but invoking it was left to each public function; writers that resolve their path first (fork_case) or were shaped as standalone utilities (run_attempts, log_event) simply never call it, so AC7 coverage regressed to whichever entries remembered the call (resume_case, inspect_case, create_case, phase_event).",
              "Value-validation gap (F3): _gate_input validates members syntactically — string, nonempty, separators, duplicates — and copies the blankness rule onto the actor alone, omitting member.strip(), so whitespace IDs satisfy the completeness predicate and gates stop pausing.",
              "Test-contract gaps (F4/F5/F6): interruption fixtures simulate termination through exceptions the runner catches and hand-build journal states out of producer order, so assertions hold over unreachable or trivially-failing states and the real contracts — hard-kill reconciliation, reachable-prefix rollback, recursive descendant identity — stay unproven."
            ],
            "decisiveEvidence": [
              "_attempt_workspace.py:247-263 — non-pending non-original coherence is literally 'not backup.exists()' with no live check, while _rollback (:109-136) deletes live when the backup is absent and original is false and writes status rolled_back only once after the loop: admits F1/F9 forgery and creates F8's rejected partial-rollback shape.",
              "workflow.py:22-83 — P1 tuple includes data/sources.json and P7 tuple includes README.md, so forged journals pass _read_journal's exact write-set equality; readme.py returns without recreating a missing README, making F9's content loss permanent.",
              "case.py:104-107 vs :166-181 — create_case calls _scan_case_tree(root) on the unresolved path before root.resolve() and any write, while fork_case resolves dest first, scans only source, and shutil.copytrees through the link: the precedent for the one-call containment repair exists in the same file.",
              "attempts.py:35-40,82-86 — run_attempts goes straight to load_snapshot (durable tree + state writes) and only later scans the copied workspace, never the root; skills/anomaly/SKILL.md documents run_attempts publicly, confirming a dispatcher-independent write path (F10).",
              "events.py:39-43,62 — log_event mkdir(parents=True) plus append-open with no scan, versus _scan_case_tree(Path(root)) solely inside the phase_event wrapper (F11); the broad except returning None keeps a scan-first change within the never-raise contract.",
              "workflow_inputs.py:156-169 — the member predicate checks isinstance/not-empty/separators/duplicates but never item.strip() while the adjacent actor check uses actor.strip(): whitespace-only IDs are classified complete (F3).",
              "tests/test_pipeline_walk.py:605-641 — all three interruptions are KeyboardInterrupt raised inside execute, always caught by attempts.py's BaseException handler which persists evidence before re-raising, so the reconciliation branch is unreachable from this test (F4).",
              "tests/test_pipeline_walk.py:644-672 vs _attempt_workspace.py:84-105 — fixture leaves entries[0..2] pending with only the events backup while marking the last entry applied; sequential _apply makes that state unreachable, and validation has no prefix-order guard, so the test passes vacuously (F5).",
              "tests/test_pipeline_walk.py:196-215 and identities.py:78-104 — the mutation table rmtrees findings/charts wholesale while the completion identity recursively hashes descendants by path/kind/content; no test mutates a completed chart descendant (F6), though production behavior appears correct."
            ],
            "findingVotes": [
              {
                "id": "F1",
                "blocking": true,
                "rationale": "Verified: the non-original branch sets coherent = not backup.exists() with no live-absence check, and _rollback discards live when the backup is absent and original is false; the P1 registry tuple binds data/sources.json, so the forged journal passes write-set equality and deletes a legitimate artifact. One half of the shared promotion-trust defect; no separate work beyond the single confined rewrite."
              },
              {
                "id": "F2",
                "blocking": true,
                "rationale": "Verified: fork_case runs Path(dest).resolve() before scanning only the source and copytree follows the link; create_case already demonstrates scan-unresolved-before-resolve, so this is the shared containment-placement defect closable by reusing one existing call, not a new mechanism."
              },
              {
                "id": "F3",
                "blocking": true,
                "rationale": "Verified: _gate_input accepts ['   '] as detector/claim IDs while requiring actor.strip(); Gate A burns attempts on an unknown detector and Gate B can seal an empty accepted set and complete, violating the locked human-gate no-attempt pause. Smallest product fix in the union: one predicate condition plus one invalid-value table row."
              },
              {
                "id": "F4",
                "blocking": true,
                "rationale": "Verified: run_attempts' BaseException handler persists failure.json and state before re-raising, so KeyboardInterrupt interruptions always leave recorded evidence and the missing-evidence reconciliation branch is never exercised; AC1's attempt-four guarantee rests on an unproven branch until a fixture constructs counted-attempt-without-evidence durable state directly. Bounded test edit, no product change shown necessary."
              },
              {
                "id": "F5",
                "blocking": true,
                "rationale": "Verified: _apply persists each entry applied in registry order before advancing, so the fixture's pending-prefix/applied-last journal holding only the events backup is unreachable by any real interruption; the test currently proves nothing about genuine rollback. Bounded fixture rebuild to a reachable applied prefix with its artifacts."
              },
              {
                "id": "F6",
                "blocking": true,
                "rationale": "Verified: _completion_identity hashes chart descendants by relative path, kind, and bytes, but P7_OUTPUT_MUTATIONS only rmtrees the directory — a mutation an existence-only implementation would also fail — so the recursive identity feature ships unproven. One matrix row closes a headline completion-truth gap cheaply."
              },
              {
                "id": "F7",
                "blocking": true,
                "rationale": "Same lines and mechanism as F2 from the standards lens; blocking because the underlying escape blocks convergence, but pragmatically it consumes no additional work — it is one repair counted once in the shared containment fix, and the vote records the deduplication."
              },
              {
                "id": "F8",
                "blocking": true,
                "rationale": "Verified: _rollback replaces backups onto live without journalizing per-entry progress, and validation requires backup.exists() for applied originals, so interruption mid-rollback produces a state every later public restart deterministically rejects — a permanent wedge. The opposite face of the same coherence predicate as F1/F9; fixed at the same single site."
              },
              {
                "id": "F9",
                "blocking": true,
                "rationale": "Verified identical mechanism to F1 against README.md, bound by the P7 registry tuple; project_readme returns without recreating a missing file, so deletion of journalist-authored content is permanent. Same confined repair as F1 — one defect reported twice by correctness and audit lenses."
              },
              {
                "id": "F10",
                "blocking": true,
                "rationale": "Verified: run_attempts checks only the phase name before load_snapshot durably writes through a potentially symlinked root, and the only later scan examines the copied workspace; the installed skill documents run_attempts as a public utility, so AC7's every-public-write-path requirement genuinely fails. Closed by the same scan-before-write call as F2/F11."
              },
              {
                "id": "F11",
                "blocking": true,
                "rationale": "Verified: standalone log_event mkdirs and append-opens .anomaly/events.jsonl with no scan; only the phase_event wrapper scans. Adding the pre-write scan inside the existing try preserves the documented never-raise contract while containing the redirected write — the third instance of the one shared containment fix."
              }
            ],
            "problemRestatement": "Eleven cycle-2 blockers survived refutation at hash 429c34de. Re-verified first-hand, they reduce to four roots: (a) the recovery-coherence predicate in _attempt_workspace.py both admits forged original=false journals whose rollback deletes live registry-bound artifacts (F1 data/sources.json, F9 README.md) and permanently rejects legitimately partially-rolled-back states because rollback never journalizes per-entry progress (F8) — one under-specified predicate, two failure directions; (b) whole-case no-symlink enforcement is caller-side discipline instead of a choke point on unresolved caller paths, so fork_case resolves dest before any scan (F2, duplicated as F7), run_attempts durably writes before any scan (F10), and standalone log_event mkdirs/appends through case-controlled symlinks (F11); (c) _gate_input strips the actor but never the sequence members, so whitespace-only Gate A/B IDs count as complete decisions (F3); (d) three tests prove contracts over states the pipeline cannot produce or paths it never exercises — catchable KeyboardInterrupt never reaches missing-evidence reconciliation (F4), a pending-prefix/applied-last journal contradicts sequential _apply (F5), and P7's mutation table rmtrees the whole charts directory instead of mutating a descendant (F6).",
            "question": "Can all eleven survivors close with three bounded product sites — (1) one confined rewrite of the promotion coherence-and-rollback pair for F1/F8/F9, (2) one reuse of the existing create_case-style scan-on-unresolved-path-before-first-write at fork_case/run_attempts/log_event for F2/F7/F10/F11, (3) member blankness checks in _gate_input for F3 — plus bounded test-contract edits for F4/F5/F6, adding no new framework, runner, registry, or alias, or does any finding force more? Are these independent defects, or evidence that this part of the design should be reconstructed?",
            "solutionStrategies": [
              "confined-repair",
              "test-contract-repair"
            ]
          }
        }
      ],
      "findings": [
        {
          "id": "F1",
          "source": "review",
          "summary": "Registry binding still lets an untrusted promotion journal delete a legitimate phase-owned artifact by lying that it did not originally exist.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F2",
          "source": "review",
          "summary": "The public fork writer still follows a symlinked destination ancestor and can create the child case outside the caller-selected path.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F3",
          "source": "review",
          "summary": "Whitespace-only Gate A and Gate B IDs are still classified as complete human decisions.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F4",
          "source": "review",
          "summary": "The interrupted-attempt test never exercises restart reconciliation for a counted attempt with no failure evidence.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F5",
          "source": "review",
          "summary": "The claimed valid interrupted-promotion test constructs a journal state the sequential promoter cannot produce.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F6",
          "source": "review",
          "summary": "The P7 identity matrix does not prove that a changed chart descendant invalidates completion.",
          "blockingVotes": 1,
          "survived": false,
          "followupTaskId": "ledger"
        },
        {
          "id": "F7",
          "source": "review2",
          "summary": "The public fork writer follows a symlinked destination namespace and can copy a case outside the caller's requested tree.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F8",
          "source": "review2",
          "summary": "A second interruption during promotion rollback leaves a valid recovery state that the next restart rejects permanently.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F9",
          "source": "audit",
          "summary": "A schema-valid, registry-bound forged P7 promotion journal can still delete the case README and its journalist-authored content during restart.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F10",
          "source": "audit",
          "summary": "The documented public run_attempts utility writes before enforcing whole-case no-symlink containment.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        },
        {
          "id": "F11",
          "source": "audit",
          "summary": "The standalone public log_event writer follows case-controlled symlinks outside the case.",
          "blockingVotes": 3,
          "survived": true,
          "followupTaskId": null
        }
      ],
      "synthesis": {
        "problemRestatement": "Eleven cycle-2 blockers survived refutation at gate hash 429c34de (clean, green) over the anomaly resolver-consolidation task, collapsing into four causal families on one substrate. Trust model (F1, F8, F9): _validate_recovery_artifacts checks journal schema and exact registry write-set equality but trusts the journal's own original flag about the world — a forged applied/original=false/no-backup entry passes coherence solely because the backup is absent, and _rollback deletes the live registry-bound artifact (data/sources.json; README.md, irreversibly, because project_readme returns on a missing file), all reachable through public run_workflow recovering any regular-file portable case; meanwhile _rollback consumes backups without journalizing per-entry progress and writes rolled_back once after the loop, so a second interruption mid-rollback produces an applied/original/no-backup/live shape the same validator rejects on every future restart. Containment chokepoint (F2/F7, F10, F11): whole-case no-symlink enforcement is caller-side discipline instead of a mandatory pre-write invariant — fork_case resolves dest before scanning anything, run_attempts durably writes snapshot state before its only scan (which examines the copied workspace, not the caller root documented as public utility surface), and standalone log_event mkdirs and append-opens through case-controlled symlinks unscanned. Gate validation (F3): _gate_input strips the actor but not sequence members, so whitespace-only Gate A/B IDs count as complete human decisions — Gate A burns attempts on an unknown detector, Gate B seals an empty accepted set and completes P7, violating the locked pauses-consume-no-attempts contract. Test seams (F4, F5, F6): the interrupted-attempt test only raises catchable KeyboardInterrupts whose evidence the BaseException handler persists, leaving the missing-evidence reconciliation branch unreachable; the valid-rollback fixture fabricates a pending-prefix/applied-last journal sequential _apply cannot produce; and the P7 matrix only rmtrees the whole charts directory, so recursive descendant path/kind/content hashing ships unproven.",
        "survivingBlockers": [
          "F1",
          "F2",
          "F3",
          "F4",
          "F5",
          "F7",
          "F8",
          "F9",
          "F10",
          "F11"
        ],
        "causalHypotheses": [
          "One unstated default splits the design: durability artifacts (promotion.json, events.jsonl, state.json) and caller-supplied paths are treated as trusted internal records at some sites and untrusted input at others, wherever convenient — the recovery validator substitutes schema and registry-shape checking for provenance verification, making deletion authority forgeable (F1/F9) while producer-derived invariants simultaneously reject genuine rollback progress (F8); making the trust boundary explicit per artifact would have surfaced the entire family.",
          "AC7's whole-case no-symlink guarantee was implemented as a library primitive (_scan_case_tree) plus per-entry-point discipline instead of a structural chokepoint on the public write API; create_case/resume_case/phase_event remember scan-unresolved-before-resolve-or-write, fork_case/run_attempts/log_event forgot it — four blockers are lapses of one architectural guarantee, not four independent bugs.",
          "The cycle-2 seams were authored to demonstrate planned behavior rather than discriminate contract from accident: fixtures synthesize states by editing fields instead of replaying producer sequences (KeyboardInterrupt through a handler that persists evidence; a hand-built applied-last journal contradicting ordered _apply; rmtree of a whole directory that an existence-only implementation would also fail), so the suite stays green while the genuinely reachable double-interruption state deterministically bricks recovery — the seams concealed F8 rather than merely missing it.",
          "_gate_input validates members syntactically (string, nonempty, separators, duplicates) and copies the blankness discipline onto the actor alone, omitting member.strip(), so whitespace IDs satisfy the completeness predicate and the human turn boundary silently stops pausing (F3)."
        ],
        "solutionStrategies": [
          "causal-subgraph-reconstruction",
          "confined-repair",
          "test-contract-repair",
          "refactor"
        ],
        "rejectedAlternatives": [
          "confined-repair",
          "test-contract-repair",
          "refactor"
        ],
        "selectedStrategy": "causal-subgraph-reconstruction",
        "decisiveEvidence": [
          "refute-final-1..11.json: all eleven verdicts are \"survives\" (cycle 2, sources review/review2/audit) against gate commit 429c34deae1b9f5a92252147ec16acebfcb4812b recorded clean+green in task.json — deterministic tallies give every finding blockingVotes 3/3 and the union is exactly F1–F11.",
          "src/anomaly/_attempt_workspace.py:244-264 — asymmetric coherence: non-pending original=false entries pass iff the backup is absent with NO live-absence requirement, while applied originals require backup.exists() or (applying and live.exists()); this single pair admits the F1/F9 forgery and permanently rejects F8's genuine partial-rollback shape.",
          "Impossibility check across inquiries: the forged applied/original=false/no-backup/live-present state is extensionally identical to the legitimate outcome of promoting a brand-new artifact — README.md is genuinely first created at P7 — so the security lens's own proposed confined rule ((not original) implies not live.exists()) would also reject that genuine state; separation demands provenance or fail-closed possession rules, which is the reconstruction boundary.",
          "src/anomaly/_attempt_workspace.py:109-136 — _rollback restores each non-pending original via backup.replace(live) with no journal write inside the loop and persists rolled_back only after the loop, so interruption mid-loop bricks every later public restart (F8); any F1/F9 fix must define its accepted-state set inside the same transition model that adds journaled restore progress, or the forgery surface reopens.",
          "Reachability and irreversibility: src/anomaly/workflow.py:163-168 — public run_workflow resumes any regular-file portable case and calls recover_interrupted_promotion before loading state; registry tuples (workflow.py:22-83) bind data/sources.json (P1) and README.md (P7), so forged journals pass exact write-set equality; src/anomaly/readme.py:24-30 returns when README.md is absent, so rollback-deleted journalist content is never recreated.",
          "Containment precedent is in-file, bounding the repair: case.py:104-107 create_case scans the unresolved root before resolve/write versus fork_case:166-181 resolving dest first and copytree-ing through it (F2/F7); attempts.py:35-40,82-86 run_attempts durably writes via load_snapshot before its only scan, which targets the copied workspace, while skills/anomaly/SKILL.md documents run_attempts publicly (F10); events.py:39-43 log_event mkdirs and append-opens unscanned inside a broad never-raise try that admits mapping UnsafeCasePathError to None (F11).",
          "src/anomaly/workflow_inputs.py:150-176 — _gate_input applies actor.strip() but only emptiness/type/separator checks to sequence members, so ['   '] is capability-complete; recommend.py:335-379 makes Gate A fail inside a consumed attempt and review.py:366-474 lets Gate B seal an empty accepted set and complete P7 (F3), violating the locked pauses-consume-no-attempts contract.",
          "Seam vacuity is structural, not incidental: attempts.py:88-110 — both BaseException handlers persist failure evidence before re-raising, so tests/test_pipeline_walk.py:605-641 can never reach _reconcile_interrupted_attempt's missing-evidence branch (F4); _apply:82-104 advances strictly in registry order persisting each entry applied, making the fixture's pending-prefix/applied-last journal at :644-694 unreachable (F5); P7_OUTPUT_MUTATIONS:196-215 only rmtrees the charts directory while identities.py:78-105 hashes descendants by relative path/kind/bytes, so no row discriminates content identity from existence (F6).",
          "Rejected alternative rationale: test-contract-repair as the selection — honest seams alone leave F1/F2/F8/F9/F10/F11 unfixed in production; unverifiable guarantees over a forgeable destructive capability do not close the union.",
          "Rejected alternative rationale: Pure confined-repair of the trust family — unsound per the impossibility argument: any journal+tree-local predicate either rejects the legitimate fresh-artifact promotion shape (breaking ordinary P7-interruption recovery) or accepts the forged one; provenance/fail-closed possession rules are unattainable by patching the existing predicate in place while rollback progress journalization changes the very shapes validation must classify.",
          "Rejected alternative rationale: full-replan — unwarranted blast radius: the containment primitive, registry write sets, gate tables, and attempt mechanics are individually sound; the defect is one under-modeled recovery subgraph plus lapsed call-site discipline, not a broken architecture, and the mandate forbids new runner/framework work.",
          "Rejected alternative rationale: operator-escalation — no prerequisite is missing: all eleven refutes returned grounded line-level evidence, the three inquiries are validated, and the remedy site is fully identified."
        ]
      },
      "verdict": "block",
      "outcome": "blocked-to-operator",
      "synthesizer_agent_id": "SynthesizeAnomalyCouncil-2",
      "researchProvenance": "canonical",
      "cycle": 2
    },
    "recovery": {
      "episode": 1,
      "route": "causal-subgraph-reconstruction",
      "baselineGate": {
        "hash": "429c34deae1b9f5a92252147ec16acebfcb4812b",
        "clean": true,
        "green": true,
        "command": "uv run --extra test pytest tests/",
        "at": "2026-08-24T12:14:57Z"
      },
      "test_author_agent_id": "PlanAnomalyCouncilRecoverySol",
      "builder_agent_id": "ImplementAnomalyCouncilRecovery-2",
      "original": {
        "complexity": "complex",
        "audit_required": true,
        "absentLineage": [],
        "plan": {
          "result": "red",
          "slices": [
            "Complete source/Gate A/Gate B value validation before capability creation and scan unresolved create_case paths before any write.",
            "Reconcile unfinished attempts into bounded durable evidence; preserve valid promotion rollback/restart; reject promotion journals whose path set, status, or original metadata is not registry-bound and valid.",
            "Bind every authoritative P7 product and receipt to completion identity/README truth and redact compound environment credential assignments through the shared sanitizer.",
            "Delete stale runner/verb/event-resume/MAX_ATTEMPTS skill prose and its obsolete test assertion while preserving all prior orchestration deletions and existing demo/order proofs."
          ],
          "testFiles": [
            "tests/test_pipeline_walk.py",
            "tests/test_case.py",
            "tests/test_skill.py"
          ],
          "redRun": {
            "command": "uv run --extra test pytest tests/test_pipeline_walk.py::test_value_invalid_source_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_value_invalid_gate_a_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_value_invalid_gate_b_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_public_dispatcher_persists_three_failed_attempts_and_blocks tests/test_pipeline_walk.py::test_interrupted_attempts_recover_with_evidence_and_stop_at_the_limit tests/test_pipeline_walk.py::test_public_restart_rolls_back_an_interrupted_promotion_before_retry tests/test_pipeline_walk.py::test_public_restart_rejects_untrusted_promotion_journal_without_case_mutation tests/test_pipeline_walk.py::test_failed_reasoning_attempts_redact_credentials_from_all_durable_evidence tests/test_pipeline_walk.py::test_public_dispatcher_invalidates_changed_gate_b_from_p7 tests/test_pipeline_walk.py::test_changed_or_missing_p7_output_demotes_completion_and_readme tests/test_case.py::test_create_case_rejects_a_symlink_before_writing tests/test_skill.py::test_skill_contracts_durable_state_bounded_retries_and_portable_paths",
            "output": "30 failed, 4 passed in 3.71s (exit 1)"
          },
          "escalation": null,
          "refactorOpportunity": "Delete the obsolete skill Verbs and runner/event-resume contract instead of replacing it; derive promotion-journal validation from the existing _OWNER_REGISTRY write tuples rather than adding a second phase map; reuse complete source/gate predicates at capability and owner boundaries and remove superseded shape-only helpers after migration."
        },
        "test_author_agent_id": "PlanAnomalySecondRepair",
        "builder_agent_id": "ImplementAnomalySecondRepair",
        "implement": {
          "agent_id": "ImplementAnomalySecondRepair",
          "result": "green",
          "files": [
            "skills/anomaly/SKILL.md",
            "src/anomaly/_attempt_workspace.py",
            "src/anomaly/attempts.py",
            "src/anomaly/case.py",
            "src/anomaly/identities.py",
            "src/anomaly/owners.py",
            "src/anomaly/semantics.py",
            "src/anomaly/workflow.py",
            "src/anomaly/workflow_inputs.py"
          ],
          "greenRun": {
            "command": "uv run --extra test pytest tests/test_pipeline_walk.py::test_value_invalid_source_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_value_invalid_gate_a_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_value_invalid_gate_b_input_pauses_before_an_attempt tests/test_pipeline_walk.py::test_public_dispatcher_persists_three_failed_attempts_and_blocks tests/test_pipeline_walk.py::test_interrupted_attempts_recover_with_evidence_and_stop_at_the_limit tests/test_pipeline_walk.py::test_public_restart_rolls_back_an_interrupted_promotion_before_retry tests/test_pipeline_walk.py::test_public_restart_rejects_untrusted_promotion_journal_without_case_mutation tests/test_pipeline_walk.py::test_failed_reasoning_attempts_redact_credentials_from_all_durable_evidence tests/test_pipeline_walk.py::test_public_dispatcher_invalidates_changed_gate_b_from_p7 tests/test_pipeline_walk.py::test_changed_or_missing_p7_output_demotes_completion_and_readme tests/test_case.py::test_create_case_rejects_a_symlink_before_writing tests/test_skill.py::test_skill_contracts_durable_state_bounded_retries_and_portable_paths",
            "output": "34 passed in 2.81s"
          }
        }
      }
    }
  },
  "judgmentHistory": [
    {
      "at": "2026-08-24T10:44:30Z",
      "review": {
        "verdict": "needs-work",
        "reportedVerdict": "needs-work",
        "reviewer_agent_id": "ReviewAnomalyCorrectnessSchema",
        "findings": [
          {
            "file": "src/anomaly/workflow.py",
            "line": 109,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Invocation-wide gate decisions can bypass both required human turn boundaries.",
            "why": "run_workflow normalizes every supplied input once and carries the same gate_a/gate_b keys through every freshly resolved phase. A decision supplied before its gate is reached therefore becomes immediately consumable after P3 or P6 instead of ending the turn at Gate A or Gate B; the resolver probe returned ready P4 and P7 owners for those carried keys.",
            "refute": {
              "agent_id": "AnRefute01",
              "source": "review",
              "finding": "src/anomaly/workflow.py:109 Invocation-wide gate decisions can bypass both required human turn boundaries.",
              "verdict": "survives",
              "rationale": "No upstream stage-aware guard exists: the public entry accepts Gate A and Gate B together, retains both decisions for the whole invocation, checks only key presence at P4/P7, and continues after successful P3/P6 seals, so both required paused returns are reachable to bypass.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:102-138",
                  "output": "run_workflow calls normalize_inputs once into supplied before entering while True; every iteration builds available from that same supplied mapping, pauses only when resolution.status == 'paused', otherwise executes the owner, and continues unless the phase becomes blocked or unavailable."
                },
                {
                  "command": "read src/anomaly/workflow.py:28-68 and src/anomaly/workflow_inputs.py:11-29",
                  "output": "_REQUIRED_INPUTS maps P4 to gate_a and P7 to gate_b; _missing_input returns no pause whenever the name is present. normalize_inputs accepts both names in _PUBLIC_INPUTS, copies the whole caller mapping, and has no phase-aware rejection or deferral guard."
                },
                {
                  "command": "read src/anomaly/owners.py:52-70,124-134 and src/anomaly/attempts.py:52-73",
                  "output": "The P4 handler reads inputs['gate_a'] and immediately approves/detects; P7 reads inputs['gate_b'] and immediately accepts/reports. A successful non-P7 phase is sealed with status 'active', so after P3 and P6 run_workflow loops directly to the next ready owner instead of returning a human-turn pause."
                },
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/task.md and task.json gate evidence",
                  "output": "The locked acceptance criterion requires Gate A/Gate B turn boundaries, and task state records the inspected candidate as clean and green at gated hash 4a1746f9050120df52e7845166f0ab935c4e584a."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 35,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The resolver treats presence-only, incomplete phase inputs as ready and can spend attempts before boundary validation.",
            "why": "_REQUIRED_INPUTS omits the timezone-aware now value required by P1-P4 and tests only whether gate_a/gate_b top-level names exist. Missing now or incomplete gate mappings consequently resolve ready and fail later inside an attempt, contradicting the complete missing-input/human-decision contract and the rule that human stops consume no attempt.",
            "refute": {
              "agent_id": "AnRefute02",
              "source": "review",
              "finding": "src/anomaly/workflow.py:35 The resolver treats presence-only, incomplete phase inputs as ready and can spend attempts before boundary validation.",
              "verdict": "survives",
              "rationale": "The public dispatcher passes only top-level input names to the resolver, so missing `now` and incomplete Gate A/B mappings resolve ready; the selected owners validate or index those values only inside `run_attempts`, whose broad exception path durably increments and retries through all three attempts rather than pausing without attempt consumption.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/workflow.py:32-138",
                  "output": "`_REQUIRED_INPUTS = {\"P1\": \"sources\", \"P4\": \"gate_a\", \"P7\": \"gate_b\"}`; `_missing_input` checks only membership in `supplied`; `run_workflow` derives `available` from top-level names, resolves, and sends every ready result directly to `run_attempts`."
                },
                {
                  "command": "read anomaly/src/anomaly/workflow_inputs.py:13-71",
                  "output": "`normalize_inputs` validates only mapping/key shape and unknown top-level names; `input_time` later raises unless `now` is a timezone-aware datetime; `mapping_input` accepts any subset of allowed Gate A/B fields and does not require the fields consumed by owners."
                },
                {
                  "command": "read anomaly/src/anomaly/owners.py:49-92,135-143",
                  "output": "P2 and P3 call `input_time(inputs)`; P4 reads `gate[\"approved_ids\"]` and `gate[\"approved_by\"]` after subset-only mapping validation, then calls `input_time`; P7 passes a subset-only Gate B mapping to `_accept_and_report`, which reads required fields. These checks occur in owner execution, after resolution."
                },
                {
                  "command": "read anomaly/src/anomaly/attempts.py:21-67",
                  "output": "`run_attempts` increments and persists the phase attempt before calling `execute`; `except Exception` records the failure and retries until `MAX_ATTEMPTS`, proving these ordinary missing/incomplete inputs consume attempts and can end unavailable."
                },
                {
                  "command": "read anomaly/.jeff/tasks/0027-resolver-consolidation/task.md; read anomaly/.jeff/tasks/0027-resolver-consolidation/notes.md",
                  "output": "The accepted contract requires the pure resolver to return missing input/human decision, and the plan states Gate A/Gate B pauses end the turn without consuming an attempt; the observed execution path contradicts that contract."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/owners.py",
            "line": 59,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The attempt utility does not provide atomic phase staging; deterministic owners write directly into the live case.",
            "why": "_run_handler discards _attempt_dir, and _register_sources promotes each request inside its loop. If a later source is invalid, the attempt fails and retries while earlier source bytes, receipts, and catalogue records remain live. The supplied gated-hash probe ended P1 unavailable after three attempts with the first source already present, violating AC3 atomic promotion.",
            "refute": {
              "agent_id": "AnRefute03",
              "source": "review",
              "finding": "src/anomaly/owners.py:59 The attempt utility does not provide atomic phase staging; deterministic owners write directly into the live case.",
              "verdict": "survives",
              "rationale": "The failure is reachable through the public P1 source-batch path: only request shape and duplicate IDs are prevalidated, each valid request is then persisted directly into the live case before the next request is validated, and run_attempts records failures without rolling those mutations back; the gated-hash probe confirms a later invalid source leaves the earlier source live after P1 becomes unavailable.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:101-139 and src/anomaly/attempts.py:21-105",
                  "output": "run_workflow passes the live case_root to consume_owner inside run_attempts; run_attempts creates attempt_dir but success/failure handling only writes attempt result/failure evidence and state, with no snapshot, rollback, or cleanup of owner mutations."
                },
                {
                  "command": "read src/anomaly/owners.py:53-103",
                  "output": "_run_handler names the staging argument _attempt_dir and does not use it. For P1, _register_sources loops over requests and calls register_local_source(root, ...) against the live root for each item."
                },
                {
                  "command": "read src/anomaly/workflow_inputs.py:42-57 and src/anomaly/acquire.py:25-126",
                  "output": "source_requests validates only list/mapping shape and required/unknown fields. Per-item path existence, extension, portability, and metadata validation happen later inside register_local_source, after earlier loop items may already have written raw bytes, receipts, and data/sources.json."
                },
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/review-correctness.json:115-145",
                  "output": "The supplied gated-hash public probe with a valid CSV followed by an unsupported .txt ended {\"attempts\":{\"P0\":1,\"P1\":3},\"phase\":\"P1\",\"status\":\"unavailable\",\"sources\":[{\"source_id\":\"good\",...}],\"raw\":[\"data/raw/good/good.csv\"]}, directly demonstrating reachability and durable partial promotion."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/readme.py",
            "line": 36,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "README projection deletes journalist-authored content that exactly matches the legacy Outputs block.",
            "why": "After removing the marked Anomaly-owned block, project_readme globally replaces every exact _OUTPUTS occurrence. An unmarked journalist-authored Outputs section is therefore erased; the pure replacement probe preserved only the later Notes section.",
            "refute": {
              "agent_id": "AnRefute04",
              "source": "review",
              "finding": "src/anomaly/readme.py:36 README projection deletes journalist-authored content that exactly matches the legacy Outputs block.",
              "verdict": "survives",
              "rationale": "A journalist-edited regular README can contain this exact unmarked section, and public completion, completed-case resume, or invalidation reaches the unconditional global replacement, silently deleting user-authored content in direct conflict with the preservation acceptance criterion.",
              "evidence": [
                {
                  "command": "read src/anomaly/readme.py:24-48",
                  "output": "project_readme accepts any existing non-symlink README, removes the marked Anomaly block, then unconditionally executes .replace(_OUTPUTS, \"\"); Python str.replace removes every exact occurrence, with no ownership or position guard."
                },
                {
                  "command": "read src/anomaly/workflow.py:101-139; src/anomaly/attempts.py:21-88; src/anomaly/state.py:34-52,133-171",
                  "output": "The public run_workflow entry repeatedly calls load_snapshot; completed P7 snapshots call project_readme, successful P7 sealing calls it, and invalidation from a previously completed P7 calls it again. The destructive branch is therefore reachable during ordinary completion, restart, and invalidation flows."
                },
                {
                  "command": "read src/anomaly/case.py:196-224,237-251",
                  "output": "resume_case/_load_case validates case.json and progress but imposes no README-content restriction; the initial README is ordinary journalist-facing Markdown. No upstream guard makes an exact unmarked Outputs section impossible."
                },
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/task.md; grep gated hash and finding evidence in task state/review return",
                  "output": "The locked acceptance criterion explicitly requires README projection to preserve journalist-authored content; gate state binds the inspected candidate to 4a1746f9050120df52e7845166f0ab935c4e584a, and the supplied pure replacement probe reports journalist_outputs_preserved=False with only the later Notes section remaining."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/case.py",
            "line": 146,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Public resume, inspect, and direct writer paths do not enforce the required whole-case no-symlink scan.",
            "why": "resume_case and inspect_case load selected files without _scan_case_tree, and public domain writers such as register_local_source rely on resume_case. A nested in-case symlink is accepted; in source registration, a destination symlink resolving to another in-case artifact can be followed and overwritten. Only run_workflow and fork_case currently scan the whole tree.",
            "refute": {
              "agent_id": "AnRefute05",
              "source": "review",
              "finding": "src/anomaly/case.py:146 Public resume, inspect, and direct writer paths do not enforce the required whole-case no-symlink scan.",
              "verdict": "survives",
              "rationale": "The finding is reachable and honestly high severity: direct resume/inspect calls never scan, while register_local_source calls the unguarded resume path, resolves a destination symlink to an allowed in-case target, and then writes through that resolved path, permitting overwrite of another case artifact; run_workflow's upstream scan does not protect direct public calls.",
              "evidence": [
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/case.py (lines 139-165)",
                  "output": "139: def inspect_case(root: Path) -> ExistingCaseOffer | None:\n140:     root = Path(root)\n141:     if not _case_exists(root):\n142:         return None\n143:     return _offer(root)\n146: def resume_case(root: Path) -> Case:\n147:     root = Path(root)\n148:     if not _case_exists(root):\n149:         raise CaseNotFoundError(str(root))\n150:     return _load_case(root)\n161:     source = Path(source)\n162:     dest = Path(dest).resolve()\n163:     _scan_case_tree(source)\n165:     parent = resume_case(source)"
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/acquire.py (lines 30-46, 71-75, 125-130, 158-163)",
                  "output": "30: @phase_event(\"P1\", \"register_local_source\")\n31: def register_local_source(...):\n45:     root = Path(root).resolve()\n46:     resume_case(root)\n71:     relative = _posix(\"data/raw\", persisted_source_id, source_path.name)\n72:     dest = _under_root(root, relative)\n75:     payload = source_path.read_bytes() if source_path.is_file() else None\n125:     if existing_index is None:\n126:         if included:\n128:             dest.parent.mkdir(parents=True, exist_ok=True)\n129:             dest.write_bytes(payload)\n158: def _under_root(root: Path, relative: str) -> Path:\n159:     base = Path(root).resolve()\n160:     candidate = (base / relative).resolve()\n161:     if candidate != base and base not in candidate.parents:\n162:         raise UnsafeCasePathError(relative)\n163:     return candidate"
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 102-113)",
                  "output": "102: def run_workflow(...):\n110:     case_root = Path(root)\n111:     _scan_case_tree(case_root)\n112:     case_root = case_root.resolve()\n113:     resume_case(case_root)"
                },
                {
                  "command": "read src/anomaly/events.py:50-68",
                  "output": "phase_event wraps the function with try/except, calls the wrapped function directly, then performs best-effort log_event; it contains no case-tree scan or path guard."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 119,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The multi-phase loop does not recheck whole-case containment after dynamic reasoning owners mutate the case.",
            "why": "run_workflow scans once before entering the loop. P5/P6 invoke external adapters that write case artifacts, then the next iteration loads state and executes another owner without rescanning. A symlink introduced by an invoked owner can therefore reach later public reads or writes in the same call, contrary to AC7's every-path preflight.",
            "refute": {
              "agent_id": "AnRefute06",
              "source": "review",
              "finding": "src/anomaly/workflow.py:119 The multi-phase loop does not recheck whole-case containment after dynamic reasoning owners mutate the case.",
              "verdict": "survives",
              "rationale": "The finding is reachable: run_workflow performs its only whole-tree scan before the loop, grants the external reasoning invoker the case root, and after that invoker returns run_attempts writes through the previously created attempt directory with no rescan or symlink-parent guard, so an owner that replaces that directory with a symlink can make the same public call write result.json outside the case.",
              "evidence": [
                {
                  "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 101-140)",
                  "output": "111: _scan_case_tree(case_root); 119: while True; 128-138: run_attempts(... lambda attempt_dir: consume_owner(...)); there is no _scan_case_tree call in the loop before or after owner execution."
                },
                {
                  "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 89-99); git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/owners.py (lines 37-56)",
                  "output": "workflow.py:99 passes case_root=Path(case_root) to the external invoke callable; owners.py:49-55 calls that reasoning owner for P5 and accepts its return after only checking that findings/draft.json is_file()."
                },
                {
                  "command": "git -C /Users/tomvaillant/buried_signals/tools/anomaly show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py (lines 41-48,122-124); git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py (lines 102-105)",
                  "output": "attempts.py creates attempt_dir before invocation, then executes the owner and calls _write_output(attempt_dir, output); _write_output calls write_json_atomic(attempt_dir / \"result.json\", payload). state.py implements that helper as temporary.write_text(...) followed by temporary.replace(path), with no containment rescan or symlink-parent rejection."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/owners.py",
            "line": 122,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Canonical-equivalent Unicode identities bypass reviewer independence at both gates.",
            "why": "The Gate A/reviewer and reviewer/Gate B checks compare casefold values only. Composed 'Réviewer' and decomposed 'Réviewer' compare unequal by casefold but equal under the project's canonical_key, so one logical identity can occupy separated roles.",
            "refute": {
              "agent_id": "AnRefute07",
              "source": "review",
              "finding": "src/anomaly/owners.py:122 Canonical-equivalent Unicode identities bypass reviewer independence at both gates.",
              "verdict": "survives",
              "rationale": "The public workflow accepts nonempty identity strings without Unicode normalization, both role-separation checks compare only casefolded text, and the project’s canonical identity function additionally normalizes to NFC, so canonically equivalent composed and decomposed identities can reach and pass both gates.",
              "evidence": [
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:34-38,101-109,266-287",
                  "output": "The task binds the reviewed candidate to gated hash 4a1746f9050120df52e7845166f0ab935c4e584a and records that reviewed product source matches that gate; the supplied review finding is at src/anomaly/owners.py:122."
                },
                {
                  "command": "read src/anomaly/owners.py:116-150",
                  "output": "Gate A/reviewer separation is `reviewer_id.casefold() == gate_a_approver.casefold()` and reviewer/Gate B separation is `journalist_id.casefold() == reviewer_id.casefold()`; `_required_identity` only requires a nonempty string and returns `value.strip()`."
                },
                {
                  "command": "read src/anomaly/semantics.py:63-79",
                  "output": "`canonical_key` returns `unicodedata.normalize(\"NFC\", unicodedata.normalize(\"NFC\", value).casefold())`, establishing NFC normalization as part of the project’s canonical identity policy."
                },
                {
                  "command": "read src/anomaly/workflow.py:101-139; src/anomaly/workflow_inputs.py:10-65; src/anomaly/recommend.py:335-379; src/anomaly/review.py:249-335",
                  "output": "`run_workflow` forwards caller gate inputs and dynamic reviewer output into owners; gate mappings restrict field names but not Unicode form, Gate A accepts and persists any nonempty `approved_by`, and review recording only strips `reviewer_id` while its attestation check compares against that same unnormalized value. No upstream NFC guard makes the bypass unreachable."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/state.py",
            "line": 39,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "A completed snapshot with missing authoritative identities is trusted as P7-complete.",
            "why": "changed_identities iterates only identity keys already recorded; a missing or malformed identities map becomes an empty mapping and yields no invalidation. A sequential completed map through P7 is then accepted without checking source, detector, gate, draft, replay, review, or Gate B artifacts; the pure probe reported completed P7 with identity_change None.",
            "refute": {
              "agent_id": "AnRefute08",
              "source": "review",
              "finding": "src/anomaly/state.py:39 A completed snapshot with missing authoritative identities is trusted as P7-complete.",
              "verdict": "survives",
              "rationale": "Normal sealing captures identities, but the public portable-case entry accepts any regular state.json; load_snapshot converts a missing or non-mapping identities value to {}, changed_identities cannot notice omitted required keys, and resolve_workflow then treats a sequential completed map through P7 as terminal and even projects README completion without validating the authoritative artifacts.",
              "evidence": [
                {
                  "command": "read src/anomaly/attempts.py:70-90; src/anomaly/state.py:34-49; src/anomaly/identities.py:22-31",
                  "output": "_seal_success normally calls capture_identities before write_state, but load_snapshot passes {} when identities is absent or malformed, and changed_identities compares only recorded.items(); it has no required-key check, so omitted identities produce no invalidation."
                },
                {
                  "command": "read src/anomaly/workflow.py:37-49,106-128; src/anomaly/case.py:146-150,255-301",
                  "output": "run_workflow is a real public entry: its case-tree guard rejects links/special files but accepts regular files, resume_case validates the case record rather than state provenance, then load_snapshot feeds resolve_workflow; resolve_workflow returns complete solely when completed_phase is P7."
                },
                {
                  "command": "read src/anomaly/state.py:22-49; src/anomaly/readme.py:24-58",
                  "output": "completed_phase accepts any sequential P0-P7 key mapping; load_snapshot calls project_readme when it sees P7, and project_readme marks Status complete and adds accepted-findings/report links without checking that those artifacts or their identities exist."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 263,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The retained retry tests no longer verify credential-redacted durable failure evidence.",
            "why": "The current test asserts three attempts and relative attempt paths but injects no credential and inspects neither failure.json, event error fields, state failures, nor blocked_reason. The deleted WorkflowRunner test was the only redaction assertion. AC3's durable failure evidence is consumer-observable, so the plan's ambiguous reuse/delete claim is dominated by reuse but is not valid: a focused public replacement test is owed.",
            "refute": {
              "agent_id": "AnRefute09",
              "source": "review",
              "finding": "tests/test_pipeline_walk.py:263 The retained retry tests no longer verify credential-redacted durable failure evidence.",
              "verdict": "survives",
              "rationale": "The public retry path can persist one sanitized exception message into failure.json, event error fields, state failures, and blocked_reason, but the gated retry test supplies no credential and asserts none of those redaction outcomes, while no focused retry redaction coverage exists in the retained orchestration tests.",
              "evidence": [
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py [lines 263-288]",
                  "output": "The test sets source_id to \"../unsafe\", runs run_workflow, and asserts only blocked/unavailable status, three P1 attempts, relative attempt paths, and attempt-directory existence; it does not inject a credential or inspect persisted error text."
                },
                {
                  "command": "git grep -n -E 'sk_live_|ghp_|github_pat_|failure\\.json|phase_failed' 4a1746f9050120df52e7845166f0ab935c4e584a -- tests/test_pipeline_walk.py tests/test_workflow.py",
                  "output": "Exit 1 with no matches."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py [lines 91-108]",
                  "output": "_record_failure computes message = safe_error(error), writes it to attempt failure.json, appends it as the phase_failed event error, stores it in state failures, and on the third attempt assigns the same message to blocked_reason."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py [lines 120-125]",
                  "output": "safe_error returns _safe_text(value)[:2000], and _safe_text applies credential redaction; this shows the intended fail-safe exists in production but does not supply the missing public durable-evidence regression assertion."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 349,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "Deleting the invalidation matrix left most authoritative identity classes without earliest-suffix coverage.",
            "why": "Current public tests cover source/P1, prepared/P2, and Gate B/P7, but production still maps recommendation/P3, Gate A and detector/P4, draft/P5, and replay/review/P6. Deleting their behavioral assertions without deleting those behaviors violates the remove rule; focused state/resolution tests must replace the synthetic runner composition.",
            "refute": {
              "agent_id": "AnRefute10",
              "source": "review",
              "finding": "tests/test_pipeline_walk.py:349 Deleting the invalidation matrix left most authoritative identity classes without earliest-suffix coverage.",
              "verdict": "survives",
              "rationale": "Production directly trusts the unguarded identity-to-phase table to choose and destructively clear the invalidation suffix, while the candidate deleted the only P3–P6 behavioral matrix and retained public tests exercise only source/P1, prepared/P2, and Gate B/P7, so no upstream guard or equivalent coverage makes the blocker unreachable or less severe.",
              "evidence": [
                {
                  "command": "git rev-parse HEAD",
                  "output": "4a1746f9050120df52e7845166f0ab935c4e584a"
                },
                {
                  "command": "read src/anomaly/identities.py:9-30",
                  "output": "IDENTITY_PHASES maps recommendation→P3, gate_a/detector→P4, draft→P5, replay/review→P6; changed_identities compares every recorded digest and returns the minimum mapped phase without any independent validation or fallback."
                },
                {
                  "command": "read src/anomaly/state.py:34-45,124-162",
                  "output": "load_snapshot passes changed_identities' start directly to _invalidate_state; _invalidate_state removes every completed phase from that start onward, clears attempts/failures and identities for the same suffix, and persists the result."
                },
                {
                  "command": "git diff bd3d49e 61a2b7c -- tests/test_pipeline_walk.py",
                  "output": "The task-27 cutover deletes MUTATIONS entries for source/P1, prepared/P2, detector/P4, parameters/P3, draft/P5, replay/P6, review/P6, Gate A/P4, and Gate B/P7, and deletes test_fresh_session_invalidates_changed_authoritative_inputs_from_earliest_phase, which asserted invalidated_from, retained prefix, cleared suffix, and suffix rerun."
                },
                {
                  "command": "grep 'invalidat|rebuilds_stale|replaces_changed' tests",
                  "output": "Current workflow-dispatch coverage is tests/test_pipeline_walk.py:349 prepared mutation, :371 source replacement, and :438 Gate-B mutation; tests/test_m5_handoff.py detector-change cases exercise replay/acceptance rejection rather than load_snapshot's earliest workflow suffix, and no tests reference changed_identities, capture_identities, artifact_identity, or IDENTITY_PHASES."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 185,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "No public test proves that future Gate A or Gate B decisions cannot cross a turn.",
            "why": "The demo deliberately supplies each human decision only on a later invocation, so it cannot fail when run_workflow incorrectly carries a pre-supplied future decision through the resolver loop. The exact gate-turn behavior is consumer-observable and explicitly required by AC4.",
            "refute": {
              "agent_id": "AnRefute11",
              "source": "review",
              "finding": "tests/test_pipeline_walk.py:185 No public test proves that future Gate A or Gate B decisions cannot cross a turn.",
              "verdict": "survives",
              "rationale": "At the gated hash, run_workflow normalizes inputs once and reuses the same supplied mapping on every resolver iteration, with no guard that rejects or consumes a future gate decision, while the cited demo supplies Gate A and Gate B only in separate later invocations; the consumer-observable AC4 turn boundary is therefore reachable and unproved.",
              "evidence": [
                {
                  "command": "git rev-parse HEAD",
                  "output": "4a1746f9050120df52e7845166f0ab935c4e584a"
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py",
                  "output": "109: supplied = normalize_inputs(inputs)\n119: while True:\n120:     snapshot = load_snapshot(case_root)\n122:     available = frozenset({*supplied, *({\"sources\"} if has_sources else set())})\n123:     resolution = resolve_workflow(snapshot, supplied=available)\n126:     if resolution[\"status\"] == \"paused\":\n127:         return pause_at(case_root, snapshot, resolution[\"phase\"], resolution[\"missing\"])\n131-137: consume_owner receives the unchanged resolution and supplied mapping; the loop then resolves again without clearing gate keys."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py",
                  "output": "101: the first invocation supplies only source inputs and asserts pause at gate_a; 118-130: the next invocation supplies gate_a and asserts pause at gate_b; 142-151: a third invocation supplies gate_b and asserts completion. No invocation pre-supplies a future gate decision."
                },
                {
                  "command": "grep pattern 'gate_a|gate_b' path tests",
                  "output": "Public-dispatcher gate inputs occur in tests/test_pipeline_walk.py at gate_a lines 122, 334, 402 and gate_b lines 145, 425, 470, 496; the canonical demo path at lines 92-151 supplies each gate only after the prior invocation has paused."
                },
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/task.md",
                  "output": "Acceptance criterion 4: \"Gate A/Gate B turn boundaries, identity separation, source replacement, earliest invalidation, event-failure resume, case portability and exact P5→P6→Gate B→P7 order remain green.\""
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 438,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "README preservation coverage omits completed restart and failed recomputation transitions.",
            "why": "The journalist notes fixture is added immediately before Gate B invalidation and covers invalidation plus recompletion, while the chart-failure test contains no journalist-authored content. The plan explicitly claims preservation across completed restart and failure too, and those observable lifecycle cases remain unproved.",
            "refute": {
              "agent_id": "AnRefute12",
              "source": "review",
              "finding": "tests/test_pipeline_walk.py:438 README preservation coverage omits completed restart and failed recomputation transitions.",
              "verdict": "survives",
              "rationale": "Both transitions are reachable production branches that invoke README projection, but the gated tests do not exercise either branch with journalist-authored content despite the plan explicitly requiring preservation across completed restart and failure.",
              "evidence": [
                {
                  "command": "grep -n \"survive completed restart\" .jeff/tasks/0027-resolver-consolidation/notes.md",
                  "output": "47|5. **README lifecycle and links — `revise`.** Consumer behavior: public P7 completion projects complete/P7 and three resolving relative links; later journalist-authored README sections survive completed restart, Gate B invalidation, failure, and recompletion. Seams: revised public demo and Gate B invalidation tests."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py (lines 185-219, 438-508)",
                  "output": "The completed-restart test calls resumed = run_workflow(root) and asserts only resumed == first_state; it never adds or checks journalist content. Journalist notes are first added at lines 446-452 inside the Gate B invalidation test and are checked through invalidation/recompletion. The chart-failure test at lines 481-508 adds no journalist content and asserts only unavailable/P7/three-attempt state plus removal of complete/P7 README claims."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py; git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py (targeted lines)",
                  "output": "state.py:49-51 calls project_readme whenever completed_phase(state) == \"P7\", so completed restart reaches projection. attempts.py:48-60 records each failure and calls project_readme after the third P7 failure, so failed recomputation also reaches projection. No upstream guard makes either lifecycle transition impossible."
                }
              ]
            }
          },
          {
            "file": "tests/test_review.py",
            "line": 425,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The Markdown inertness test does not contain a standalone bare URL.",
            "why": "Both URL occurrences are embedded inside Markdown-link and HTML-image syntax. The assertion that the raw URL substring disappears therefore does not exercise AC6's specifically named bare HTTP(S) autolink boundary; add prose containing a standalone URL and assert it remains visible but non-linking.",
            "refute": {
              "agent_id": "AnRefute13",
              "source": "review",
              "finding": "tests/test_review.py:425 The Markdown inertness test does not contain a standalone bare URL.",
              "verdict": "survives",
              "rationale": "AC6 explicitly requires bare HTTP(S) autolink coverage, but the only tested URLs are structurally embedded and arbitrary dataset statements can reach report serialization, so no upstream guard makes a standalone URL unreachable; the current generic escaping regex is fail-safe today but does not replace the missing behavioral regression proof.",
              "evidence": [
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/task.md; grep 4a1746f9050120df52e7845166f0ab935c4e584a .jeff/tasks/0027-resolver-consolidation/task.json",
                  "output": "The locked acceptance criterion says \"Dataset-derived Markdown is inert, including bare URL autolinks.\" The task gate records clean=true and green=true at 4a1746f9050120df52e7845166f0ab935c4e584a."
                },
                {
                  "command": "grep -n 'https?://' tests/test_review.py",
                  "output": "The only input occurrences are line 432, \"Acme [click](https://example.invalid/pixel)\", and line 433, \"<img src=https://example.invalid/pixel>\"; lines 450-453 only assert those constructs and the shared raw substring are absent. There is no standalone prose URL anywhere in the file."
                },
                {
                  "command": "read src/anomaly/review.py:170-235,360-465,609-703",
                  "output": "draft_findings copies signal['statement'] without a URL-shape restriction, accept_findings promotes that statement, and write_report passes it to _markdown_text. Thus a standalone dataset URL is reachable from the same public path exercised by the test."
                },
                {
                  "command": "read src/anomaly/review.py:647-656",
                  "output": "_markdown_text currently neutralizes every word-boundary HTTP(S) scheme with re.sub(r'(?i)\\bhttps?://', ...) after generic Markdown escaping. This makes the present implementation fail safe, but it is not an upstream impossibility guard and the existing embedded-only fixture would also pass an implementation that removed link/image constructs while leaving standalone autolinks active."
                }
              ]
            }
          }
        ],
        "evidence": [
          {
            "command": "git rev-parse HEAD; git status --short",
            "output": "HEAD is gated hash 4a1746f9050120df52e7845166f0ab935c4e584a. Only .jeff task recording files are dirty/untracked; reviewed product and test source matches the gate."
          },
          {
            "command": "read task.md, notes.md, context.md, task.json, candidate production files, tests, and bundled code-standards/testing skills",
            "output": "Re-derived all nine AC dispositions from the locked criteria and current source. AC3's invalid multi-claim plan disposition was normalized to dominant claimed=reuse; the conflicting deletion and resulting test loss are reflected by ok=false and findings."
          },
          {
            "command": ".venv/bin/python -c '<pure resolver boundary probes>'",
            "output": "future_gate_a resolved P4 ready/approve-and-detect; future_gate_b resolved P7 ready/accept-and-report; sources_without_now resolved P1 ready/register-sources."
          },
          {
            "command": ".venv/bin/python -c '<canonical identity comparison>'",
            "output": "casefold_equal=False; canonical_equal=True for composed/decomposed Réviewer."
          },
          {
            "command": ".venv/bin/python -c '<README legacy-block replacement probe>'",
            "output": "journalist_outputs_preserved=False; only the later Notes content remained."
          },
          {
            "command": ".venv/bin/python -c '<completed state with empty identity map probe>'",
            "output": "completed='P7'; identity_change=None."
          },
          {
            "command": "git diff --unified=5 9854273 61a2b7c -- tests/test_pipeline_walk.py tests/test_workflow.py",
            "output": "Confirmed removal of the nine-class authoritative-invalidation matrix and the only credential-redaction retry assertion while the corresponding production behaviors remain."
          },
          {
            "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:20-50",
            "output": "Task-bound gate records clean=true, green=true, command 'uv run --extra test pytest tests/' at 4a1746f9050120df52e7845166f0ab935c4e584a. The green suite omits the blocking cases above."
          }
        ],
        "acLedger": [
          {
            "ac": "AC1",
            "claimed": "write",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC2",
            "claimed": "write",
            "rederived": "write",
            "ok": true
          },
          {
            "ac": "AC3",
            "claimed": "reuse",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC4",
            "claimed": "reuse",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC5",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC6",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC7",
            "claimed": "reuse",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC8",
            "claimed": "delete",
            "rederived": "delete",
            "ok": true
          },
          {
            "ac": "AC9",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          }
        ]
      },
      "review2": {
        "verdict": "needs-work",
        "reportedVerdict": "needs-work",
        "reviewer_agent_id": "ReviewAnomalyStandardsSchema",
        "findings": [
          {
            "file": "src/anomaly/owners.py",
            "line": 59,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Phase attempts do not stage or atomically promote owner outputs.",
            "why": "AC3 and the installed method require outputs to be written under the attempt directory, validated, then promoted. Deterministic owners discard the attempt directory, dynamic owners receive only the live case root, and P5 requires a live findings/draft.json before the attempt result is recorded. Failures can therefore leave partially promoted case state instead of an auditable atomic attempt.",
            "refute": {
              "agent_id": "AnRefute14",
              "source": "review2",
              "finding": "src/anomaly/owners.py:59 Phase attempts do not stage or atomically promote owner outputs.",
              "verdict": "survives",
              "rationale": "The public dispatcher reaches this path with normal phase inputs, but deterministic and dynamic owners write against the live case root while the attempt utility records only a post-hoc result and has no validation, promotion, or rollback of owner artifacts, so a later owner failure can leave partially committed case state and the high-severity AC3 violation is reachable.",
              "evidence": [
                {
                  "command": "read skills/anomaly/SKILL.md:49-98",
                  "output": "The installed operating contract requires every phase's outputs to be written under .anomaly/attempts/<phase>/attempt-<n>/ first, validated for hashes, schemas, safety, and relative paths, and only then moved into case folders; superseded attempts must remain auditable."
                },
                {
                  "command": "read src/anomaly/workflow.py:101-145 and src/anomaly/owners.py:30-75",
                  "output": "run_workflow passes both case_root and attempt_dir into consume_owner. Handler dispatch then calls _run_handler, whose attempt parameter is named _attempt_dir and unused while every deterministic operation receives root. Dynamic dispatch invokes the reasoning owner with only case_root, and P5 rejects the result unless live root/findings/draft.json already exists."
                },
                {
                  "command": "read src/anomaly/attempts.py:18-114",
                  "output": "run_attempts calls execute(attempt_dir) before writing only result.json into the attempt directory. On exception it records failure.json and state evidence but performs no owner-output rollback; on success _seal_success only updates identities/state and performs no artifact validation or atomic promotion."
                },
                {
                  "command": "read src/anomaly/owners.py:67-76,135-144 and src/anomaly/detect.py:583-744",
                  "output": "Reachable deterministic phases perform compound live mutations: P4 seals Gate A and then executes detectors, while detector execution creates live run directories, snapshots, provenance, and appends signals inside a per-detector loop before later operations can raise; P7 likewise accepts findings, writes the report, and only then generates charts. No phase-level transaction protects these sequences."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 29,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The resolver's missing-input boundary is schema-blind and omits required timestamps.",
            "why": "P1-P4 owners require now, but the resolver does not treat it as missing. Presence of gate_a or gate_b alone also counts as supplied even when required decision fields are absent. These reachable inputs resolve ready and consume three attempts through late ValueError or KeyError failures rather than returning truthful missing-input or human-decision state, breaking AC1, AC4, and explicit boundary-error standards.",
            "refute": {
              "agent_id": "AnRefute15",
              "source": "review2",
              "finding": "src/anomaly/workflow.py:29 The resolver's missing-input boundary is schema-blind and omits required timestamps.",
              "verdict": "survives",
              "rationale": "No upstream guard makes these inputs unreachable: the public dispatcher reduces inputs to their top-level names, the resolver therefore marks missing `now` or structurally incomplete gate mappings ready, and owner-side validation failures are caught only after attempt accounting and retried until the phase becomes unavailable.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:29-62,101-138 and src/anomaly/workflow_inputs.py:15-64",
                  "output": "`_REQUIRED_INPUTS` contains only P1 `sources`, P4 `gate_a`, and P7 `gate_b`; `run_workflow` passes the keys of normalized inputs to `resolve_workflow`, while `normalize_inputs` validates only top-level names and does not validate values or require `now`, so these malformed or incomplete public inputs reach a ready resolution."
                },
                {
                  "command": "read src/anomaly/owners.py:37-101,136-144",
                  "output": "P1 with supplied sources and P2-P4 call `input_time(inputs)`, which raises when `now` is absent; P4 indexes `gate[\"approved_ids\"]` and `gate[\"approved_by\"]`, and P7 indexes `gate[\"accepted_claim_ids\"]`, so present-but-incomplete gate mappings fail only inside owner execution."
                },
                {
                  "command": "read src/anomaly/attempts.py:21-71 and src/anomaly/state.py:1-14",
                  "output": "`run_attempts` increments and persists the attempt before executing the owner, catches every owner exception, retries while the attempt is below `MAX_ATTEMPTS`, and marks the phase unavailable at the limit; `MAX_ATTEMPTS` is 3."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 111,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The multi-phase dispatcher performs whole-case containment validation only once.",
            "why": "P5 and P6 invoke an external reasoning adapter that can create case artifacts. The next loop iteration loads state and executes another owner without rescanning, so a symlink introduced between phases can reach subsequent reads or writes. This violates AC7's whole-case no-symlink guarantee; the surviving tests cover only links present before run_workflow starts.",
            "refute": {
              "agent_id": "AnRefute16",
              "source": "review2",
              "finding": "src/anomaly/workflow.py:111 The multi-phase dispatcher performs whole-case containment validation only once.",
              "verdict": "survives",
              "rationale": "The public dispatcher scans only before entering its multi-phase loop, while the P5/P6 adapter receives the live case root and later state/event operations use unrescanned paths; no upstream guard prevents an adapter-created symlink from being followed, including by the post-phase event append.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:101-145",
                  "output": "run_workflow calls _scan_case_tree(case_root) once at line 111 before while True; each iteration then calls load_snapshot, resolves an owner, and invokes run_attempts without another whole-case scan. The reasoning wrapper passes the live case_root to invoke_resolved_owner."
                },
                {
                  "command": "read src/anomaly/owners.py:37-51 and src/anomaly/workflow.py:75-99",
                  "output": "For P5, consume_owner calls the external reason adapter, which is invoked with case_root, and accepts the result after only checking that findings/draft.json is_file; there is no post-adapter containment scan."
                },
                {
                  "command": "read src/anomaly/attempts.py:21-59 and src/anomaly/state.py:31-48,65-93",
                  "output": "After owner execution, run_attempts seals success and calls append_event. load_snapshot performs durable-state and identity work but no whole-case scan, while append_event directly opens root/.anomaly/events.jsonl in append mode. Therefore a valid P5 adapter can create a symlink at that path and the successful post-phase append follows it; the code does not fail safe."
                },
                {
                  "command": "read tests/test_pipeline_walk.py:512-553",
                  "output": "Both surviving dispatcher containment tests create their symlinks before run_workflow is called and assert rejection before attempts begin; neither exercises a link introduced by P5 or P6 during the loop."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 348,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The authoritative-identity invalidation matrix was deleted without replacement coverage for P3-P6.",
            "why": "Production still tracks recommendation, Gate A, detector, draft, replay, and review identities and promises earliest-suffix invalidation. Current tests cover source/P1, prepared/P2, and Gate B/P7 only. Deleting the behavioral test while retaining the behavior violates the testing skill's removal rule and makes AC4's claimed reuse disposition invalid; focused state/resolution coverage is owed.",
            "refute": {
              "agent_id": "AnRefute17",
              "source": "review2",
              "finding": "tests/test_pipeline_walk.py:348 The authoritative-identity invalidation matrix was deleted without replacement coverage for P3-P6.",
              "verdict": "survives",
              "rationale": "The public dispatcher reaches identity comparison for ordinary modified case artifacts, production maps six retained P3–P6 identities to earliest-suffix invalidation, and no current test exercises that state/resolution contract for those phases, so neither reachability nor the medium blocking severity is disproved.",
              "evidence": [
                {
                  "command": "read src/anomaly/identities.py:6-18; read src/anomaly/state.py:34-52,116-171; read src/anomaly/workflow.py:101-129",
                  "output": "IDENTITY_PHASES maps recommendation→P3, gate_a/detector→P4, draft→P5, and replay/review→P6; load_snapshot calls changed_identities and _invalidate_state removes the suffix from the earliest mapped phase; run_workflow calls load_snapshot on every public resolver iteration."
                },
                {
                  "command": "grep 'changed_identities|capture_identities|artifact_identity|IDENTITY_PHASES|invalidated_from' anomaly/tests; grep 'invalidat|_mark\\(' anomaly/tests/test_pipeline_walk.py",
                  "output": "No test imports or invokes the identity comparison/capture API; test_workflow.py only places invalidated_from=P3 in a resolver input fixture, while test_pipeline_walk.py mutates prepared/transforms.json (P2) and gate-b.json (P7), with no recommendation, Gate A, detector, draft, replay, or review mutation matrix."
                },
                {
                  "command": "read src/anomaly/case.py:255-303; read tests/test_m5_handoff.py:283-354",
                  "output": "The upstream case scan rejects symlinks, special files, and executable artifacts but permits changed regular case files, so the identity-change path is reachable; the detector tests in test_m5_handoff exercise replay_signals/accept_findings domain checks rather than run_workflow/load_snapshot earliest-suffix invalidation and do not replace P3–P6 matrix coverage."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 438,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "README preservation tests omit completed restart and failed recomputation with journalist content.",
            "why": "The AC5 disposition promises journalist-authored content survives completed restart, invalidation, failure, and recompletion. The notes fixture is added only immediately before invalidation, and the chart-failure test has no journalist content. Invalidation and recompletion are covered, but the other consumer-observable lifecycle transitions are not.",
            "refute": {
              "agent_id": "AnRefute18",
              "source": "review2",
              "finding": "tests/test_pipeline_walk.py:438 README preservation tests omit completed restart and failed recomputation with journalist content.",
              "verdict": "survives",
              "rationale": "Both omitted transitions are reachable through run_workflow and invoke README projection, while the completed-restart test adds no journalist content and the P7 chart-failure test contains no preservation assertion, so no upstream guard makes the AC5 coverage gap unreachable or less severe.",
              "evidence": [
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:tests/test_pipeline_walk.py",
                  "output": "Lines 218-219 restart the completed case immediately (`resumed = run_workflow(root)`) before any journalist notes are added; lines 445-452 add `journalist_notes` only inside the later invalidation test; lines 481-508 exercise failed P7 chart recomputation but neither add journalist content nor assert its preservation."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py && git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/state.py && git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/attempts.py",
                  "output": "workflow.py:119-125 calls `load_snapshot(case_root)` before returning a complete case; state.py:49-52 calls `project_readme` whenever that snapshot is complete through P7; attempts.py:48-61 records P7 failures and calls `project_readme(root, state, completed_phase(state))` after the final failed attempt. Therefore both omitted lifecycle transitions reach README rewriting."
                }
              ]
            }
          },
          {
            "file": "tests/test_review.py",
            "line": 425,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The inert-Markdown test does not exercise a standalone bare URL.",
            "why": "Both URL occurrences are inside Markdown-link or HTML-image syntax. The raw-substring assertion therefore does not prove AC6's explicit bare HTTP(S) autolink boundary. A prose-only URL case is owed, with assertions that visible text remains while no autolink is produced.",
            "refute": {
              "agent_id": "AnRefute19",
              "source": "review2",
              "finding": "tests/test_review.py:425 The inert-Markdown test does not exercise a standalone bare URL.",
              "verdict": "survives",
              "rationale": "The acceptance criterion explicitly requires inert bare HTTP(S) autolinks, but the fixture supplies URLs only as Markdown-link and HTML-image operands, so it can pass if those enclosing constructs are stripped while a prose-only URL still autolinks.",
              "evidence": [
                {
                  "command": "read tests/test_review.py:425-454",
                  "output": "The statement fixture is `Acme [click](https://example.invalid/pixel)`, `<img src=https://example.invalid/pixel>`, and a forged heading; there is no standalone prose URL, and the assertions only reject the two enclosing constructs, the heading, and the raw URL substring."
                },
                {
                  "command": "read task.md:13-15; read src/anomaly/review.py:655-665",
                  "output": "AC6 says dataset-derived Markdown must be inert `including bare URL autolinks`; `_markdown_text` has a dedicated context-independent HTTP(S) colon-neutralization branch, but the test never drives that branch with a prose-only bare-URL input or asserts that its visible text survives."
                }
              ]
            }
          }
        ],
        "evidence": [
          {
            "command": "read task.md, notes.md, context.md, and gated task.json",
            "output": "Re-derived all nine acceptance criteria from the locked task and plan. The mixed AC3 plan label was normalized to the single consumer-behavior disposition reuse; deletion applies only to obsolete composition tests. Gated hash is 4a1746f9050120df52e7845166f0ab935c4e584a with a clean green full-suite record."
          },
          {
            "command": "read src/anomaly/workflow.py:1-170, owners.py:1-220, attempts.py:1-170, workflow_inputs.py",
            "output": "The resolver registry is compact and dynamic owner paths are fixed, satisfying AC2. The owner boundary ignores attempt_dir for deterministic phases, dynamic invocation receives only case_root, required inputs track sources/gate_a/gate_b but not now, gate mappings are validated only after an attempt begins, and the case scan occurs only before entering the loop."
          },
          {
            "command": ".venv/bin/python -c '<pure resolver boundary probes>'",
            "output": "resolver_missing_now resolved P1 status=ready with missing=null; resolver_incomplete_gate_a resolved P4 status=ready with missing=null. These probes used no filesystem mutation."
          },
          {
            "command": "git diff --unified=25 9854273 4a1746f -- tests/test_pipeline_walk.py tests/test_workflow.py",
            "output": "The nine-case earliest-invalidation matrix and the attempt redaction test were deleted. Surviving public invalidation coverage exercises source, prepared generation, and Gate B, while focused resolver tests cover one ready P3 resolution and P5/P6 owner loading."
          },
          {
            "command": "read tests/test_pipeline_walk.py:185-553 and tests/test_review.py:399-454",
            "output": "The canonical demo proves both dynamic owners, completion, relative links, resume, and P5→P6→Gate B→P7 ordering. README notes are asserted only through invalidation/recompletion; the failure path has no notes. Markdown URLs appear only inside link/image constructs."
          },
          {
            "command": "grep WorkflowRunner|PhaseResult|RetryLimitExceeded|run_product_workflow|product_workflow|last_completed_phase across src, tests, PRD, and installed skill",
            "output": "No obsolete runner, result, retry exception, or product_workflow surface remains. state.write_state only removes legacy last_completed_phase and state-level gate keys during canonical writes, so AC8's delete disposition is backed by actual production removal."
          },
          {
            "command": "read .jeff/tasks/0027-resolver-consolidation/task.json",
            "output": "Jeff's supplied gate evidence records uv run --extra test pytest tests/ as green and clean at 4a1746f9050120df52e7845166f0ab935c4e584a. The passing suite does not cover the six blockers above."
          }
        ],
        "acLedger": [
          {
            "ac": "AC1",
            "claimed": "write",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC2",
            "claimed": "write",
            "rederived": "write",
            "ok": true
          },
          {
            "ac": "AC3",
            "claimed": "reuse",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC4",
            "claimed": "reuse",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC5",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC6",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC7",
            "claimed": "reuse",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC8",
            "claimed": "delete",
            "rederived": "delete",
            "ok": true
          },
          {
            "ac": "AC9",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          }
        ]
      },
      "audit": {
        "required": true,
        "verdict": "needs-work",
        "audit_agent_id": "AuditAnomalySchema",
        "evidence": [
          {
            "command": "read /Users/tomvaillant/buried_signals/tools/anomaly/scratchpads/security-audit-20260824-092723.md",
            "output": "The deterministic scan processed 7 files with zero regex findings and emitted PASS, but its ledger marked 10 of 12 scanner categories not_covered. Manual boundary analysis below supplies the missing reachability coverage rather than treating PASS as the audit verdict."
          },
          {
            "command": "git rev-parse HEAD",
            "output": "4a1746f9050120df52e7845166f0ab935c4e584a"
          },
          {
            "command": "read task.md and task.json gate evidence",
            "output": "Task 27 explicitly requires whole-case validation on every public resume/read/write path, strict human turn boundaries, identity/invalidation integrity, inert Markdown, journalist README preservation, atomic attempts, and audit of case containment, source mutation, detector execution, and report serialization. The supplied clean gate is 711/711 passing at the audited hash."
          },
          {
            "command": "read src/anomaly/case.py:112-166,252-377 and src/anomaly/acquire.py:31-132",
            "output": "inspect_case and resume_case do not scan the tree; _under_root resolves aliases and accepts targets that remain under the root. register_local_source enters through resume_case and its new-source branch writes directly to the resolved destination, confirming the in-case alias overwrite path."
          },
          {
            "command": "read src/anomaly/workflow.py:31-139 and src/anomaly/owners.py:37-137",
            "output": "Dynamic loader identities map only to fixed installed skill/persona paths and unknown owners are rejected, so case-selected code loading is not reachable. However, run_workflow scans only before the loop, gives dynamic invocations the live case root, never rescans between owners, and reuses all initially supplied gate inputs across fresh resolutions."
          },
          {
            "command": "read src/anomaly/identities.py:8-53 and src/anomaly/state.py:22-48,184-203",
            "output": "The identity phase map covers P1-P7 artifacts, but comparison iterates only recorded identities and state loading validates only that state is a dict. Missing identities therefore produce no invalidation before a sequential P7 completed map is honored."
          },
          {
            "command": "read src/anomaly/owners.py:116-142 and src/anomaly/semantics.py:63-79",
            "output": "Role separation uses casefold-only comparisons while the project's canonical_key normalizes NFC after case folding and portable identity validation rejects non-normalized/control-bearing components. The stronger canonical policy is not applied to reviewer or journalist identities."
          },
          {
            "command": "read src/anomaly/state.py:15,93-121, src/anomaly/attempts.py:87-121, and src/anomaly/review.py:20-31,1046-1068",
            "output": "The narrow state sanitizer is applied before writing failure.json, event records, failures, and blocked_reason. The report boundary independently handles sensitive keys, assignment forms, Bearer values, Slack/AWS/provider prefixes, demonstrating that common credentials missed by state.py are expected inputs."
          },
          {
            "command": "read src/anomaly/owners.py:79-108 and src/anomaly/acquire.py:118-131",
            "output": "P1 loops over requests and each registration commits live source bytes, its receipt, and sources.json before the next request is validated. attempt_dir is not used by this handler, so a later failure has no transactional rollback."
          },
          {
            "command": "read src/anomaly/detect.py:227-254,300-370,491-515,524-660",
            "output": "Detector metadata and query paths come from fixed installed packages; YAML uses safe_load or a restricted literal parser; detector/table identifiers are constrained; SQL is SELECT-only, parameterized, and rejects external readers; DuckDB is read-only with external access disabled; execution limits are positive integers; owned paths reject aliases. No command execution, unsafe deserialization, SQL injection, network transport, or weak crypto exploit was found in this boundary."
          },
          {
            "command": "read src/anomaly/review.py:594-663 and src/anomaly/readme.py:1-65",
            "output": "Dataset claim IDs and statements are HTML-escaped, Markdown metacharacters are escaped, and HTTP(S) colons are neutralized, so dataset-derived XSS and bare autolinks are not reachable. Separately, README projection globally deletes every exact legacy Outputs block, including journalist-authored content."
          },
          {
            "command": "read scanner dependency and permissions sections plus implement-return.json",
            "output": "The scan reported no dependency vulnerability and no insecure-permission hit; the implementation file list changes no dependency manifest. Changed runtime paths perform no outbound TLS/network activity, shell command execution, eval/exec, pickle loading, or permissive chmod."
          }
        ],
        "reportedVerdict": "needs-work",
        "findings": [
          {
            "file": "src/anomaly/acquire.py",
            "line": 46,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Direct source registration can follow an existing in-case symlink and overwrite another case artifact.",
            "why": "register_local_source resumes and validates only registered documents, then _under_root resolves the destination alias and accepts it when the target remains inside the case. The new-source branch writes bytes to that resolved target. A shared case can place data/raw/new-id/payload.csv as a symlink to case.json and cause the CSV payload to replace case.json; no whole-tree scan runs on this public path.",
            "cwe": "CWE-59",
            "refute": {
              "agent_id": "AnRefute20",
              "source": "audit",
              "finding": "src/anomaly/acquire.py:46 Direct source registration can follow an existing in-case symlink and overwrite another case artifact.",
              "verdict": "survives",
              "rationale": "The finding survives because this mainline registration entry calls only unscanned case/document validation, resolves an in-case destination symlink to its artifact target, and writes payload bytes to that resolved target; neither its decorator nor an upstream guard makes the overwrite unreachable or fail-safe.",
              "evidence": [
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/acquire.py (lines 31-47, 71-75, 123-131, 158-163)",
                  "output": "register_local_source calls resume_case(root) and validate_case_documents(root), builds relative = data/raw/<source_id>/<source-name>, and sets dest = _under_root(root, relative). _under_root computes candidate = (base / relative).resolve(), rejects only candidates outside base, and returns an in-case resolved candidate. For a new included source, lines 128-129 execute dest.parent.mkdir(...) and dest.write_bytes(payload), so a destination symlink resolving to case.json causes the CSV payload to be written to case.json."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/case.py src/anomaly/semantics.py src/anomaly/events.py src/anomaly/workflow.py (targeted guard inspection)",
                  "output": "case.py:146-150 resume_case only checks _case_exists and calls _load_case; it does not call _scan_case_tree. semantics.py:180-225 validate_case_documents reads data/sources.json and scans only .anomaly/receipts. events.py:51-67 phase_event merely calls the wrapped function and logs success/failure. workflow.py:102-113 does scan in run_workflow, but that guard belongs only to that separate wrapper and does not precede a direct register_local_source call."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 111,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The dispatcher scans the case only once before a multi-owner loop, so a dynamic owner can introduce a link that later phases follow.",
            "why": "P5 and P6 invoke external reasoning code with the live case_root. After that code returns, the loop calls load_snapshot and subsequent owners without another _scan_case_tree. A link introduced between phases can therefore redirect state, replay, review, receipt, or report reads and writes outside their intended artifact boundary during the same public invocation.",
            "cwe": "CWE-59",
            "refute": {
              "agent_id": "AnRefute21",
              "source": "audit",
              "finding": "src/anomaly/workflow.py:111 The dispatcher scans the case only once before a multi-owner loop, so a dynamic owner can introduce a link that later phases follow.",
              "verdict": "survives",
              "rationale": "The public dispatcher gives the P5/P6 invoker the live case root after its sole tree scan, and post-callback state/event operations perform ordinary path reads or appends without no-follow validation, so a callback can replace state.json or events.jsonl with a symlink and cause the same invocation to access an external target.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:101-145 and src/anomaly/owners.py:37-62",
                  "output": "run_workflow calls _scan_case_tree only once before entering while True; P5 then calls reason(resolution), which reaches invoke(owner=..., instructions=..., case_root=Path(case_root)), and the loop contains no later _scan_case_tree call."
                },
                {
                  "command": "read src/anomaly/attempts.py:21-71 and src/anomaly/state.py:31-110,188-203",
                  "output": "After execute returns, run_attempts writes the result, calls _seal_success/load_snapshot, and appends phase_completed. _read_state follows .anomaly/state.json via read_text, while append_event follows .anomaly/events.jsonl via open('a'); neither path is lstat-checked or opened with no-follow semantics."
                },
                {
                  "command": "grep 4a1746f9050120df52e7845166f0ab935c4e584a and supplied finding in .jeff/tasks/0027-resolver-consolidation/task.json",
                  "output": "The task gate records clean=true and green=true at 4a1746f9050120df52e7845166f0ab935c4e584a, and the active audit finding is the workflow.py:111 single-scan finding evaluated here."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/state.py",
            "line": 38,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "A completed snapshot with missing artifact identities is trusted as complete.",
            "why": "load_snapshot converts a missing or non-mapping identities field to an empty mapping, and changed_identities checks only keys that are present. A shared case can provide sequential P0-P7 completed entries, status complete, and no identities; resolve_workflow then returns terminal completion without hashing any required source, prepared data, detector plan, gate receipt, draft, replay, review, or Gate B artifact.",
            "cwe": "CWE-345",
            "refute": {
              "agent_id": "AnRefute22",
              "source": "audit",
              "finding": "src/anomaly/state.py:38 A completed snapshot with missing artifact identities is trusted as complete.",
              "verdict": "survives",
              "rationale": "The public workflow accepts the supplied state because missing identities become an empty mapping, the identity checker examines only recorded keys, and sequential P0-P7 completion causes an immediate terminal return without validating required artifact identities.",
              "evidence": [
                {
                  "command": "read src/anomaly/state.py:18-48",
                  "output": "completed_phase advances by key presence through PHASES; load_snapshot passes {} to changed_identities when identities is missing or not a Mapping, then returns the state unchanged when no change is reported."
                },
                {
                  "command": "read src/anomaly/identities.py:22-31",
                  "output": "changed_identities builds changed only from `for name, digest in recorded.items()` and returns None when that list is empty; it does not require any IDENTITY_PHASES key."
                },
                {
                  "command": "read src/anomaly/workflow.py:37-47,101-126",
                  "output": "resolve_workflow returns status `complete` as soon as completed_phase(snapshot) is P7; run_workflow calls load_snapshot, then returns that snapshot immediately for a complete resolution before run_attempts or any owner executes."
                },
                {
                  "command": "read src/anomaly/readme.py:24-62",
                  "output": "The P7 load-time README projection checks only completed/status fields and writes static output links; it does not verify that the linked artifacts exist or have identities."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/owners.py",
            "line": 122,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Canonical-equivalent Unicode identities bypass Gate A, reviewer, and Gate B role separation.",
            "why": "Both independence checks compare casefold values while _required_identity only strips whitespace. Composed and decomposed forms such as Café and Café remain unequal under casefold alone even though canonical_key treats them as the same identity, allowing one logical person to occupy supposedly independent roles.",
            "cwe": "CWE-178",
            "refute": {
              "agent_id": "AnRefute23",
              "source": "audit",
              "finding": "src/anomaly/owners.py:122 Canonical-equivalent Unicode identities bypass Gate A, reviewer, and Gate B role separation.",
              "verdict": "survives",
              "rationale": "The public dispatcher accepts unnormalized role identities, both separation checks compare only whitespace-stripped casefold values, and no downstream attestation or persistence guard canonicalizes across roles, so composed and decomposed spellings let one logical identity pass both independent-role checks.",
              "evidence": [
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/owners.py (lines 111-165)",
                  "output": "_replay_and_review obtains reviewer_id through _required_identity, reads Gate A approved_by through the same helper, and rejects equality only with reviewer_id.casefold() == gate_a_approver.casefold() at line 122; _accept_and_report performs the analogous casefold-only reviewer/Gate B comparison at line 134; _required_identity at lines 162-165 only validates non-empty string and returns value.strip()."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/workflow.py (lines 102-137) and src/anomaly/workflow_inputs.py (lines 25-36, 69-76)",
                  "output": "run_workflow is the reachable public entry, copies inputs with normalize_inputs, and passes the resulting supplied mapping to consume_owner; normalize_inputs validates only top-level input names, while mapping_input copies gate mappings and rejects only unknown fields. Neither performs Unicode normalization or identity canonicalization."
                },
                {
                  "command": "git show 4a1746f9050120df52e7845166f0ab935c4e584a:src/anomaly/recommend.py (lines 364-377) and src/anomaly/review.py (lines 295-330, 399-407)",
                  "output": "Gate A validates approved_by only as a non-empty string and persists it verbatim. record_review only strips reviewer_id; strict attestation checks attested_by == reviewer but does not compare a canonical identity against Gate A. Gate B validates non-empty identity and optional exact configured equality, with no canonical cross-role guard."
                },
                {
                  "command": "python3 -c 'compare Café with Cafe\\u0301 using str.casefold and the gated canonical_key algorithm'",
                  "output": "{'casefold_equal': False, 'canonical_equal': True, 'a_nfc': True, 'b_nfc': False}"
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 109,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Future Gate A and Gate B decisions are retained across the resolver loop and consumed without a fresh human turn.",
            "why": "normalize_inputs captures all supplied decisions before execution, and the same mapping is reused after every newly completed phase. A gate_a decision supplied before P3 or gate_b supplied before P6 can become immediately actionable once the preceding phase seals, bypassing the required informed pause at each human gate.",
            "cwe": "CWE-863",
            "refute": {
              "agent_id": "AnRefute24",
              "source": "audit",
              "finding": "src/anomaly/workflow.py:109 Future Gate A and Gate B decisions are retained across the resolver loop and consumed without a fresh human turn.",
              "verdict": "survives",
              "rationale": "The public dispatcher captures every input once before its execution loop, carries that same mapping through every newly sealed phase, and treats mere key presence as sufficient to cross Gate A or Gate B; downstream artifact and identity validation does not prove that either decision was supplied in a fresh, informed human turn.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:101-139 and src/anomaly/workflow_inputs.py:16-31",
                  "output": "run_workflow calls normalize_inputs(inputs) once before while True, then on every iteration builds available from the unchanged supplied mapping and passes supplied into consume_owner; normalize_inputs copies all public inputs, including gate_a and gate_b, without phase or turn binding."
                },
                {
                  "command": "read src/anomaly/workflow.py:37-69,160-167 and src/anomaly/owners.py:49-80,125-137",
                  "output": "resolve_workflow pauses only when the phase's required input name is absent. If gate_a or gate_b was supplied earlier, P4/P7 resolves ready immediately and consume_owner reads the retained mapping; neither boundary checks when the decision entered the call."
                },
                {
                  "command": "read src/anomaly/recommend.py:335-390 and src/anomaly/review.py:371-477",
                  "output": "Gate A validates approved IDs against the current deterministic plan and Gate B validates review/replay/identity artifacts, but neither receipt requires a post-P3/post-P6 turn token or decision timestamp. Gate B also permits an empty accepted_claim_ids list, so a future Gate B decision can remain structurally valid without knowing claim IDs in advance."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/state.py",
            "line": 15,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Attempt failures persist common credentials because the central error sanitizer recognizes only three token prefixes.",
            "why": "safe_error feeds failure.json, events.jsonl, state.failures, and blocked_reason, but _SECRET covers only sk_live_, ghp_, and github_pat_. Dynamic-owner exceptions containing Authorization: Bearer values, password or token assignments, AWS keys, Slack tokens, or credential-bearing URLs are stored verbatim across retries even though review.py already has broader redaction patterns.",
            "cwe": "CWE-532",
            "refute": {
              "agent_id": "AnRefute25",
              "source": "audit",
              "finding": "src/anomaly/state.py:15 Attempt failures persist common credentials because the central error sanitizer recognizes only three token prefixes.",
              "verdict": "survives",
              "rationale": "The public workflow can pass an arbitrary dynamic-owner exception directly into durable attempt recording, and the only sanitizer on that path recognizes three provider prefixes, so common credentials remain reachable and are persisted repeatedly without an upstream guard or fail-safe.",
              "evidence": [
                {
                  "command": "read src/anomaly/state.py:1-121; read src/anomaly/semantics.py:44-101",
                  "output": "safe_error calls _safe_text, which applies redact_credentials and _SECRET; both credential regexes match only sk_live_, ghp_, and github_pat_, with no assignment, Bearer, AWS, Slack, or credential-bearing URL handling."
                },
                {
                  "command": "read src/anomaly/workflow.py:75-136; read src/anomaly/attempts.py:21-121",
                  "output": "run_workflow is the public entry point, invokes the caller-supplied reasoning callback without constraining exception text, and run_attempts catches any Exception; _record_failure then writes the sanitized message to failure.json, events.jsonl, state.failures, and blocked_reason after the third attempt."
                },
                {
                  "command": "read src/anomaly/review.py:20-35,1040-1072; read skills/anomaly/SKILL.md:35-58",
                  "output": "review.py already redacts sensitive assignments, Bearer values, Slack xox tokens, and AWS AKIA keys, while the installed workflow contract requires credential-redacted failure persistence and forbids credentials in case JSON or events; no equivalent protection exists on the attempt-error path."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/owners.py",
            "line": 92,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "A failed multi-source P1 attempt partially commits earlier sources instead of preserving atomic attempt semantics.",
            "why": "_register_sources calls register_local_source directly for each request, and each call mutates live raw bytes, receipts, and sources.json. If a later request is invalid, run_attempts records failure and retries but never rolls back the earlier successful registrations, leaving durable source mutation under an unavailable P1 outcome.",
            "cwe": null,
            "refute": {
              "agent_id": "AnRefute26",
              "source": "audit",
              "finding": "src/anomaly/owners.py:92 A failed multi-source P1 attempt partially commits earlier sources instead of preserving atomic attempt semantics.",
              "verdict": "survives",
              "rationale": "The defect is reachable through the public run_workflow sources input because only request shape and duplicate IDs are prevalidated, each valid earlier request commits directly to the live case before a later request can fail source-specific validation, and run_attempts records and retries the failure without restoring those mutations, so the high-severity atomicity violation is honest.",
              "evidence": [
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:1-180",
                  "output": "The clean green gate is bound to hash 4a1746f9050120df52e7845166f0ab935c4e584a, and task AC3 requires durable attempt staging, failure evidence, and atomic promotion."
                },
                {
                  "command": "read src/anomaly/workflow.py:101-139, src/anomaly/owners.py:49-108, and src/anomaly/workflow_inputs.py:48-63",
                  "output": "Public run_workflow routes P1 into run_attempts and consume_owner; _run_handler ignores attempt_dir and _register_sources iterates register_local_source against the live case. source_requests prevalidates only nonempty list/mapping shape and required/unknown fields, leaving later per-source path, format, availability, and inclusion validation reachable after an earlier request has succeeded."
                },
                {
                  "command": "read src/anomaly/acquire.py:31-132 and src/anomaly/attempts.py:21-67,87-121",
                  "output": "For a new included source, register_local_source writes raw bytes, a receipt, and data/sources.json immediately. A later exception is caught by run_attempts, which writes failure evidence and retries or marks P1 unavailable; no rollback or attempt-directory promotion restores the earlier live writes."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/readme.py",
            "line": 36,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "implement",
            "what": "README projection can delete journalist-authored Markdown that happens to equal the legacy Outputs block.",
            "why": "After bounded marker removal, project_readme performs a global exact-string replacement of _OUTPUTS. An unmarked journalist section with the same heading and links is removed during completion or invalidation projection, violating the explicit preservation requirement and causing reachable authored-content loss.",
            "cwe": null,
            "refute": {
              "agent_id": "AnRefute27",
              "source": "audit",
              "finding": "src/anomaly/readme.py:36 README projection can delete journalist-authored Markdown that happens to equal the legacy Outputs block.",
              "verdict": "survives",
              "rationale": "The public workflow reaches this projection on P7 completion, completed-case resume, and P7 invalidation, and no upstream content guard prevents a journalist-authored block from exactly matching _OUTPUTS, so the global replacement can delete valid authored Markdown and the medium data-loss severity is honest.",
              "evidence": [
                {
                  "command": "read src/anomaly/readme.py:24-38",
                  "output": "project_readme reads the existing README, removes the bounded anomaly marker block, then calls .replace(_OUTPUTS, \"\") without a count or marker requirement; the only local guards are README existence and non-symlink status."
                },
                {
                  "command": "read src/anomaly/workflow.py:101-126; src/anomaly/state.py:34-52,151-174; src/anomaly/attempts.py:50-88",
                  "output": "run_workflow calls load_snapshot on a real case; load_snapshot projects completed P7 READMEs, _invalidate_state projects after invalidating a previously completed P7 case, and _seal_success projects after successful P7 completion."
                },
                {
                  "command": "grep 'README.md|Outputs|accepted findings' src/anomaly",
                  "output": "The only README content mutation/guard in production is readme.py; no validator rejects or distinguishes an unmarked journalist-authored exact _OUTPUTS block."
                },
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/task.md",
                  "output": "Acceptance explicitly requires README projection to preserve journalist-authored content, making deletion of a valid unmarked matching section a contract violation."
                }
              ]
            }
          }
        ],
        "scan": {
          "command": "/Users/tomvaillant/.omp/plugins/node_modules/@johanthoren/jeff/skills/security-auditor/scripts/review-security.sh --changes",
          "recommendation": "PASS",
          "reportPath": "/Users/tomvaillant/buried_signals/tools/anomaly/scratchpads/security-audit-20260824-092723.md"
        },
        "coverage": [
          {
            "category": "secrets",
            "status": "covered_with_hits"
          },
          {
            "category": "injection_sql",
            "status": "covered_no_hits"
          },
          {
            "category": "injection_command",
            "status": "covered_no_hits"
          },
          {
            "category": "path_traversal",
            "status": "covered_with_hits"
          },
          {
            "category": "insecure_deserialization",
            "status": "covered_no_hits"
          },
          {
            "category": "weak_crypto",
            "status": "covered_no_hits"
          },
          {
            "category": "dynamic_execution",
            "status": "covered_no_hits"
          },
          {
            "category": "tls_transport",
            "status": "covered_no_hits"
          },
          {
            "category": "xss",
            "status": "covered_no_hits"
          },
          {
            "category": "sensitive_logging",
            "status": "covered_with_hits"
          },
          {
            "category": "insecure_permissions",
            "status": "covered_no_hits"
          }
        ]
      },
      "agents": {
        "reviewer_agent_id": "ReviewAnomalyCorrectnessSchema",
        "reviewer2_agent_id": "ReviewAnomalyStandardsSchema",
        "audit_agent_id": "AuditAnomalySchema"
      }
    },
    {
      "at": "2026-08-24T12:01:44Z",
      "review": {
        "verdict": "needs-work",
        "reportedVerdict": "needs-work",
        "reviewer_agent_id": "ReviewAnomalyRecoveredCorrect",
        "findings": [
          {
            "file": "src/anomaly/workflow_inputs.py",
            "line": 61,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The repaired schema-aware input boundary still treats unconsumable source and gate values as complete capabilities.",
            "why": "source_requests validates only key presence, while _is_complete_mapping validates only the outer sequence and a nonblank actor identity. Empty source IDs, included=False sources without the conditionally required reason, and gate ID lists containing non-strings all become sources/gate_a/gate_b capabilities and therefore enter three execution attempts instead of pausing before an attempt. Independent probes returned ['sources'], ['gate_a'], and ['gate_b'] for those invalid payloads. This leaves the prior schema-blind-input blocker partially unresolved and requires focused public tests for value types and the conditional source schema before production repair.",
            "refute": {
              "agent_id": "AnPostRefute1",
              "source": "review",
              "finding": "src/anomaly/workflow_inputs.py:61 The repaired schema-aware input boundary still treats unconsumable source and gate values as complete capabilities.",
              "verdict": "survives",
              "rationale": "The public run_workflow path is reachable at P1, P4, and P7 with these malformed values, because capability detection accepts them before run_attempts invokes stricter downstream validators whose exceptions consume all three attempts, and no upstream guard makes that path unreachable or fail-safe.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow_inputs.py:20-119",
                  "output": "normalize_inputs validates only top-level names; source_requests requires source-field presence but does not validate an empty source_id or the included=False reason condition; _is_complete_mapping requires only an outer list/tuple and nonblank identity, without validating sequence elements."
                },
                {
                  "command": "read src/anomaly/workflow.py:85-204; src/anomaly/attempts.py:29-92; src/anomaly/owners.py:37-140",
                  "output": "run_workflow derives capabilities from input_capabilities and resolves P1/P4/P7 ready, then run_attempts calls consume_owner with the original supplied values; downstream owner exceptions are caught and retried until MAX_ATTEMPTS, so malformed values consume attempts instead of pausing."
                },
                {
                  "command": "read src/anomaly/acquire.py:31-65; src/anomaly/recommend.py:335-374; src/anomaly/review.py:366-447",
                  "output": "The missing guards exist only after attempt entry: register_local_source rejects an empty source_id and requires reason when included is false; approve_detector_plan rejects non-string detector IDs; accept_findings rejects non-string claim IDs."
                },
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/review-recovered-correctness.json:95-102",
                  "output": "The gated reviewer probe reports blank_source_id ['sources'], non_string_gate_id ['gate_a'], non_string_claim_id ['gate_b'], and included=False without reason ['sources'], confirming each malformed payload is advertised as a capability."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/attempts.py",
            "line": 48,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "A process interruption can consume an attempt without failure evidence and eventually strand run_workflow in an infinite ready/no-op loop.",
            "why": "run_attempts persists the incremented attempt count before a promotion journal exists, and catches only Exception. KeyboardInterrupt, process termination, or a crash during workspace creation/owner execution therefore leaves active state with a consumed attempt and no failure record or recovery marker. At attempts[phase] == 3, resolve_workflow still returns ready with 'attempt 4 of 3', while run_attempts skips its loop and returns the same active state; run_workflow repeats forever. The suite has no interrupted-attempt or promotion-recovery seam—its atomicity tests cover owner failure before promotion only—so AC3's durable attempts, failure evidence, atomic promotion, and finite retry contract need a new behavior test plus recovery implementation.",
            "refute": {
              "agent_id": "AnPostRefute2",
              "source": "review",
              "finding": "src/anomaly/attempts.py:48 A process interruption can consume an attempt without failure evidence and eventually strand run_workflow in an infinite ready/no-op loop.",
              "verdict": "survives",
              "rationale": "The public dispatcher reaches an interruption window after durably incrementing the phase attempt but before any failure evidence or promotion journal exists; because BaseException interruptions bypass both Exception handlers, three such interruptions leave active attempts at the limit, where the resolver still returns ready while run_attempts executes nothing, so run_workflow has no terminal path.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/attempts.py:1-180",
                  "output": "run_attempts increments attempts[phase], writes active state, creates the attempt directory, and appends phase_started before entering owner execution; both failure paths catch only Exception, while the bounded loop is `while attempt < MAX_ATTEMPTS` and falls through by returning the unchanged state."
                },
                {
                  "command": "read anomaly/src/anomaly/_attempt_workspace.py:1-190",
                  "output": "promote_workspace creates `.anomaly/promotion.json` only after owner execution and output validation reach promotion; recover_interrupted_promotion immediately returns when that journal does not exist, so it cannot rewind an interruption during workspace creation or owner execution."
                },
                {
                  "command": "read anomaly/src/anomaly/workflow.py:70-225",
                  "output": "resolve_workflow treats only status blocked/unavailable as terminal and otherwise reports ready with `attempts + 1` without enforcing MAX_ATTEMPTS; run_workflow repeatedly calls run_attempts and returns only for complete, unavailable, paused, blocked, or unavailable state, so active attempts==3 repeats ready/no-op indefinitely."
                },
                {
                  "command": "read anomaly/src/anomaly/owners.py:1-155",
                  "output": "The public run_workflow path passes its resolved owner through consume_owner, which directly invokes deterministic handlers or the supplied reasoning callback; there is no upstream BaseException guard that makes an interruption unreachable."
                },
                {
                  "command": "grep 'KeyboardInterrupt|BaseException|interrupted attempt|promotion recovery|recover_interrupted_promotion|attempt 4 of 3' anomaly/tests anomaly/src/anomaly",
                  "output": "KeyboardInterrupt/BaseException coverage exists for prepare/profile commit restoration, but no attempt-runner interruption or exhausted-active recovery test appears; the only workflow-level recovery hit is recover_interrupted_promotion."
                }
              ]
            }
          },
          {
            "file": "tests/test_skill.py",
            "line": 76,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The deleted runner/verb/event-resume model remains as a stale test expectation and installed skill contract.",
            "why": "The test still requires 'last completed event|resume from', and skills/anomaly/SKILL.md satisfies it by claiming resume authority is the last completed event at line 232 even though PRD.md and state.py make the completed-phase map authoritative and events best-effort. The skill also documents a durable runner, nonexistent read-file/write-file/execute-shell/invoke-skill and spawn-agent/wait-agent verb bindings (lines 46 and 209-220), and locates MAX_ATTEMPTS in deleted runner code (line 247). These are procedure/prose change-detector assertions for behavior removed with product_workflow.py, contradict users of the installed skill and violating AC8's explicit removal of stale expectations. Revise/delete the stale test and migrate the skill to the actual resolver, owner invoker, and state authority.",
            "refute": {
              "agent_id": "AnPostRefute3",
              "source": "review",
              "finding": "tests/test_skill.py:76 The deleted runner/verb/event-resume model remains as a stale test expectation and installed skill contract.",
              "verdict": "survives",
              "rationale": "The blocker is reachable and high-severity because the public P5 path loads the entire installed skill without filtering, while that skill and its test still encode deleted runner, verb-binding, event-resume, and MAX_ATTEMPTS-location claims that directly contradict the completed-phase-map authority required by the task and PRD.",
              "evidence": [
                {
                  "command": "read anomaly/tests/test_skill.py:69-79; read anomaly/skills/anomaly/SKILL.md:207-247",
                  "output": "test_skill.py requires `resume.{0,120}(event|receipt|state)` and `last completed event|resume from`; SKILL.md satisfies the latter with `resumes from file presence instead of the last completed event`, claims abstract read-file/write-file/search/execute-shell/invoke-skill plus spawn-agent/wait-agent bindings, and says MAX_ATTEMPTS is enforced by workflow.py's runner."
                },
                {
                  "command": "read anomaly/PRD.md:202-230; read anomaly/src/anomaly/state.py:12-25",
                  "output": "PRD.md says state.json is authoritative, events.jsonl is best-effort and never resume authority, and restart follows the canonical completed-phase map; state.completed_phase walks the snapshot `completed` mapping to determine the last completed phase."
                },
                {
                  "command": "read anomaly/src/anomaly/workflow.py:51-84,94-154; read anomaly/src/anomaly/owners.py:37-53",
                  "output": "P5 resolves to owner `{kind: skill, id: anomaly}`; invoke_resolved_owner reads all of `skills/anomaly/SKILL.md` and passes it unchanged to the caller-supplied invoker; consume_owner invokes that reasoning owner for P5, so no upstream guard makes the stale installed instructions unreachable."
                },
                {
                  "command": "read anomaly/.jeff/tasks/0027-resolver-consolidation/task.md",
                  "output": "Acceptance explicitly requires `stale expectations ... removed`, and the non-goals explicitly prohibit a shared runner."
                }
              ]
            }
          }
        ],
        "evidence": [
          {
            "command": "read .jeff/tasks/0027-resolver-consolidation/task.json:24-49",
            "output": "Recorded gate is clean=true, green=true at 8500ab972190cc5d83fc37344fede3a24816dfe2 with command `uv run --extra test pytest tests/`; the supplied gate result is 736/736 passed."
          },
          {
            "command": ".venv/bin/python -c '<input_capabilities probes>'",
            "output": "blank_source_id ['sources']\nnon_string_gate_id ['gate_a']\nnon_string_claim_id ['gate_b']\nincluded=False without reason also returned ['sources']."
          },
          {
            "command": ".venv/bin/python -c '<resolve active P1 snapshot with attempts=3>'",
            "output": "{'phase': 'P1', 'status': 'ready', 'owner': {'kind': 'handler', 'id': 'register-sources'}, 'missing': None, 'attempts': 3, 'invalidated_from': None, 'resume': 'Resume P1 after P0; attempt 4 of 3.'}"
          },
          {
            "command": "grep tool: `last completed event|durable runner|Supported in local runs|spawn-agent|wait-agent|MAX_ATTEMPTS.*workflow.py` in skills/anomaly/SKILL.md plus tests/test_skill.py",
            "output": "SKILL.md:46 documents a durable runner; 209-220 document removed verb bindings; 232 says resume uses the last completed event; 247 points MAX_ATTEMPTS to workflow.py. tests/test_skill.py:76 explicitly requires the stale last-completed-event/resume phrase."
          },
          {
            "command": "read current recovery-owned tests and source boundaries",
            "output": "Verified repaired proofs for fresh Gate A/B turns, required now and missing gate fields, P1/P7 owner-failure atomicity, redacted three-attempt evidence, all six P3-P6 invalidation classes, missing identities, canonical role separation, README restart/invalidation/failure/recompletion, standalone bare URL escaping, public/direct/post-dynamic symlink rejection, source replacement, event-store resume, relative links, and canonical demo order. No test exercises invalid value schemas, interrupted attempt reconciliation, or promotion-journal rollback/recovery."
          }
        ],
        "acLedger": [
          {
            "ac": "AC1",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC2",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          },
          {
            "ac": "AC3",
            "claimed": "revise",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC4",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC5",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          },
          {
            "ac": "AC6",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          },
          {
            "ac": "AC7",
            "claimed": "write",
            "rederived": "write",
            "ok": true
          },
          {
            "ac": "AC8",
            "claimed": "delete",
            "rederived": "delete",
            "ok": false
          },
          {
            "ac": "AC9",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          }
        ]
      },
      "review2": {
        "verdict": "needs-work",
        "reportedVerdict": "needs-work",
        "reviewer_agent_id": "ReviewAnomalyRecoveredStandards",
        "findings": [
          {
            "file": "src/anomaly/case.py",
            "line": 101,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The recovered whole-case containment boundary still omits the public create path.",
            "why": "create_case resolves the caller's root before any _scan_case_tree call and never scans the prospective case. A symlinked root is followed and an unrelated nested symlink can remain inside a newly created case, so the public writer can create/write through a case path that inspect_case, resume_case, and decorated writers correctly reject. AC7 says every public resume/read/write path is protected, and the plan-owned tests cover inspect/resume/register/dynamic continuation but not create_case. Add a behavioral create-case root/nested-link test, then scan the unresolved path before resolve or any write.",
            "refute": {
              "agent_id": "AnPostRefute4",
              "source": "review2",
              "finding": "src/anomaly/case.py:101 The recovered whole-case containment boundary still omits the public create path.",
              "verdict": "survives",
              "rationale": "The public create_case entry is directly reachable and resolves a symlinked root before mkdir and every write without ever calling _scan_case_tree, while inspect_case and resume_case scan the unresolved caller path first; therefore creation can follow a root symlink or preserve an unrelated nested symlink and produce/write through a case boundary that the other public paths reject.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/case.py:102-151",
                  "output": "create_case starts with `root = Path(root).resolve()`, then creates directories and writes AGENTS.md, README.md, case.json, instruction, data, findings, state, and event files with no `_scan_case_tree` call. By contrast, inspect_case and resume_case retain `Path(root)` and call `_scan_case_tree(root)` before existence checks or loads."
                },
                {
                  "command": "read anomaly/src/anomaly/case.py:257-288",
                  "output": "`_scan_case_tree` converts the caller path with abspath rather than resolve, rejects symlinks in the root/ancestor chain via lstat, and walks existing descendants rejecting nested symlinks and special files; create_case bypasses this guard entirely."
                },
                {
                  "command": "read anomaly/tests/test_case.py:227-238",
                  "output": "The whole-case nested-symlink behavioral test is parametrized only over `[inspect_case, resume_case]`; create_case is not covered."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/identities.py",
            "line": 17,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "P7 completion does not validate the identities or existence of the outputs that README advertises.",
            "why": "The sole P7 identity is gate_b, and _identity_files maps it only to .anomaly/receipts/gate-b.json. findings/findings.json, findings/report.md, findings/unresolved.md, findings/charts, and the charts receipt are absent from resume invalidation. Deleting or modifying a completed report therefore leaves completed_phase=P7; load_snapshot projects Status: complete and links to a missing or stale output. This breaks complete invalidation and truthful README completion in AC4/AC5. Add a consumer test that removes or changes each authoritative P7 output after completion and expects invalidation from P7 plus README demotion, then bind those outputs into the P7 identity/receipt validation.",
            "refute": {
              "agent_id": "AnPostRefute5",
              "source": "review2",
              "finding": "src/anomaly/identities.py:17 P7 completion does not validate the identities or existence of the outputs that README advertises.",
              "verdict": "survives",
              "rationale": "The blocker is reachable because load_snapshot derives completion invalidation solely from changed_identities, P7 requires only the gate_b identity, and that identity hashes only .anomaly/receipts/gate-b.json; no upstream guard revalidates findings/findings.json, findings/report.md, findings/unresolved.md, findings/charts, or .anomaly/receipts/charts.json before project_readme preserves Status: complete and its output links.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/identities.py:8-18,22-39,74-101",
                  "output": "IDENTITY_PHASES assigns only \"gate_b\": \"P7\". changed_identities iterates that table and invalidates only when artifact_identity differs or is absent. _identity_files maps gate_b solely to (\".anomaly/receipts/gate-b.json\",)."
                },
                {
                  "command": "read anomaly/src/anomaly/state.py:32-50",
                  "output": "load_snapshot calls changed_identities(... through_phase=completed_phase(state)); if no mapped identity changed, completed remains P7 and project_readme(root, state, completed) runs."
                },
                {
                  "command": "read anomaly/src/anomaly/readme.py:7-17,24-58",
                  "output": "The complete projection advertises findings/findings.json, findings/report.md, and findings/unresolved.md, and sets Status to complete whenever completed is P7 and snapshot status is complete; it performs no existence or digest validation of those targets."
                },
                {
                  "command": "read anomaly/src/anomaly/workflow.py:65-79",
                  "output": "The P7 owner write set explicitly includes findings/findings.json, findings/report.md, findings/unresolved.md, findings/charts, .anomaly/receipts/gate-b.json, .anomaly/receipts/charts.json, events, and README, confirming the omitted artifacts are authoritative P7 outputs."
                },
                {
                  "command": "read anomaly/tests/test_pipeline_walk.py:319-381,748-789",
                  "output": "The completion test checks output existence only immediately after generation and resumes without mutating outputs; the P7 invalidation test mutates only gate-b.json. There is no consumer test deleting or changing each advertised/authoritative P7 output after completion."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow_inputs.py",
            "line": 39,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The input-capability boundary remains only shape-aware, not schema-aware, for source and human-decision values.",
            "why": "source_requests accepts any values when the required keys are present, while _is_complete_mapping accepts list/tuple decision collections without validating their elements or Gate A's required non-empty selection. Inputs such as approved_ids=[1] or accepted_claim_ids=[None] are classified as complete, enter run_attempts, and are rejected only by the owner after an attempt has been consumed; equivalent malformed source fields do the same. That contradicts the locked schema-aware-input/human-turn repair and boundary-validation standard. Add malformed-value public tests proving no attempt is spent, and make capability derivation perform the complete input schema check (or reject explicitly before execution).",
            "refute": {
              "agent_id": "AnPostRefute6",
              "source": "review2",
              "finding": "src/anomaly/workflow_inputs.py:39 The input-capability boundary remains only shape-aware, not schema-aware, for source and human-decision values.",
              "verdict": "survives",
              "rationale": "The public path is concretely reachable: malformed but shape-complete source and gate values become capabilities, make P1/P4/P7 ready, and reach run_attempts before the owner performs the missing value validation; run_attempts increments and persists the attempt first, retries the same invalid input through all three attempts, and marks the phase unavailable, so no upstream guard makes the failure unreachable or less severe.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow_inputs.py:39-130",
                  "output": "input_capabilities adds sources whenever source_requests accepts the container, but source_requests checks only list/nonempty, mapping, and field names; it does not validate field values. _is_complete_mapping adds gate_a/gate_b when the decision field is merely a list/tuple and the identity is a nonblank string; it neither validates collection elements nor requires Gate A approved_ids to be nonempty."
                },
                {
                  "command": "read src/anomaly/workflow.py:158-205",
                  "output": "run_workflow is the public dispatcher. It derives capabilities from the supplied values, resolves the phase from those capabilities, and, for a ready resolution, calls run_attempts with consume_owner; there is no intervening schema validation."
                },
                {
                  "command": "read src/anomaly/owners.py:52-91,129-136; src/anomaly/recommend.py:343-362; src/anomaly/review.py:428-432",
                  "output": "The P4 owner passes approved_ids to approve_detector_plan, which rejects an empty collection or non-string entries; the P7 owner passes accepted_claim_ids to accept_findings, which rejects non-string entries. These owner checks occur inside the callback already handed to run_attempts. Source requests likewise reach _register_sources after being classified complete."
                },
                {
                  "command": "read src/anomaly/attempts.py:29-83,153-203",
                  "output": "run_attempts increments attempts[phase], writes active state, and records phase_started before execute(workspace). An owner exception is recorded as a failed attempt and retried while attempt < MAX_ATTEMPTS; at attempt 3 the phase is persisted as unavailable and blocked."
                },
                {
                  "command": "read .jeff/tasks/0027-resolver-consolidation/notes.md:9-18; tests/test_pipeline_walk.py:209-318",
                  "output": "The locked approach says schema validation must produce complete input capabilities and keep human pauses attempt-free. Existing public tests cover missing now and missing gate keys, but the shown gate tests do not cover malformed decision elements, Gate A's empty selection at a Gate A entry, or malformed source-field values."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 518,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "No behavioral test proves journalled promotion rollback or interrupted-promotion recovery.",
            "why": "The revised AC3 tests demonstrate that an owner failure before promotion cannot leak an earlier source or P7 output, but none exercises a failure/interruption after _apply begins or the new recover_interrupted_promotion path. The acceptance criterion explicitly requires atomic promotion and durable recovery, so this consumer-observable restart contract is owed. Add deterministic failure/restart coverage that observes the live case restored to the last sealed state, the attempt rewound consistently, and a fresh resolution succeeding; do not assert private call order.",
            "refute": {
              "agent_id": "AnPostRefute7",
              "source": "review2",
              "finding": "tests/test_pipeline_walk.py:518 No behavioral test proves journalled promotion rollback or interrupted-promotion recovery.",
              "verdict": "survives",
              "rationale": "The blocker survives because the line-518 test fails during owner execution before promotion starts, while the public workflow can be interrupted after an entry is applied and invokes durable recovery on the next run; no test reaches or observes that restart contract.",
              "evidence": [
                {
                  "command": "grep -RniE 'promotion|recover_interrupted|rollback|_apply|attempt_workspace|promote' tests",
                  "output": "The only relevant test hits are tests/test_pipeline_walk.py:518 and :791, both pre-promotion failure-isolation cases, plus tests/test_case.py:426, which only checks fork cleanup; no test references recover_interrupted_promotion, _apply, rollback, or a promotion journal."
                },
                {
                  "command": "read src/anomaly/_attempt_workspace.py:25-69,75-105; src/anomaly/workflow.py:163-170",
                  "output": "promote_workspace writes .anomaly/promotion.json, _apply journals each entry as applying/applied around live replacements, and recover_interrupted_promotion rolls back an unsealed journal, rewinds the attempt, and removes recovery artifacts. run_workflow calls recover_interrupted_promotion at every public entry, so interruption after replacement and restart is reachable."
                },
                {
                  "command": "read tests/test_pipeline_walk.py:518-551,791-828",
                  "output": "The line-518 invalid-source case raises while consuming the source batch in the workspace, and the P7 case makes findings/charts a file so phase execution fails in the workspace; neither forces a failure after _apply begins, restarts with a journal, checks restoration to the sealed live case, checks attempt rewind, or proves a fresh resolution succeeds."
                }
              ]
            }
          },
          {
            "file": "skills/anomaly/SKILL.md",
            "line": 46,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "implement",
            "what": "Installed documentation still resurrects the removed runner and states the wrong resume authority and constant owner.",
            "why": "The skill says a durable runner records phases, later tells readers to resume from the last completed event, and at lines 246-247 says MAX_ATTEMPTS lives in workflow.py where 'the runner' enforces it. Current code has no runner, events are explicitly observational, completion comes from state.completed, and MAX_ATTEMPTS is owned by state.py/run_attempts. Incorrect method documentation is a bug and violates AC8's runner/stale-expectation removal. Rewrite these claims to the resolver → owner → run_attempts/state model.",
            "refute": {
              "agent_id": "AnPostRefute8",
              "source": "review2",
              "finding": "skills/anomaly/SKILL.md:46 Installed documentation still resurrects the removed runner and states the wrong resume authority and constant owner.",
              "verdict": "survives",
              "rationale": "The installed P5 skill is loaded verbatim with no correcting guard, yet it names a durable runner, treats the last completed event as resume authority, and assigns MAX_ATTEMPTS to workflow.py even though resolution derives progress from state.completed and attempt enforcement imports the constant from state.py, directly violating the task's no-runner and stale-expectation criteria.",
              "evidence": [
                {
                  "command": "grep -n 'durable runner\\|last completed event\\|MAX_ATTEMPTS' skills/anomaly/SKILL.md",
                  "output": "46: `.anomaly/receipts/`, and `.anomaly/attempts/`. The durable runner records each; 232: resumes from file presence instead of the last completed event; 247: `MAX_ATTEMPTS` ... `src/anomaly/workflow.py` (the runner refuses any other value)."
                },
                {
                  "command": "read src/anomaly/workflow.py:1-93,94-210 and src/anomaly/owners.py:30-65",
                  "output": "P5 maps to owner (`skill`, `anomaly`); `_OWNER_INSTRUCTIONS` maps that owner to `skills/anomaly/SKILL.md`; `invoke_resolved_owner` reads the entire file as instructions; `consume_owner` invokes it for P5. `resolve_workflow` calls `completed_phase(snapshot)` to choose the next phase, and `run_workflow` delegates execution to `run_attempts`."
                },
                {
                  "command": "read src/anomaly/state.py:1-35 and src/anomaly/attempts.py:1-125",
                  "output": "`MAX_ATTEMPTS = 3` is defined in state.py; `completed_phase` walks snapshot[`completed`]. attempts.py imports MAX_ATTEMPTS and enforces `while attempt < MAX_ATTEMPTS`, then records success in state[`completed`]; events are appended after state/promotion logic and are not consulted for resume."
                },
                {
                  "command": "grep -n 'No shared runner\\|stale expectations\\|Durable attempt staging' .jeff/tasks/0027-resolver-consolidation/task.md",
                  "output": "11: durable attempt staging and retries remain separate execution utilities rather than a second orchestration API; 16: stale expectations are removed; 21: No shared runner."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 185,
            "severity": "low",
            "class": "follow-up",
            "kickTo": "refactor",
            "what": "run_workflow resolves every snapshot twice even after the invocation entry phase is already known.",
            "why": "The first result is used only to initialize entry_phase, yet both resolve_workflow calls run on every later loop iteration. Restrict the preliminary resolution to entry_phase is None, then perform the capability-filtered resolution once; this is a behavior-preserving simplification and removes avoidable computation."
          }
        ],
        "evidence": [
          {
            "command": "read .jeff/tasks/0027-resolver-consolidation/task.md; notes.md; context.md; task.json:1-50; journal.jsonl:70-110",
            "output": "Locked AC1-AC9 and recovery union were read. The ledger binds the clean green full-suite gate to 8500ab972190cc5d83fc37344fede3a24816dfe2 with command `uv run --extra test pytest tests/`; journal seq 86 records that gate and seq 87-89 record fresh post-recovery judgments. Supplied gate evidence reports 736/736 passed."
          },
          {
            "command": "git log --oneline --max-count=15 8500ab972190cc5d83fc37344fede3a24816dfe2; git diff --stat 9854273..8500ab9 -- src/anomaly tests PRD.md skills/anomaly/SKILL.md",
            "output": "The full reviewed lineage is 9854273 task-25 baseline → 61a2b7c → 4a1746f → 747df42 recovered candidate → 709b177 recovery tests → 8500ab9 gate-test repair. Product/test/doc diff removes product_workflow.py and rewrites workflow.py while adding attempts/workspace/owners/state/identity/input modules and focused tests."
          },
          {
            "command": "grep `WorkflowRunner|PhaseResult|RetryLimitExceeded|product_workflow|runner` across src/tests/PRD/skills/agents; read workflow.py:22-203 and owners.py:1-164",
            "output": "No removed runner/result/retry/product-workflow symbols remain in source or tests. One fixed _OWNER_REGISTRY selects all P0-P7 deterministic, skill, and persona owners and their bounded writes; consume_owner is the sole owner consumer and run_attempts the dispatcher execution utility. Remaining runner references are stale installed-skill prose, including lines 46 and 247."
          },
          {
            "command": "read identities.py:8-105; grep P7 output deletion/mutation tests",
            "output": "IDENTITY_PHASES has only `gate_b: P7`; `_identity_files` maps gate_b solely to `.anomaly/receipts/gate-b.json`. No test deletes or mutates report.md, findings.json, charts, or their receipt after successful completion."
          },
          {
            "command": "read case.py:96-170,257-290; grep `_scan_case_tree(` in src/anomaly; grep create_case/symlink tests",
            "output": "inspect_case and resume_case scan before reads; phase_event scans direct decorated writers; attempt workspaces scan post-owner. create_case resolves and writes without scanning, and no create-case symlink test exists."
          },
          {
            "command": "read workflow_inputs.py:39-119; read recommend.py:334-374; grep malformed decision/source tests",
            "output": "Capability checks validate top-level mapping/key/container shape only. Gate A element/non-empty validation and Gate B element validation occur later inside owner APIs; plan-owned workflow tests cover missing keys/timestamps but no malformed collection elements or malformed source-field types."
          },
          {
            "command": "grep promotion/interrupted/rollback/workspace in tests; read attempts.py:29-105 and _attempt_workspace.py:13-185",
            "output": "New production code creates case-shaped workspaces, journals entry-by-entry promotion, rolls back before sealing state, and exposes interrupted-promotion recovery. Tests prove pre-promotion owner-failure isolation only; no test invokes or behaviorally reaches promotion recovery/rollback."
          },
          {
            "command": "read test_workflow.py:1-124; test_pipeline_walk.py:180-909; test_case.py:227-246; test_acquire.py:188-211; test_review.py:425-456",
            "output": "AC1 purity/resume, AC2 dynamic selection, gate-turn binding, bounded redacted retries, pre-promotion isolation, P3-P6 identity invalidation, canonical role separation, Gate-B receipt invalidation, README authored-block preservation, inspect/resume/direct-writer containment, demo order/relative links, and standalone bare-URL inertness are genuine consumer-observable tests. AC8 correctly avoids symbol-absence change-detector tests."
          }
        ],
        "acLedger": [
          {
            "ac": "AC1",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC2",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          },
          {
            "ac": "AC3",
            "claimed": "revise",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC4",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC5",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC6",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          },
          {
            "ac": "AC7",
            "claimed": "write",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC8",
            "claimed": "delete",
            "rederived": "delete",
            "ok": false
          },
          {
            "ac": "AC9",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          }
        ]
      },
      "audit": {
        "required": true,
        "verdict": "needs-work",
        "audit_agent_id": "AuditAnomalyRecovered",
        "findings": [
          {
            "file": "src/anomaly/_attempt_workspace.py",
            "line": 109,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "A case-controlled promotion journal can delete arbitrary regular files or directories inside the case during public resume recovery.",
            "why": "run_workflow calls recover_interrupted_promotion after the whole-tree scan, but _read_journal validates only schema_version, relative path syntax, and the phase/attempt spelling. It does not bind entries to the fixed phase write set or validate each entry's original/status transition. A portable case can supply a regular .anomaly/promotion.json with an unsealed P0-P7 attempt, an entry such as {\"path\":\"README.md\",\"original\":false,\"status\":\"applied\"}, and no backup. _rollback then reaches discard_workspace(live) and removes README.md before rewinding state. The same path can target data/raw, instructions, or other in-case evidence, making this reachable case-data loss rather than a path-escape false positive.",
            "cwe": "CWE-73",
            "refute": {
              "agent_id": "AnPostRefute9",
              "source": "audit",
              "finding": "src/anomaly/_attempt_workspace.py:109 A case-controlled promotion journal can delete arbitrary regular files or directories inside the case during public resume recovery.",
              "verdict": "survives",
              "rationale": "The blocker is reachable: run_workflow accepts a portable case whose promotion journal and target are ordinary files, then recovery trusts the journal's unbound original/status fields, and an unsealed applied entry with original=false and no backup causes _rollback to call discard_workspace on the attacker-selected in-case path.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/workflow.py:158-166",
                  "output": "run_workflow calls resume_case(case_root), resolves the root, and then calls recover_interrupted_promotion(case_root) before loading the workflow snapshot."
                },
                {
                  "command": "read anomaly/src/anomaly/case.py:257-290",
                  "output": "_scan_case_tree rejects symlinks, non-regular artifacts, and executable files, but accepts ordinary .anomaly/promotion.json and README.md files; it does not validate promotion-journal semantics."
                },
                {
                  "command": "read anomaly/src/anomaly/_attempt_workspace.py:57-69,109-119,159-193",
                  "output": "Recovery reads the journal, treats a non-sealed/non-rolled-back journal as rollback work, and _rollback skips only status=pending; when backup.exists() is false and entry['original'] is false, it calls discard_workspace(live). _read_journal validates schema_version, entry object/list shape, relative path syntax, and phase/attempt path spelling, but does not bind paths to _OWNER_REGISTRY write sets or validate original/status transitions."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/semantics.py",
            "line": 52,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The shared durable-error sanitizer misses standard compound credential assignments such as AWS_SECRET_ACCESS_KEY and OPENAI_API_KEY.",
            "why": "_SECRET_ASSIGNMENT requires a word boundary immediately before api_key, secret, or token. In environment-style names the preceding underscore is a word character, so the match never starts; _TOKEN_PREFIX catches only a small prefix list. The direct probe returned AWS_SECRET_ACCESS_KEY=abcdEFGHijklMNOPqrstUVWXyz0123456789ABCD unchanged. A dynamic P5/P6 owner exception containing that standard configuration spelling flows through safe_error into failure.json, events.jsonl, state.failures, and blocked_reason for all three attempts. The recovered test covers an AKIA-prefixed access-key ID labeled only aws=, not an AWS secret-access-key assignment, so durable secret exposure remains reachable.",
            "cwe": "CWE-532",
            "refute": {
              "agent_id": "AnPostRefute10",
              "source": "audit",
              "finding": "src/anomaly/semantics.py:52 The shared durable-error sanitizer misses standard compound credential assignments such as AWS_SECRET_ACCESS_KEY and OPENAI_API_KEY.",
              "verdict": "survives",
              "rationale": "The blocker survives because the gated sanitizer's leading word boundary cannot start at SECRET or API after an underscore, no other sanitizer pattern covers these compound assignment names, and real P5/P6 invoker exceptions are caught and copied through safe_error into every named durable failure surface without an upstream guard.",
              "evidence": [
                {
                  "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/semantics.py",
                  "output": "58:_SECRET_ASSIGNMENT = re.compile(\n59:    r\"(?i)\\b(api[_-]?key|access[_-]?token|auth(?:entication)?|authorization|\"\n60:    r\"credential|password|passwd|secret|token|private[_-]?key)\"\n61:    r\"\\s*[:=]\\s*(?:Bearer\\s+)?[^,;\\s]+\"\n63:_BEARER = re.compile(r\"(?i)\\bBearer\\s+[A-Za-z0-9._~+/=-]{8,}\")\n64:_TOKEN_PREFIX = re.compile(\n65:    r\"\\b(?:sk_live_|ghp_|github_pat_|xox[baprs]-|AKIA)[A-Za-z0-9_./+=-]{8,}\"\n67:_URL_USERINFO = re.compile(r\"(?i)\\b(https?://)[^/\\s:@]+:[^@\\s/]+@\")"
                },
                {
                  "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/workflow.py && git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/attempts.py",
                  "output": "workflow.py:155:return invoke(owner=selected, instructions=instructions, case_root=Path(case_root)); workflow.py:198-206 passes consume_owner into run_attempts. attempts.py:63-68 catches Exception from execute(workspace) and sends it to _finish_failure."
                },
                {
                  "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/state.py && git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:src/anomaly/attempts.py",
                  "output": "state.py:122-127 safe_error returns str(redact_credentials(str(value)))[:2000]. attempts.py:172-196 assigns that message to failure.json, phase_failed events.jsonl, state.failures, and final-attempt blocked_reason; lines 197-205 also emit it in phase_unavailable."
                },
                {
                  "command": "git -C anomaly show 8500ab972190cc5d83fc37344fede3a24816dfe2:tests/test_pipeline_walk.py",
                  "output": "test_failed_reasoning_attempts_redact_credentials_from_all_durable_evidence covers Authorization: Bearer, password=, AKIA under aws=, xoxb under slack=, and URL userinfo, but contains no AWS_SECRET_ACCESS_KEY or OPENAI_API_KEY assignment."
                }
              ]
            }
          }
        ],
        "evidence": [
          {
            "command": "read /Users/tomvaillant/buried_signals/tools/anomaly/scratchpads/security-recovered/security-audit-20260824-105650.md",
            "output": "Scanner covered 25 src/anomaly files, reported six high regex hits, two coverage gaps, zero audit-tool failures, and REVIEW. The six hits were one ast.literal_eval and five interpolated DuckDB statements; GitHub Actions and XSS were the two scanner gaps."
          },
          {
            "command": "read src/anomaly/detect.py:92-129,227-388,583-660; src/anomaly/prepare.py:459-495; src/anomaly/profile.py:80-220",
            "output": "All six machine hits are refuted as exploits. ast.literal_eval parses only quoted scalar literals and cannot execute code. Detector memory_mb and threads are type-checked positive integers before interpolation. Prepared table IDs are generated as tbl_ plus a SHA-256 hex digest and view names come from enumerate. Profile table IDs must match tbl_[0-9a-f]{64} before interpolation. Detector SQL is fixed package-owned SELECT-only text, rejects comments, extra statements, mutating words, implicit file relations, and external reader functions; DuckDB opens the index read-only with external access disabled."
          },
          {
            "command": "read src/anomaly/_attempt_workspace.py:57-69,109-145,166-205 and src/anomaly/workflow.py:158-176",
            "output": "Public dispatch scans through resume_case and then immediately invokes recovery. Recovery accepts journal-controlled relative entries without binding them to _OWNER_REGISTRY writes. For every non-pending entry, rollback restores a backup when present, otherwise deletes the live target whenever journal.original is false. The parser does not validate original/status fields or require a genuine backup/workspace, confirming the crafted-journal deletion path."
          },
          {
            "command": "uv run python -c 'from anomaly.semantics import redact_credentials; print(redact_credentials(\"AWS_SECRET_ACCESS_KEY=abcdEFGHijklMNOPqrstUVWXyz0123456789ABCD\"))'",
            "output": "AWS_SECRET_ACCESS_KEY=abcdEFGHijklMNOPqrstUVWXyz0123456789ABCD"
          },
          {
            "command": "read src/anomaly/semantics.py:52-119; src/anomaly/state.py:73-127,164-181; src/anomaly/attempts.py:138-204; tests/test_pipeline_walk.py:443-481",
            "output": "The unchanged credential string reaches the common safe_error/safe_json boundary used by attempt failure files, API/workflow events, state failures, and blocked_reason. The recovered test proves Bearer, password, AKIA, xoxb, and URL-userinfo forms, but its AWS value is prefix-detected and does not exercise compound environment-variable names or an unprefixed secret-access-key value."
          },
          {
            "command": "read src/anomaly/case.py:139-180,257-378; src/anomaly/events.py:25-80; src/anomaly/attempts.py:29-104; src/anomaly/acquire.py:31-132; tests/test_case.py:227-245; tests/test_acquire.py:188-210; tests/test_pipeline_walk.py:827-909",
            "output": "The recovered containment repairs otherwise hold: public inspect/resume and every phase_event-decorated mainline writer perform a whole-tree no-link/no-special-file scan before access; direct source registration also resumes through that scan; dynamic owners receive a staged case-shaped workspace; the workspace is rescanned before output or promotion; unsafe links escape without failure writes; and only declared artifacts are promoted with state sealed last. Tests exercise public readers, direct destination links, durable namespace links, and a link introduced by a dynamic owner."
          },
          {
            "command": "read src/anomaly/workflow_inputs.py:24-119; src/anomaly/workflow.py:85-228; src/anomaly/owners.py:37-164; src/anomaly/identities.py:8-116; src/anomaly/state.py:20-181",
            "output": "Schema-aware capabilities require a timezone-aware clock and complete gate mappings; entry-phase binding discards future Gate A/B capabilities; fixed dynamic owners load only installed skill/persona paths; canonical_key enforces reviewer separation; and changed_identities derives every required identity through the completed phase, invalidating missing or changed artifacts from the earliest owner phase."
          },
          {
            "command": "read src/anomaly/review.py:589-658,1030-1117; src/anomaly/report.py:221-280; src/anomaly/readme.py:1-65; tests/test_review.py:425-463; tests/test_pipeline_walk.py:319-382,748-826",
            "output": "The XSS gap is manually covered with no finding: dataset claim text is credential-redacted, whitespace-collapsed, HTML-escaped, Markdown metacharacter-escaped, and HTTP(S) colons are neutralized to prevent bare autolinks; SVG labels/titles are XML-escaped. README mutation removes only bounded anomaly markers and tests preserve exact journalist-authored legacy-shaped Outputs content across completion, invalidation, P7 failure, and recompletion while positively resolving relative links."
          },
          {
            "command": "read pyproject.toml; read implement-recovery-return.json; supplied clean gate evidence",
            "output": "Runtime dependencies remain the existing pyarrow, duckdb, and pytz set; recovery changed no dependency manifest. The scanner reported no dependency vulnerabilities and zero tool failures. GitHub Actions is outside the supplied src/anomaly scan/change scope, while its other uncovered category, XSS, was covered manually. The authoritative clean gated hash is 8500ab972190cc5d83fc37344fede3a24816dfe2 with the supplied full-suite result 736/736."
          }
        ],
        "reportedVerdict": "needs-work",
        "scan": {
          "command": "/Users/tomvaillant/.omp/plugins/node_modules/@johanthoren/jeff/skills/security-auditor/scripts/review-security.sh src/anomaly --report-dir scratchpads/security-recovered",
          "recommendation": "REVIEW",
          "reportPath": "/Users/tomvaillant/buried_signals/tools/anomaly/scratchpads/security-recovered/security-audit-20260824-105650.md"
        },
        "coverage": [
          {
            "category": "secrets",
            "status": "covered_with_hits"
          },
          {
            "category": "injection_sql",
            "status": "covered_no_hits"
          },
          {
            "category": "injection_command",
            "status": "covered_no_hits"
          },
          {
            "category": "path_traversal",
            "status": "covered_with_hits"
          },
          {
            "category": "insecure_deserialization",
            "status": "covered_no_hits"
          },
          {
            "category": "weak_crypto",
            "status": "covered_no_hits"
          },
          {
            "category": "dynamic_execution",
            "status": "covered_no_hits"
          },
          {
            "category": "tls_transport",
            "status": "covered_no_hits"
          },
          {
            "category": "xss",
            "status": "covered_no_hits"
          },
          {
            "category": "sensitive_logging",
            "status": "covered_with_hits"
          },
          {
            "category": "insecure_permissions",
            "status": "covered_no_hits"
          }
        ]
      },
      "agents": {
        "reviewer_agent_id": "ReviewAnomalyRecoveredCorrect",
        "reviewer2_agent_id": "ReviewAnomalyRecoveredStandards",
        "audit_agent_id": "AuditAnomalyRecovered"
      }
    },
    {
      "at": "2026-08-24T14:06:43Z",
      "review": {
        "verdict": "needs-work",
        "reportedVerdict": "needs-work",
        "reviewer_agent_id": "ReviewAnomalyFinalCorrect",
        "findings": [
          {
            "file": "src/anomaly/_attempt_workspace.py",
            "line": 262,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "Registry binding still lets an untrusted promotion journal delete a legitimate phase-owned artifact by lying that it did not originally exist.",
            "why": "For a P1 journal, `data/sources.json` is in the exact registry write set. An entry `{path: \"data/sources.json\", original: false, status: \"applied\"}` with no backup passes `_validate_recovery_artifacts` because the non-original applied branch checks only that the backup is absent. The other required entries can truthfully be pending/original and make the complete journal schema-valid. Public recovery then reaches `_rollback` line 128 and recursively deletes the live target before rewinding. Binding deletion to the owner registry narrows the prior exploit but does not eliminate reachable case-data loss or satisfy the locked rule to reject unverifiable journals without mutation. Add a public-restart regression for a fixed-write-set target with a forged boolean `original`, then redesign recovery so rollback authority is verifiable rather than journal-asserted.",
            "refute": {
              "agent_id": "AnFinalRefute1",
              "source": "review",
              "finding": "src/anomaly/_attempt_workspace.py:262 Registry binding still lets an untrusted promotion journal delete a legitimate phase-owned artifact by lying that it did not originally exist.",
              "verdict": "survives",
              "rationale": "The public dispatcher accepts a regular-file portable case containing a forged but schema-valid P1 journal, and no upstream guard independently establishes the target's pre-promotion existence before the non-original applied branch authorizes deletion.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:22-83,158-176; src/anomaly/case.py:139-151,257-295",
                  "output": "P1's fixed registry write set includes data/sources.json; run_workflow calls resume_case and then recover_interrupted_promotion, while resume_case's tree scan rejects links/special/executable files but does not authenticate promotion-journal contents or artifact provenance."
                },
                {
                  "command": "read src/anomaly/case.py:37-62,102-124",
                  "output": "create_case creates data/raw and .anomaly/receipts and writes legitimate data/sources.json and .anomaly/events.jsonl, so all four P1 live targets exist before P1."
                },
                {
                  "command": "read src/anomaly/_attempt_workspace.py:166-264",
                  "output": "_read_journal accepts an exact P1 path set with boolean original values and allowed entry statuses; for a prepared entry with original=false and status=applied, _validate_recovery_artifacts declares it coherent solely when the backup is absent, without checking the existing live target."
                },
                {
                  "command": "read src/anomaly/_attempt_workspace.py:57-70,109-136",
                  "output": "An unsealed public recovery calls _rollback; for the forged applied data/sources.json entry, the absent backup and original=false branch calls discard_workspace(live), deleting the legitimate file, before _rewind_attempt and cleanup."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/case.py",
            "line": 166,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The public fork writer still follows a symlinked destination ancestor and can create the child case outside the caller-selected path.",
            "why": "`fork_case` resolves `dest` before any destination scan and only scans the source; line 179 copies directly to the resolved destination. A destination such as `alias/child`, where `alias` is a symlink to an external directory, is converted to the external target and populated there. Existing fork containment tests cover symlinks inside the source case, not destination-root or destination-ancestor links. This is a reachable path escape and leaves AC7's every public read/write path incomplete. Add root/ancestor destination-link tests and scan the unresolved prospective destination before resolve or copy.",
            "refute": {
              "agent_id": "AnFinalRefute2",
              "source": "review",
              "finding": "src/anomaly/case.py:166 The public fork writer still follows a symlinked destination ancestor and can create the child case outside the caller-selected path.",
              "verdict": "survives",
              "rationale": "The public fork_case entry point resolves the caller-supplied destination before validation, scans only the source, and passes the resolved path directly to shutil.copytree, so an existing symlink ancestor is followed and a nonexistent child is created under the ancestor's external target with no later guard to restore caller-selected containment.",
              "evidence": [
                {
                  "command": "read anomaly/.git/HEAD:raw",
                  "output": "429c34deae1b9f5a92252147ec16acebfcb4812b"
                },
                {
                  "command": "read anomaly/src/anomaly/case.py:157-181",
                  "output": "fork_case performs `dest = Path(dest).resolve()`, then `_scan_case_tree(source)`, and later `shutil.copytree(source, dest)`; there is no scan or containment check on the unresolved destination."
                },
                {
                  "command": "read anomaly/src/anomaly/case.py:257-286",
                  "output": "_scan_case_tree would reject a symlink in the path argument's ancestor chain via lstat, but fork_case invokes this guard only for source after dest has already been resolved."
                },
                {
                  "command": "read anomaly/tests/test_case.py:320-450",
                  "output": "The fork symlink tests exercise links within the source tree and a symlinked source alias; they do not exercise a symlinked destination root or destination ancestor."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow_inputs.py",
            "line": 168,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "Whitespace-only Gate A and Gate B IDs are still classified as complete human decisions.",
            "why": "The gate sequence predicate rejects `not item` but not `not item.strip()`, even though it correctly applies nonblank validation to the actor. The direct pure probe returned both `gate_a` and `gate_b` capabilities for `approved_ids=['   ']` and `accepted_claim_ids=['   ']`. Gate A then spends attempts rejecting an unknown detector, while Gate B can silently promote no claims and complete because unknown requested claims are ignored. This contradicts the complete value-schema and no-attempt human-boundary contract. Extend both public value tables with whitespace-only IDs and reject them during capability derivation.",
            "refute": {
              "agent_id": "AnFinalRefute3",
              "source": "review",
              "finding": "src/anomaly/workflow_inputs.py:168 Whitespace-only Gate A and Gate B IDs are still classified as complete human decisions.",
              "verdict": "survives",
              "rationale": "The public capability predicate accepts whitespace-only sequence IDs, so Gate A enters bounded execution and consumes attempts on an unknown detector while Gate B can ignore the unknown claim, seal an empty accepted set, and complete instead of pausing at the human boundary.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow_inputs.py:39-57,116-174",
                  "output": "input_capabilities adds gate_a/gate_b whenever gate_a_input/gate_b_input returns; _gate_input rejects sequence members only when they are non-strings, empty strings, or (for Gate A) contain separators, and never checks item.strip(), although the actor is explicitly checked with actor.strip(). Thus ['   '] is phase-consumable for both gates."
                },
                {
                  "command": "read src/anomaly/workflow.py:85-128,158-205; src/anomaly/owners.py:49-72,135-145; src/anomaly/attempts.py:29-75",
                  "output": "P4/P7 readiness depends on the derived gate capability; run_workflow sends a ready phase into run_attempts, whose exception path records and retries attempts. P4 passes the whitespace ID to approve_detector_plan, while P7 passes it to accept_findings, so no upstream guard restores the required no-attempt pause."
                },
                {
                  "command": "read src/anomaly/recommend.py:335-379; src/anomaly/review.py:366-474",
                  "output": "approve_detector_plan rejects an ID absent from the recommended detector set, making whitespace-only Gate A fail inside an attempt. accept_findings builds by_id from real claims and continues when claim is missing; it then writes findings and the Gate B receipt from the promoted list, so a whitespace-only requested claim can produce an empty accepted set and complete P7."
                },
                {
                  "command": "read tests/test_pipeline_walk.py:378-431; .jeff/tasks/0027-resolver-consolidation/task.md:7-15; .jeff/tasks/0027-resolver-consolidation/context.md:58-90",
                  "output": "The public invalid-value tables cover empty/non-string/duplicate/unsafe/over-limit Gate A values and non-string/duplicate Gate B values, but omit whitespace-only IDs. The locked contract preserves Gate A/Gate B turn boundaries, and the context states human-gate pauses consume no phase attempt."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 605,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The interrupted-attempt test never exercises restart reconciliation for a counted attempt with no failure evidence.",
            "why": "Its interrupt is `KeyboardInterrupt`, which `run_attempts` catches in the `except BaseException` branch and immediately records through `_finish_failure` before re-raising. Every subsequent invocation therefore sees an already evidenced failure; `_reconcile_interrupted_attempt`'s missing-evidence path is never reached. The test can stay green if hard termination once again strands attempt three. AC3 explicitly requires finite, evidenced recovery after an interruption, so a deterministic crash/restart seam—such as a subprocess hard exit after the durable counter write—is owed.",
            "refute": {
              "agent_id": "AnFinalRefute4",
              "source": "review",
              "finding": "tests/test_pipeline_walk.py:605 The interrupted-attempt test never exercises restart reconciliation for a counted attempt with no failure evidence.",
              "verdict": "survives",
              "rationale": "The test's KeyboardInterrupt is always caught by run_attempts' BaseException handler and persisted by _finish_failure before control returns, so the next invocation finds recorded evidence and cannot enter the missing-evidence reconciliation branch; no upstream guard or alternate assertion makes the claimed restart failure unreachable.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/attempts.py:29-125",
                  "output": "run_attempts durably increments and writes the attempt count, but both execution and promotion wrap BaseException with _finish_failure(...) before re-raising; _reconcile_interrupted_attempt only synthesizes interrupted failure evidence when no matching recorded failure exists."
                },
                {
                  "command": "read anomaly/src/anomaly/attempts.py:220-275",
                  "output": "_finish_failure calls _record_failure, which writes failure.json, appends phase_failed, adds the failure to state, and writes state before returning."
                },
                {
                  "command": "read anomaly/tests/test_pipeline_walk.py:605-641",
                  "output": "The test raises KeyboardInterrupt three times through run_attempts, then asserts failures 1, 2, and 3 and each failure.json exist; it never creates a durable counted attempt lacking failure evidence or restarts from such a state."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 644,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The claimed valid interrupted-promotion test constructs a journal state the sequential promoter cannot produce.",
            "why": "`P1_PROMOTION_WRITES` orders `.anomaly/events.jsonl` last, but the fixture marks only that last entry `applied` while every earlier entry remains `pending`. `_apply` processes entries in order, so the event artifact cannot be applied until all preceding entries are applied. The test therefore proves recovery from a fabricated transition rather than a real interrupted promotion and can miss rollback regressions on genuine applied-prefix states. Revise the fixture or use controlled interruption so the journal, backups, live artifacts, and staged workspace match a reachable promoter checkpoint.",
            "refute": {
              "agent_id": "AnFinalRefute5",
              "source": "review",
              "finding": "tests/test_pipeline_walk.py:644 The claimed valid interrupted-promotion test constructs a journal state the sequential promoter cannot produce.",
              "verdict": "survives",
              "rationale": "The producer preserves the P1 write order and `_apply` advances sequentially, persisting each entry as `applied` before touching the next, so a journal with only the final events entry applied is unreachable; the fixture also supplies only that entry's backup rather than the applied-prefix artifacts a genuine interruption would leave, and journal validation contains no prefix-order guard that could make this fabricated state representative.",
              "evidence": [
                {
                  "command": "read anomaly/tests/test_pipeline_walk.py:644-672",
                  "output": "The fixture creates canonical-order entries, creates only `promotion-backup/.anomaly/events.jsonl`, mutates the live events file, then executes `entries[-1][\"status\"] = \"applied\"`; all three preceding entries remain `pending`."
                },
                {
                  "command": "read anomaly/src/anomaly/workflow.py:8-28 and anomaly/src/anomaly/_attempt_workspace.py:25-47,82-104",
                  "output": "P1 writes are ordered `data/sources.json`, `data/raw`, `.anomaly/receipts`, `.anomaly/events.jsonl`; `promote_workspace` creates entries in validated write order, and `_apply` loops in that order, persisting `applying`, moving artifacts, then persisting `applied` before advancing."
                },
                {
                  "command": "read anomaly/src/anomaly/_attempt_workspace.py:166-223,244-270",
                  "output": "Journal parsing validates allowed per-entry statuses and write-path set equality but does not require statuses to form an applied/applying prefix; artifact validation consequently accepts the test's pending-prefix/applied-last construction when only the events backup exists."
                }
              ]
            }
          },
          {
            "file": "tests/test_pipeline_walk.py",
            "line": 196,
            "severity": "medium",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The P7 identity matrix does not prove that a changed chart descendant invalidates completion.",
            "why": "The matrix mutates the three files and charts receipt and removes the entire charts directory, but never changes, adds, removes, or changes the kind of a descendant inside `findings/charts/`. Descendant content is a distinct consumer-observable part of the locked completion identity; directory removal alone would pass with an implementation that hashes only directory existence. Add at least one completed-case chart-descendant mutation and require P7 invalidation plus README demotion.",
            "refute": {
              "agent_id": "AnFinalRefute6",
              "source": "review",
              "finding": "tests/test_pipeline_walk.py:196 The P7 identity matrix does not prove that a changed chart descendant invalidates completion.",
              "verdict": "survives",
              "rationale": "The completed-case matrix only deletes the whole charts directory; because that mutation also fails an existence-only implementation, it does not exercise the distinct descendant path that production hashes by relative path, kind, and file content, and no other test requires a descendant mutation to cause P7 invalidation plus README demotion.",
              "evidence": [
                {
                  "command": "read anomaly/tests/test_pipeline_walk.py:196-215",
                  "output": "P7_OUTPUT_MUTATIONS mutates findings.json, report.md, unresolved.md, removes findings/charts with shutil.rmtree, and mutates charts.json; it never changes, adds, removes, or changes the kind of a charts descendant."
                },
                {
                  "command": "read anomaly/tests/test_pipeline_walk.py:1081-1101",
                  "output": "test_changed_or_missing_p7_output_demotes_completion_and_readme applies only P7_OUTPUT_MUTATIONS, then asserts paused at gate_b, invalidated_from == P7, Status: active, Last completed phase: P6, and removal of the outputs block."
                },
                {
                  "command": "read anomaly/src/anomaly/identities.py:78-105",
                  "output": "_completion_identity separately requires findings/charts to be a directory, recursively enumerates descendants, rejects symlinks and unsupported kinds, hashes directory markers, and hashes each descendant file's relative path, kind, and bytes."
                },
                {
                  "command": "grep 'findings[\"\\' /]+charts|charts_dir|charts_path|_chart_files' anomaly/tests",
                  "output": "The only pipeline-walk mutations are whole-directory removal and replacing charts with a non-directory before initial P7; test_report.py exercises chart generation/idempotence and stale-file cleanup, but no test mutates a completed chart descendant and asserts workflow invalidation or README demotion."
                }
              ]
            }
          }
        ],
        "evidence": [
          {
            "command": "git rev-parse HEAD; git status --short",
            "output": "HEAD is the requested gated hash 429c34deae1b9f5a92252147ec16acebfcb4812b. Only Jeff recording files and scratchpads/security-final are dirty/untracked; reviewed production, tests, PRD, and installed skill match the checkpoint."
          },
          {
            "command": "uv run --extra test pytest tests/ (supplied clean gate evidence)",
            "output": "766/766 passed. This establishes suite greenness but does not cover the missing or invalid test seams identified above."
          },
          {
            "command": "read task.md, notes.md, context.md, bundled code-standards and testing skills, and the full task lineage diff 9854273..429c34d",
            "output": "Re-derived all nine locked AC dispositions. The full change keeps one fixed P0-P7 registry, dynamic P5/P6 loading, focused attempt/promotion utilities, identity invalidation, README projection, containment preflights, and deletion of product_workflow.py and stale runner/verb/event-resume prose."
          },
          {
            "command": ".venv/bin/python -c '<input_capabilities whitespace gate probe>'",
            "output": "['gate_a', 'gate_b'] for whitespace-only selected IDs, confirming incomplete value validation at the pre-attempt boundary."
          },
          {
            "command": "read src/anomaly/_attempt_workspace.py:109-264 and src/anomaly/workflow.py:22-83",
            "output": "The parser enforces exact registry paths and boolean `original`, but for a non-original applied entry validates only absence of a backup; rollback then deletes the live artifact. P1's fixed set includes existing `data/sources.json` and `data/raw` targets."
          },
          {
            "command": "read src/anomaly/case.py:159-180; grep fork/symlink tests",
            "output": "`fork_case` calls `Path(dest).resolve()` and `shutil.copytree` without scanning the unresolved destination. Current fork symlink tests cover links in the source case only."
          },
          {
            "command": "read src/anomaly/attempts.py:42-180 and tests/test_pipeline_walk.py:605-640",
            "output": "`KeyboardInterrupt` is caught, cleaned, and persisted immediately by the BaseException path; the test never produces an incremented durable attempt without failure evidence and therefore never tests restart reconciliation."
          },
          {
            "command": "grep P7_OUTPUT_MUTATIONS and chart descendant mutations across tests",
            "output": "The P7 table covers findings.json, report.md, unresolved.md, deletion of findings/charts, and charts.json; no test mutates any descendant within the charts tree."
          },
          {
            "command": "grep obsolete orchestration symbols/prose; glob tests/fixtures/*demo*.csv; read canonical demo test",
            "output": "WorkflowRunner, PhaseResult, RetryLimitExceeded, product_workflow, stale event-resume prose, and the obsolete Verbs section are absent. Exactly one orchestration_demo.csv remains, and the end-to-end test exercises dynamic skill/persona loading, both human turns, completion, portable links, restart, and P5→P6→Gate B ordering."
          }
        ],
        "acLedger": [
          {
            "ac": "AC1",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC2",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          },
          {
            "ac": "AC3",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC4",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC5",
            "claimed": "revise",
            "rederived": "revise",
            "ok": false
          },
          {
            "ac": "AC6",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          },
          {
            "ac": "AC7",
            "claimed": "revise",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC8",
            "claimed": "delete",
            "rederived": "delete",
            "ok": true
          },
          {
            "ac": "AC9",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          }
        ]
      },
      "review2": {
        "verdict": "needs-work",
        "reportedVerdict": "needs-work",
        "reviewer_agent_id": "ReviewAnomalyFinalStandards",
        "findings": [
          {
            "file": "src/anomaly/case.py",
            "line": 166,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "The public fork writer follows a symlinked destination namespace and can copy a case outside the caller's requested tree.",
            "why": "fork_case resolves dest before any destination containment scan. If an ancestor such as alias/ is a symlink to an external directory and alias/child does not yet exist, Path.resolve() converts it to the external path and shutil.copytree writes the full case there. Existing fork containment tests validate the source namespace only; none supplies a symlinked destination. This is a reachable path escape and violates AC7's every-public-read/write no-symlink boundary. Add a consumer test for a root or ancestor destination symlink proving no external write, then scan the unresolved destination before resolve or copy.",
            "refute": {
              "agent_id": "AnFinalRefute7",
              "source": "review2",
              "finding": "src/anomaly/case.py:166 The public fork writer follows a symlinked destination namespace and can copy a case outside the caller's requested tree.",
              "verdict": "survives",
              "rationale": "The public entry point resolves the caller-supplied destination before scanning only the source, so an existing symlink ancestor is converted into an external destination and no later guard restores containment to the requested namespace.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/case.py:150-181",
                  "output": "fork_case assigns `dest = Path(dest).resolve()`, calls `_scan_case_tree(source)` only, and then calls `shutil.copytree(source, dest)`; there is no destination scan between resolution and the copy."
                },
                {
                  "command": "read anomaly/src/anomaly/case.py:334-381",
                  "output": "Downstream `_under_root` checks paths relative to the already-resolved `root`; once `dest` has become the external path, these checks accept writes inside that external path rather than enforcing the caller's original namespace."
                },
                {
                  "command": "read anomaly/tests/test_case.py:414-441",
                  "output": "The existing fork symlink test passes a symlink as `source` and a normal `dest`; it proves source rejection but provides no guard or coverage for a symlinked destination ancestor."
                },
                {
                  "command": "grep 429c34deae1b9f5a92252147ec16acebfcb4812b anomaly/.jeff/tasks/0027-resolver-consolidation/task.json",
                  "output": "The task gate records hash `429c34deae1b9f5a92252147ec16acebfcb4812b` with `clean: true` and `green: true`, confirming the inspected final-cycle checkpoint."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/_attempt_workspace.py",
            "line": 112,
            "severity": "high",
            "class": "blocking",
            "kickTo": "plan",
            "what": "A second interruption during promotion rollback leaves a valid recovery state that the next restart rejects permanently.",
            "why": "_rollback restores an original artifact with backup.replace(live), but it does not update that entry's applied status or persist rollback progress until every entry is restored and the journal is finally marked rolled_back. If the process stops after one restore, restart sees status prepared/promoted plus an original/applied entry whose live file exists and backup is absent. _validate_recovery_artifacts at line 259 accepts that shape only for status applying, so it raises WorkflowError instead of continuing the idempotent rollback. The case can remain partially promoted and cannot resume, breaking AC3's durable atomic promotion/restart contract. Add deterministic public-restart coverage for interruption during rollback and make rollback progress restart-idempotent, for example by journalizing each restored entry before advancing.",
            "refute": {
              "agent_id": "AnFinalRefute8",
              "source": "review2",
              "finding": "src/anomaly/_attempt_workspace.py:112 A second interruption during promotion rollback leaves a valid recovery state that the next restart rejects permanently.",
              "verdict": "survives",
              "rationale": "The failure is reachable because applied original entries are persisted before rollback, rollback can restore one by consuming its backup without journalizing that entry, and every later public restart deterministically rejects the resulting original/applied/live-without-backup state before rollback can resume.",
              "evidence": [
                {
                  "command": "read src/anomaly/_attempt_workspace.py:82-128",
                  "output": "_apply persists each entry as status \"applied\" and can persist the whole journal as \"promoted\"; _rollback then calls backup.replace(live) for each non-pending original entry but neither changes the entry status nor writes the journal inside the loop, writing status \"rolled_back\" only after every entry is restored."
                },
                {
                  "command": "read src/anomaly/_attempt_workspace.py:245-270",
                  "output": "For a non-rolled-back original entry, _validate_recovery_artifacts accepts only backup.exists() or (status == \"applying\" and live.exists()). After partial rollback of an \"applied\" entry, backup is absent and live exists, so coherent is false and WorkflowError is raised; no upstream recovery guard accepts this legitimate rollback-progress shape."
                },
                {
                  "command": "read src/anomaly/workflow.py:158-178",
                  "output": "The public run_workflow entry calls recover_interrupted_promotion before normal resolution/execution, so the inconsistent validation is reached on a real public restart and aborts that restart."
                },
                {
                  "command": "read tests/test_pipeline_walk.py:190-228,644-694",
                  "output": "P1's real promotion write set has four entries, and the existing restart test models only one applied entry with its backup still present. Multiple applied original entries make interruption after restoring an earlier applied entry reachable, while current coverage does not exercise a second interruption during rollback."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/workflow.py",
            "line": 191,
            "severity": "low",
            "class": "follow-up",
            "kickTo": "refactor",
            "what": "run_workflow still resolves the same snapshot twice on every loop iteration.",
            "why": "After entry_phase is initialized, the first resolve_workflow result is unused and is immediately overwritten at line 198. Restrict the preliminary resolution to the first iteration, then perform one capability-filtered resolution. This is a behavior-preserving deletion that supports the task's lean-architecture objective and avoids needless computation."
          },
          {
            "file": "tests/test_case.py",
            "line": 277,
            "severity": "low",
            "class": "follow-up",
            "kickTo": "plan",
            "what": "A retained test name still describes events as part of resume progress authority.",
            "why": "test_resume_reads_progress_from_state_and_events_not_reports neither reads nor asserts events, while the current PRD and skill correctly state that events are observational and never resume authority. Rename it to state-not-report wording so the test suite does not preserve a stale event-resume contract."
          }
        ],
        "evidence": [
          {
            "command": "read task.md; notes.md; context.md; plan-second-repair.json; inspect task.json gate",
            "output": "Read the locked AC1-AC9, cycle-2 dispositions, full recovery lineage, and supplied context map. HEAD is 429c34deae1b9f5a92252147ec16acebfcb4812b; the task gate binds that exact hash to `uv run --extra test pytest tests/`, clean=true and green=true. Supplied gate evidence reports 766/766 passed."
          },
          {
            "command": "git diff --stat/--name-status/--numstat 9854273..429c34d; grep owner registry and removed symbols",
            "output": "The full task lineage deletes src/anomaly/product_workflow.py and 706 lines from workflow.py, introduces focused resolver/owner/attempt/state/identity/input utilities, and removes WorkflowRunner/PhaseResult/RetryLimitExceeded/product_workflow contracts. Exactly one `_OWNER_REGISTRY` remains; one canonical demo CSV remains. Product code is +1617/-1012, tests +918/-212, and method docs +42/-52."
          },
          {
            "command": "read workflow.py:22-247, owners.py:1-165, attempts.py:1-286, workflow_inputs.py:1-186, identities.py:1-163, state.py:1-205",
            "output": "AC1/AC2 architecture is real: resolve_workflow is pure, one registry owns P0-P7 owners and write tuples, dynamic P5/P6 instructions are loaded only through the resolved owner, and attempts are a separate bounded execution utility. Input value schemas, gate-turn filtering, identity invalidation, P7 completion identity, README demotion, and compound secret sanitization are implemented at explicit boundaries."
          },
          {
            "command": "read _attempt_workspace.py:25-284; grep `rolled_back|promotion-backup|interrupted.*rollback|rollback.*interrupt|_rollback` in tests",
            "output": "Promotion apply is journaled entry-by-entry, but rollback changes filesystem entries without persisting per-entry rollback progress; recovery validation rejects an original/applied entry after its backup has already been restored. Tests contain one interrupted-apply fixture with a backup and no interruption-during-rollback case, so this consumer-observable restart edge is owed under AC3."
          },
          {
            "command": "read case.py:102-381; grep fork/symlink containment tests",
            "output": "create, inspect, resume, source reads, and owner workspaces scan for links. fork_case scans only the source and calls `Path(dest).resolve()` before `shutil.copytree`; tests cover symlinks inside or aliasing the source case, not a symlink in the destination root/ancestor. AC7 therefore remains under-tested and the public destination writer remains escapable."
          },
          {
            "command": "read test_pipeline_walk.py boundary/recovery/completion ranges; test_case.py containment ranges; test_review.py inert-markdown range; test_skill.py conformance range",
            "output": "The new source/Gate A/Gate B tables, bounded interrupted-attempt evidence, valid interrupted-promotion restart, untrusted-journal rejection, compound redaction, P7 output invalidation/README demotion, and create-time containment tests are consumer-observable and deterministic. Existing tests genuinely retain gate turns, P3-P6 invalidation, source replacement, identity separation, inert bare URLs, relative links, canonical demo order, and dynamic owner loading. No new change-detector/symbol-absence smell test was added."
          },
          {
            "command": "grep stale runner/verb/event-resume/direct-completion contracts across src, skills, tests, and PRD",
            "output": "No removed runner, result, retry, product-workflow, handler-map, or direct-report-completion surface remains. PRD and installed skill correctly make state.completed authoritative and events observational. The only stale wording found is the test name at tests/test_case.py:277; its assertions are state-only, so it is classified follow-up rather than an AC8 behavior-removal failure."
          },
          {
            "command": "read scratchpads/security-final/security-audit-20260824-121514.md",
            "output": "The supplied scanner report covers 25 source files, reports six high regex hits, two uncovered categories, no critical findings, and recommendation REVIEW. Those scanner hits are assigned to the parallel audit station; this standards review independently covered the path-containment and crash-recovery boundaries above."
          }
        ],
        "acLedger": [
          {
            "ac": "AC1",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          },
          {
            "ac": "AC2",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          },
          {
            "ac": "AC3",
            "claimed": "revise",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC4",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          },
          {
            "ac": "AC5",
            "claimed": "revise",
            "rederived": "revise",
            "ok": true
          },
          {
            "ac": "AC6",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          },
          {
            "ac": "AC7",
            "claimed": "revise",
            "rederived": "write",
            "ok": false
          },
          {
            "ac": "AC8",
            "claimed": "delete",
            "rederived": "delete",
            "ok": true
          },
          {
            "ac": "AC9",
            "claimed": "reuse",
            "rederived": "reuse",
            "ok": true
          }
        ]
      },
      "audit": {
        "required": true,
        "verdict": "needs-work",
        "audit_agent_id": "AuditAnomalyFinal",
        "findings": [
          {
            "file": "src/anomaly/_attempt_workspace.py",
            "line": 261,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "A schema-valid, registry-bound forged P7 promotion journal can still delete the case README and its journalist-authored content during restart.",
            "why": "The new parser binds entry paths to the fixed phase write set, but _validate_recovery_artifacts accepts every non-pending entry with original=false whenever no backup exists, regardless of whether the live path already exists. README.md is in P7's fixed write tuple. A portable unsealed case can therefore supply the exact P7 entry set with README.md marked original=false/status=applied and no backup while the other entries are coherent pending entries. recover_interrupted_promotion runs before snapshot resolution; _rollback reaches the no-backup/original=false branch and discard_workspace removes README.md. This is reachable user-content loss despite passing the new exact-schema, registry-set, and artifact-coherence checks.",
            "cwe": "CWE-73",
            "refute": {
              "agent_id": "AnFinalRefute9",
              "source": "audit",
              "finding": "src/anomaly/_attempt_workspace.py:261 A schema-valid, registry-bound forged P7 promotion journal can still delete the case README and its journalist-authored content during restart.",
              "verdict": "survives",
              "rationale": "The blocker is reachable: public run_workflow resumes an ordinary unsealed case and immediately recovers any journal; an exact P7 journal may mark the live README.md as applied/original=false with no backup, which passes artifact validation and causes rollback to delete the README, while the only README projector returns without recreating a missing file.",
              "evidence": [
                {
                  "command": "read src/anomaly/workflow.py:66-79,166-175",
                  "output": "P7's fixed registry write tuple includes \"README.md\"; run_workflow calls resume_case(case_root), resolves the root, then calls recover_interrupted_promotion(case_root, lambda phase: _OWNER_REGISTRY[phase][2]) before loading and resolving the workflow snapshot."
                },
                {
                  "command": "read src/anomaly/_attempt_workspace.py:195-220,237-264",
                  "output": "_read_journal requires entry paths to equal the phase write set, but for every non-pending entry with original=false, _validate_recovery_artifacts sets coherent = not backup.exists() and does not require the live path to be absent; therefore an applied/original=false README.md with no backup is accepted even when README.md exists."
                },
                {
                  "command": "read src/anomaly/_attempt_workspace.py:57-72,112-132; src/anomaly/readme.py:24-30",
                  "output": "For an unsealed journal, recover_interrupted_promotion calls _rollback. _rollback skips only pending entries; when an entry has no backup and original=false it calls discard_workspace(live), deleting the existing README.md. project_readme explicitly returns when README.md is not a file, so later P7 execution does not restore the deleted journalist-authored content."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/attempts.py",
            "line": 29,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The documented public run_attempts utility writes before enforcing whole-case no-symlink containment.",
            "why": "run_attempts begins with load_snapshot(root), which calls _ensure_durable_tree and can create .anomaly/receipts, .anomaly/attempts, state.json, and events.jsonl. It does not scan the supplied root until after create_workspace and owner execution, when it scans only the copied workspace. A case with a symlinked root or .anomaly directory therefore causes durable state and attempt writes to follow the link outside the case before any rejection. run_workflow is guarded by resume_case, but run_attempts is a separately named execution utility, is called directly by tests, and is taught by the installed skill, so the guarded dispatcher does not close this public path.",
            "cwe": "CWE-59",
            "refute": {
              "agent_id": "AnFinalRefute10",
              "source": "audit",
              "finding": "src/anomaly/attempts.py:29 The documented public run_attempts utility writes before enforcing whole-case no-symlink containment.",
              "verdict": "survives",
              "rationale": "A valid-phase direct call reaches load_snapshot before any containment check, and load_snapshot creates durable paths through a symlinked root or .anomaly directory; the only later scan examines a copied workspace after owner execution, so no upstream guard prevents the documented public utility from writing outside the case and the high CWE-59 severity is honest.",
              "evidence": [
                {
                  "command": "git rev-parse HEAD",
                  "output": "429c34deae1b9f5a92252147ec16acebfcb4812b"
                },
                {
                  "command": "read src/anomaly/attempts.py:29-103; read src/anomaly/state.py:25-48,184-205",
                  "output": "run_attempts validates only that phase is known and immediately calls load_snapshot(root). load_snapshot immediately calls _ensure_durable_tree(root), which mkdirs root/.anomaly/receipts and attempts, writes state.json through a temporary file plus replace, and touches events.jsonl; none of these operations performs lstat or calls _scan_case_tree."
                },
                {
                  "command": "read src/anomaly/attempts.py:55-84; read src/anomaly/_attempt_workspace.py:10-23; read src/anomaly/case.py:257-299",
                  "output": "After durable state and attempt-path writes, run_attempts calls create_workspace and execute, then calls _scan_case_tree(workspace), not _scan_case_tree(root). create_workspace uses shutil.copytree(root, workspace) without symlinks=True, so the later scan sees the copied target content rather than protecting the caller-supplied symlink path; _scan_case_tree itself would reject root/ancestor and descendant links if it had been called before the writes."
                },
                {
                  "command": "read src/anomaly/workflow.py:158-210; grep -n 'run_attempts' tests/test_pipeline_walk.py skills/anomaly/SKILL.md",
                  "output": "run_workflow is protected by resume_case before it calls run_attempts, but run_attempts has no equivalent guard. tests/test_pipeline_walk.py imports anomaly.attempts.run_attempts directly and calls it at lines 623 and 625, while the installed skill explicitly documents run_attempts as the durable attempt executor, confirming a separately reachable utility path rather than a dispatcher-only private helper."
                }
              ]
            }
          },
          {
            "file": "src/anomaly/events.py",
            "line": 25,
            "severity": "high",
            "class": "blocking",
            "kickTo": "implement",
            "what": "The standalone public log_event writer follows case-controlled symlinks outside the case.",
            "why": "phase_event wrappers now scan the case before their wrapped operation, but log_event itself remains directly callable and constructs case_root/.anomaly/events.jsonl with mkdir/open without _scan_case_tree or an owned-path check. Its tests explicitly exercise direct calls, including a missing case. If case_root or .anomaly is a symlink, the append succeeds against the external target and is not caught as an error, violating the acceptance criterion that every public read/write path enforce whole-case containment and enabling external-file corruption.",
            "cwe": "CWE-59",
            "refute": {
              "agent_id": "AnFinalRefute11",
              "source": "audit",
              "finding": "src/anomaly/events.py:25 The standalone public log_event writer follows case-controlled symlinks outside the case.",
              "verdict": "survives",
              "rationale": "The finding is reachable and high severity: direct callers bypass the only `_scan_case_tree` invocation, while `mkdir` and append-open follow a symlinked case root or `.anomaly` directory and the broad exception handler does not fail safe when that redirected write succeeds.",
              "evidence": [
                {
                  "command": "read anomaly/src/anomaly/events.py:25-68",
                  "output": "`log_event` builds `Path(case_root) / \".anomaly\" / \"events.jsonl\"`, calls `path.parent.mkdir(parents=True, exist_ok=True)`, and opens it with mode `a` at lines 39-43; it never calls `_scan_case_tree`. The only `_scan_case_tree(Path(root))` call is in the separate `phase_event` wrapper at line 62."
                },
                {
                  "command": "read anomaly/tests/test_events.py:278-305",
                  "output": "Tests import and invoke `log_event` directly, and `test_log_event_does_not_invent_state_for_a_missing_case` asserts `log_event(tmp_path / \"not-a-case\", ...) is not None`, demonstrating that the standalone writer is a reachable entry point and creates its destination without prior case validation."
                },
                {
                  "command": "read anomaly/.jeff/tasks/0027-resolver-consolidation/task.md",
                  "output": "Acceptance explicitly requires: `Whole-case no-symlink validation protects every public resume/read/write path.`"
                }
              ]
            }
          }
        ],
        "evidence": [
          {
            "command": "read /Users/tomvaillant/buried_signals/tools/anomaly/scratchpads/security-final/security-audit-20260824-121514.md",
            "output": "The supplied deterministic scan covered all 25 src/anomaly files, reported six high regex hits, two scanner gaps, no critical findings, no dependency vulnerabilities, and REVIEW. The hits were ast.literal_eval plus five interpolated DuckDB statements; GitHub Actions and XSS were the two scanner gaps."
          },
          {
            "command": "read src/anomaly/detect.py:60-115,350-390,600-660; src/anomaly/prepare.py:450-510; src/anomaly/profile.py:120-235",
            "output": "All six machine hits are refuted as exploit paths. ast.literal_eval is limited to quoted scalar parsing and does not execute code. Detector PRAGMA values are positive integers after bool/type validation. Prepared table identifiers are generated as tbl_ plus a SHA-256 digest and views come from enumerate. Profile table IDs must match tbl_[0-9a-f]{64} before interpolation. DuckDB execution is read-only with external access disabled."
          },
          {
            "command": "read src/anomaly/_attempt_workspace.py:57-150,166-272 and src/anomaly/workflow.py:158-180",
            "output": "Recovery now requires the exact registry-derived write set and validates journal/entry schemas, bounded phase attempts, entry states, and backup/live coherence. However original=false with status applying/applied is considered coherent solely when backup is absent; rollback then deletes the live path. Because P7's registry tuple includes README.md, the constructed exact-set journal remains accepted and can delete journalist content before state resolution."
          },
          {
            "command": "read src/anomaly/attempts.py:29-125 and src/anomaly/state.py:20-110,184-195",
            "output": "run_attempts calls load_snapshot before any root scan. load_snapshot's durable-tree initialization performs mkdir, touch, temporary write, and replace operations beneath root/.anomaly. The only scan in run_attempts occurs after the workspace has been copied and the owner has executed, so it cannot prevent entry-time symlink traversal."
          },
          {
            "command": "read src/anomaly/events.py:25-72; grep direct log_event call sites",
            "output": "log_event performs parent mkdir and append-open directly beneath the supplied case root, catches only resulting exceptions, and has no containment check. Direct call sites exist in tests/test_events.py; only callers routed through phase_event receive the newly added pre-call scan."
          },
          {
            "command": "uv run python -c 'from anomaly.semantics import redact_credentials; print(redact_credentials(\"AWS_SECRET_ACCESS_KEY=awsSecretValue1234567890abcd OPENAI_API_KEY=openaiSecretValue1234567890abcd\"))'",
            "output": "AWS_SECRET_ACCESS_KEY=[redacted] OPENAI_API_KEY=[redacted]"
          },
          {
            "command": "read src/anomaly/review.py:589-658,1030-1085; src/anomaly/report.py:1-300; src/anomaly/identities.py:1-170; src/anomaly/readme.py:1-70",
            "output": "Compound error redaction now reaches the shared durable boundaries. Dataset-derived report text is credential-redacted, whitespace-collapsed, HTML-escaped, Markdown-metacharacter escaped, and bare HTTP(S) colons neutralized; SVG text is XML-escaped. P7 completion identity covers findings/report/unresolved, Gate B and chart receipts, and the charts directory including descendants and empty-directory existence. README completion links remain derived and marker-bounded."
          },
          {
            "command": "read pyproject.toml; jj diff --from 98542737b170 --to 429c34deae1b --stat src/anomaly tests skills/anomaly agents/anomaly-data-reviewer.md PRD.md; jj log -r @- --no-graph -T commit_id",
            "output": "No dependency manifest change was introduced; runtime dependencies remain pyarrow, duckdb, and pytz. GitHub Actions is outside the supplied src/anomaly scan and changed product scope. The audited parent revision is exactly 429c34deae1b9f5a92252147ec16acebfcb4812b; supplied gate evidence records it clean with the full suite green at 766/766."
          }
        ],
        "reportedVerdict": "needs-work",
        "scan": {
          "command": "/Users/tomvaillant/.omp/plugins/node_modules/@johanthoren/jeff/skills/security-auditor/scripts/review-security.sh src/anomaly --report-dir scratchpads/security-final",
          "recommendation": "REVIEW",
          "reportPath": "/Users/tomvaillant/buried_signals/tools/anomaly/scratchpads/security-final/security-audit-20260824-121514.md"
        },
        "coverage": [
          {
            "category": "secrets",
            "status": "covered_no_hits"
          },
          {
            "category": "injection_sql",
            "status": "covered_no_hits"
          },
          {
            "category": "injection_command",
            "status": "covered_no_hits"
          },
          {
            "category": "path_traversal",
            "status": "covered_with_hits"
          },
          {
            "category": "insecure_deserialization",
            "status": "covered_no_hits"
          },
          {
            "category": "weak_crypto",
            "status": "covered_no_hits"
          },
          {
            "category": "dynamic_execution",
            "status": "covered_no_hits"
          },
          {
            "category": "tls_transport",
            "status": "covered_no_hits"
          },
          {
            "category": "xss",
            "status": "covered_no_hits"
          },
          {
            "category": "sensitive_logging",
            "status": "covered_no_hits"
          },
          {
            "category": "insecure_permissions",
            "status": "covered_no_hits"
          }
        ]
      },
      "agents": {
        "reviewer_agent_id": "ReviewAnomalyFinalCorrect",
        "reviewer2_agent_id": "ReviewAnomalyFinalStandards",
        "audit_agent_id": "AuditAnomalyFinal"
      }
    },
    {
      "at": "2026-08-24T14:16:17Z",
      "review": {
        "verdict": null,
        "reviewer_agent_id": null,
        "findings": [],
        "evidence": []
      },
      "review2": null,
      "audit": {
        "required": true,
        "verdict": "na",
        "audit_agent_id": null,
        "findings": [],
        "evidence": []
      },
      "agents": {
        "reviewer_agent_id": null,
        "reviewer2_agent_id": null,
        "audit_agent_id": null
      }
    }
  ]
}
