# Notes — 0014

Operator selected the finite product boundary. Blocked decoder evidence remains at jj bookmark `task-11-blocked` (`404ccaf4`).

Reuse correctness tests and implementation ideas only. Do not import task 0011's evolving Parquet object-memory estimator or claim sandbox guarantees. The required unresolved correctness item is JSONL physical LF/CRLF handling, including valid U+2028 inside JSON strings.
