# Notes — 0015

Blocked task-13 evidence remains at jj bookmark `task-13-blocked` (`95c879eb`). This task owns only the six final semantic findings: canonical identity collisions, cross-platform drive-relative paths, manifest/receipt consistency, lifecycle identity schema, duplicate acquisition IDs, and distinct fork lineage.

Do not import task 0013's filesystem matrices or expand into concurrency/resource guarantees.
