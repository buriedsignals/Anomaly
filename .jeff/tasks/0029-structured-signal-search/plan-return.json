{
  "stage": "plan",
  "result": "red",
  "complexity": "complex",
  "auditRequired": true,
  "refactorOpportunity": "Extract review._sanitize's sensitive-key removal plus credential redaction into a shared semantics.sanitize_public_value helper and reuse it unchanged from review and search, preserving the existing review field allowlist and serialized outputs.",
  "slices": [
    "Extract and reuse the existing public-value sanitizer without changing replay, draft, or report behavior.",
    "Add a contained, atomic, clock-free DuckDB signal projection and deterministic manifest bound to signal JSONL, run provenance, and detector snapshot hashes.",
    "Add validated structured filters, deterministic lexical matched_on details, separate query scores, canonical references, stable keyset pagination, and explicit stale-projection rejection."
  ],
  "testFiles": [
    "tests/test_signal_search.py"
  ],
  "redRun": {
    "command": ".venv/bin/python -m pytest tests/test_signal_search.py -q",
    "output": "10 failed in 0.12s; every collected case completed isolated fixture setup, then failed at the expected production boundary with ModuleNotFoundError: No module named 'anomaly.search' (artifact://89)."
  },
  "escalation": null
}
