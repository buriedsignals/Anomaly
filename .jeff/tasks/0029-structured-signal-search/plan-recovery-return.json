{
  "stage": "plan",
  "result": "red",
  "complexity": "complex",
  "auditRequired": true,
  "refactorOpportunity": "Harmonize schema-v2 detector snapshot path/hash validation currently duplicated between strict replay and signal projection into one shared pure validator after corrected behavior is established, preserving public outputs and errors.",
  "slices": [
    "Persist immutable content-addressed detector snapshots and adopt composite (signal_id, run_id) projection/reference/cursor semantics for canonical repeated runs.",
    "Enforce the exact public redacted payload, independently discriminating filters, complete detector metadata search, full matched_on details, malformed-key errors, and retrieval-only scores.",
    "Preserve explicit stale rejection/rebuild while pinning verified reads and canonical/derived filesystem objects across deterministic rebuild and symlink-swap interleavings."
  ],
  "testFiles": [
    "tests/test_signal_search.py"
  ],
  "redRun": {
    "command": ".venv/bin/python -m pytest tests/test_signal_search.py -q",
    "output": "10 failed, 21 passed in 0.84s; intended production seams only. Full output: artifact://186"
  },
  "escalation": null
}
