{
  "cycle": 2,
  "evidence": [
    {
      "command": "git -C anomaly show --no-patch --format='%H %s' f1f404c89e11b388687e90ff07b17e46940d0ffd; byte-compare show(sha:<file>) vs worktree",
      "output": "f1f404c89e11b388687e90ff07b17e46940d0ffd 'task 29 \u00b7 path-race verification checkpoint'; testSameAsWorktree=true projSameAsWorktree=true searchSameAsWorktree=true"
    },
    {
      "command": "grep -n 'verified_projection|read_rows|projection.close|def search_signals' src/anomaly/search.py @checkpoint",
      "output": "49:def search_signals(...); 61: projection, manifest = verified_projection(root); 72: candidates = _rank_rows(read_rows(projection, filter_columns), terms); 93: projection.close() \u2014 single query-time open site, no pathname reopen elsewhere"
    },
    {
      "command": "sed-equivalent read src/anomaly/_signal_projection.py @checkpoint lines 186-243, 523-550",
      "output": "232: projection = _open_private_projection(projection_bytes) \u2014 after manifest schema/input-order/projection_identity checks, per-input _safe_input_file hash re-reads, projection-bytes sha256==manifest['projection_hash']; _open_private_projection writes bytes to tempfile dir 0o700, duckdb.connect(read_only=True), PRAGMA enable_external_access=false, os.unlink(path)"
    },
    {
      "command": "grep -n 'monkeypatch.setattr|original_verified_projection|continue_search.wait' tests/test_signal_search.py @checkpoint",
      "output": "662: original_verified_projection = api.verified_projection; 665: projection = original_verified_projection(*args, **kwargs); 670: setattr(api,'verified_projection', pause_after_verification) \u2014 pause is AFTER full verified_projection return (open already done under either implementation); 726: setattr(projection,'_safe_input_file',\u2026) and 754: setattr(projection,'_prepare_search_root',\u2026) \u2014 build-time hooks only; tests at 700/735/765 call api.build_signal_projection exclusively, never api.search_signals"
    },
    {
      "command": "grep -rn 'search_signals|verified_projection|_signal_projection|signals\\.duckdb' tests/",
      "output": "matches only in tests/test_signal_search.py (lines 290-781); zero references in test_case.py, test_pipeline_walk.py, test_events.py, test_workflow.py, test_review.py, test_acquire.py, test_recommend.py, test_skill.py"
    }
  ],
  "finding": "tests/test_signal_search.py:654 No behavioral test distinguishes the private-copy repair from the previously green mutable-pathname reopen.",
  "rationale": "Reverting _signal_projection.py:232 (_open_private_projection, def :523) to duckdb.connect on the verified .anomaly/search/signals.duckdb pathname leaves every current test green: the sole query-time test (:654) pauses only after verified_projection fully returns \u2014 after the open under both implementations \u2014 so its concurrent os.replace rebuild hits an already-open connection/inode either way, and the three symlink-swap tests (:700,:735,:765) hook build-only helpers and never invoke search_signals.",
  "source": "review2",
  "stage": "refute",
  "verdict": "survives"
}
